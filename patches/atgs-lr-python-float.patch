diff --git a/utils/general_utils.py b/utils/general_utils.py
index ed174a8..7afd8be 100755
--- a/utils/general_utils.py
+++ b/utils/general_utils.py
@@ -64,7 +64,7 @@ def get_expon_lr_func(
             delay_rate = 1.0
         t = np.clip(step / max_steps, 0, 1)
         log_lerp = np.exp(np.log(lr_init) * (1 - t) + np.log(lr_final) * t)
-        return delay_rate * log_lerp
+        return float(delay_rate * log_lerp)
 
     return helper
 
@@ -160,4 +160,3 @@ def safe_state(silent):
     np.random.seed(0)
     torch.manual_seed(0)
     torch.cuda.set_device(torch.device("cuda:0"))
-
