**Iteration 5 REVIEW complete. Implementation acceptance remains incomplete; live S1 calibration recovery is NOT READY and cannot safely be admitted.** Existing corrections can be retained, but several guards need correction before missing validation can establish readiness.

Read the objective, latest loop status, assessment-007, validation-004, implementation-review-003, Plan 035, AGENTS.md, tracked diff, untracked S1 recovery/evidence/tests, correction/bookkeeping records and production ledger. This review writes only review-005.md and assessment-008.json. The explicit request overrides standing implementation/commit/delegation guidance. No prompts content was read.

**Evidence and work already supported.** Validation-004 remains `status=incomplete`, with no aggregate receipt. Its recorded focused run passed **13 methods, zero failures/errors/skips, exit 0, 42.534 seconds**. I verified the saved log against its embedded output and current source hashes; I did not rerun that suite. Subtest outcomes were not recorded individually. Static collection still describes 135 methods across the prescribed nine suites; that is collection information, not a current 135-test pass. Earlier 135/79 results do not validate the changed source.

Retain these improvements rather than rebuilding them:

- L1's correction propagation and real disposable controller/admission/registration/reservation/supervisor/acceptance/publication/finish/resolution fixture now work in the recorded focused run. Actual source membership, AssetBundle, annotation/input admission and 510-row guards are used; only external worker/device/host boundaries are faked. The original failed arm remains failed. Boundary negatives and actual successful `segment` progression remain open.
- L2 now validates lifecycle order and rechecks admission under lock. The previously accepted orphan start is now rejected by a disposable probe with `S1 lifecycle orphan/duplicate start`. This closes that specific defect, not concurrent-controller safety or the full event-binding contract.
- The numerical first-failure fixture now reaches `invalid raw S1 logits/probabilities/boxes`, preserves raw evidence and stops after one adapter call. Keep the existing malformed-label, serialization, native capture/reset and lower-level semantic/refinement cases. The positive serialized row already exercises an empty native phrase, exact tie and ambiguity; zero/skip options remain unused, and other numerical mutations remain uncollected.
- Explicit S1 CLI selection precedes authorization decoding. Worker `failure.json` directory creation is now guarded. Terminal parsing is partly isolated, and initial/partial/final runtime are considered. Acceptance and terminal publication now precede finish; historical resolution verifies acceptance and referenced bytes instead of repeating all numerical row qualification. These changes are useful but not sufficient.
- Monitor work and sampling now use separate CPU helpers instead of the repeating SIGALRM handler. This removes the previous signal-callback design; it does not establish a hard bound.

**Confirmed remaining findings.** Eight disposable CPU guard probes ran in **2.7705262700037565 seconds**. They used real source membership and existing synthetic prerequisites, with no production controller, device or model calls. The lower-level lifecycle fixture constructs synthetic admission; these probes are not controller integration, races, a full suite, or live evidence. Exact reproducer and outcomes are embedded in assessment-008.

| Finding | Evidence and implication | Smallest correction |
| --- | --- | --- |
| **F5-1 / L2: consumed lifecycle does not fully bind the canonical request** | `s1_recovery.py:341` verifies the reserved request's file record, but not its canonical contents; the consumed path does not call the comparison at `:409`. A disposable hash-valid reserve suffix rebound to a request with `branch=reconstruction` was accepted by `validate_binding(consumed=True)`; its new file record also passed `strict_record`. This is a missing tamper-rejection guard, not proof that the controller emitted or launched that request. Reservation initially and resolution later have stronger comparisons. | Share canonical request/worker/reference checks across consumed lifecycle, prelaunch and acceptance, without re-entering ledger locks. Check the active reservation against the supervisor's captured reservation/command as applicable. Add the intended boundary mutation and real synchronized races. |
| **F5-2 / P1: optional corruption can still prevent a minimal terminal receipt** | `terminal_receipt`'s optional wrapper at `s1_recovery.py:519` omits structural errors such as `AttributeError`. A valid JSON `[]` in `initial-runtime.json` reaches `qualify_runtime`, raises `'list' object has no attribute 'get'`, and leaves no terminal receipt. Invalid JSON alone is an insufficient corruption test. | Validate parsed types before field access; isolate optional evidence errors from trusted consumed identity and minimal publication. Preserve the primary failure and attach parsing/publication errors. Test scalar/list/object corruption as well as absent/truncated files. |
| **F5-3 / P1: produced lower bounds are still based on readable files** | At `s1_recovery.py:562`, produced rows are only hashed/parsed. Two files containing `{}` yielded `produced_lower_bound=2`, `qualified=0`. No valid row or unique identity supported that lower bound. | Validate produced-row structure, identity and available artifact bindings; deduplicate by admitted identity and reconcile order. Keep produced distinct from semantically qualified. Missing/unverifiable totals remain unknown. Reuse valid result rows when appropriate; do not discard readable qualified evidence merely because its partial filename is absent. |
| **F5-4 / P2: monitor and finalization are not fully bounded** | `supervisor.py:132` blocks in `waitpid(pid, 0)`; cleanup at `:319` has no resource sampling; dispatch supplies `poll_seconds=2.` at `execution.py:540`. Final ledger locking/write/fsync at `supervisor.py:358` occur after elapsed is measured and outside the monitor. A probe returned `work accepted` for sampler `{}`: missing fields default to healthy zeros at `:159`. The recorded multithreaded-fork warning remains unresolved. | Bound helper startup/reaping and ledger finalization, maintain <=100 ms monitor ticks and <=1 s/remaining-time sample limits throughout monitored phases, and reject missing/invalid resource readings. Include cleanup and finalization in the explicit charged scope; do not substitute an overrun flag for timely interruption. |
| **F5-5 / A4: receipt membership and execution metadata can be fabricated coherently** | `required_cases` at `s1_recovery.py:87` finds only method names and whether subTest appears. `validation_record` at `:69` accepts any unique nonempty passing subtest identities. A known-good synthetic receipt passed; replacing required subtests with unrelated identities also passed. A one-test command naming all suites, with inconsistent argv, null stdin, negative elapsed and zero aggregate metadata, was accepted while retaining claimed full collection. | Declare exact required parameter tuples in source-bound literal tables; validate method/subtest membership, outcome and all execution metadata/counters together. Independently reconcile actual execution in review; synthetic receipts remain fixture-only. |

Additional source-backed obligations belong to those same fixes, not new workstreams:

- A cleanup exception can still produce `cleanup_confirmed=True` when the fallback PID list is empty (`supervisor.py:319–335`). Track uncertainty independently of survivor count, including helper descendants. Never signal foreign processes.
- Helpers stringify exceptions (`supervisor.py:87,116`). Resource/sample failures during acceptance can inherit `result_contract, stop_required=False`, rather than their true stop classification. Preserve typed failure information and primary/secondary errors across helpers; keep receipt and finish classifications/resources consistent, including observations after receipt preparation.
- Publication can start expensive partial-row reconciliation after the work deadline and repeat first-row qualification through `reconcile_first`. Full acceptance/partial qualification belongs before the work deadline; the cleanup reserve funds bounded termination and minimal outcome publication. A timeout must retain consumed/unresolved truth, not infer cleanup or completion.
- Supervisor-generated missing-first evidence calls `first_record`, whose clock comes only from the worker environment (`s1_evidence.py:25,67`); that variable is set in the Popen environment, not the supervisor. Pass trusted reservation timing explicitly. `verify_first`/`reconcile_first` use a hardcoded 3,600-second bound; the effective reservation may be shorter. Enforce effective timing before advancement, and validate finite/nonnegative result timing/resource fields and their relationships. These concerns are source inspection findings, not additional executed probes.
- Generic CLI dispatch still assumes a decoded dictionary after its parse guard (`basketball_vipe_benchmark.py:177`); a JSON list can escape the protected block path. Terminal publication in that generic parse-exception path can replace the original exception. Include both explicit S1 selection and malformed/untyped generic entry in P1 coverage.

**Smallest concrete remaining set.** Extend the current fixtures and narrow guards. The following five packages subsume L1/L2/A1–A4/P1/P2/V1; no new architecture, scientific protocol change or repeat of credited implementation is needed.

1. **Lifecycle/binding and concurrency (L1/L2; F5-1).** Keep the real controller positive fixture. Add a stable dependency-by-applicable-boundary table for admission, registration, reservation, prelaunch, acceptance/resolution: amendment/parents, config, source membership/bytes, validation, correction/predecessor, authorization, E1 qualification/assets/runtime, inputs, annotation policy/review/amendment event, original failure/artifact/event/cleanup, historical/canonical request, worker and event references. Rehash outer containers to reach intended inner guards; explicitly mark inapplicable cells. Cover invalid prefix/head/chain/snapshot, scientific mutation, unrelated/orphan/duplicate/out-of-order appends, broken/missing bookkeeping transitions and unrecorded status edits. Use barriers with real disposable ledger locks for simultaneous admission/registration and reservation contenders; assert at most one successful registration/reservation/launch, deterministic loser rejection and no deadlock. Retain active/reduced/exhausted/typed-scope/second-identity tests; add identical registered-but-unreserved block/continuation and no replay after consumption, including prelaunch failure.

2. **Durable failures (P1; F5-2/F5-3).** From the valid fixture, exercise the actual CLI with missing/unreadable/malformed/wrong-type/changed authorization and verify an immutable unconsumed block without occupying the consumed terminal path. After reservation, cover first adapter failure, the retained numerical failure, later-row failure, missing/corrupt result/partials/runtime, abrupt worker death, controller/supervisor death without finish and cleanup uncertainty. Preserve verified failed/not-reached phase/time/error, and earlier passed first/raw evidence after a later failure. Parameterize mkdir/serialization/write/hash/immutable-conflict failures at raw, first-result, failure.json and terminal boundaries. Assert primary error survives, secondary errors are recorded, verified unique partial counts/order and 340/170 categories are truthful, total-unwritable storage is reported as unavailable, and missing finish never resolves or retries. Validate produced evidence without requiring it already pass semantic qualification.

3. **Bounded monitor and accounting (P2; F5-4).** Use real controllable CPU children, fake device readings and short allocations; no GPU probe. Cover expiry immediately before Popen; blocked hash/decompression/sample/write/ledger lock/fsync/reaping; slow and invalid samples without reentry; foreign PIDs before launch, after worker exit and during acceptance/publication/cleanup; changing memory/artifact/download caps; reduced deadlines and an in-allocation cleanup reserve; terminal failure and worker/controller death. Assert observed elapsed bounds, no new expensive work after the work deadline, bounded owned-process cleanup or explicit uncertainty, no signals to foreign PIDs, at most eight CPU workers, accurate final charge and matching stop/failure classifications. Resolve multithreaded-fork safety; an unbounded wait or missing sample must not masquerade as successful monitoring. Receipt alone is never completion; a matching valid finish is required.

4. **Adversarial contracts and real worker progression (A1–A4; F5-5).** Reuse existing numeric/runtime helpers and the credited default tied/empty/ambiguous row. Add collected zero detections, skipped oversized detections, overwritten survivors and remaining ambiguity cases. Mutate processed RGB shape/finiteness/resize/normalization; raw logits/sigmoid/nonfinite arrays; retained order/thresholds; normalized/pixel boxes; UTF-8/offsets; each detection's index/id/score/assignment and surviving semantic index/id/box/class. Runtime cases must reach admitted-to-loaded relationships: changed GroundingDINO/SAM trees, E1 import file/symbol/source, inventory/build identity, missing required native mapping versus unrelated mapped libraries, nested/missing records, wrong interpreter, aliases/duplicate paths/modules, empty manifests and forbidden roots/modules. Envelope cases cover schema/status/job, exact camera 0/frame 50/pair_start null, count/order/identity, amendment/authorization/request/runtime/checks/raw evidence, effective timing and resource consistency. Drive real `segment` with a fake backend and valid serialized evidence: durable pass before loading frame 62, full 510 order and 340/170 split, verified identical pass reuse only, stale/conflicting/failed/not-reached records preventing advancement, and later failure retaining earlier pass/raw evidence. Retain one-call first-failure cases and add explicit input-load observations. A4 must reject zero/missing/extra/duplicate/aliased/stale sources/cases, failed/error/skipped methods/subtests, passing methods with failed subtests, substituted/missing/duplicate parameter tuples, wrong order/totals/metadata, the one-test command trick and disjoint receipts. Include these receipt-rejection cases in the final aggregate itself.

5. **One final current-source validation and handoff (V1).** After final changes, run the Plan 035 CPU aggregate in this exact order: `s1_semantics, s1_recovery, backends, contracts, component_recovery, execution, budgets, supervisor, review_annotations`, using `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 .local/envs/stg-colmap/bin/python -B -`. Capture actual argv, full runner stdin, stdout/stderr, process exit, UTC/monotonic start/end/elapsed, ordered discovery, every method and exact subtest outcome, per-suite and aggregate counters. Require zero failures/errors/skips and exact declared membership; do not hardcode 135/79. Recompute source_paths membership and hashes, current amendment/parents/correction/prerequisites, frozen records, ledger prefix/chain/head/totals, bookkeeping and index; run the prescribed diff check. Any later source/test/case-table change invalidates that validation. Preserve all prior artifacts; use next unused correction/validation/implementation-review/preparation suffixes, not Plan 035's now-occupied filenames. Finish CPU work with review and a non-executable preparation record.

**Objective and preservation assessment.**

| Criterion | Iteration 5 decision |
| --- | --- |
| S1-1 | Retain source-bound semantic CPU support and the numerical-fixture repair; adversarial/worker coverage incomplete, no live output claim. |
| S1-2 | **Not met.** Positive CPU admission exists, but implementation acceptance remains incomplete and no production authorization/registration exists. |
| S1-3 | **Not met.** No bounded live recovery or cleaned-up recovery outcome exists. |
| S1-4 | **Preserved within audited scientific scope.** Existing implementation and historical evidence remain unchanged by this review. |

All **70 source/config/test records** match validation-004 and assessment-007; source membership is exact. All **38 frozen scientific records** match. The **447-event, 332,437-byte** production ledger has a valid chain, SHA-256 `2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`, head `00cab019d73b6a3205f676b54cfb2f745f492fa5cca5c69b4567087cea62172b`. There are no active reservations or S1 recovery events. Original failed finishes 250/255 and R-S skip 256 remain unchanged. GPU totals remain **30 historical attempts, 4,374.044265462899 seconds, zero reserved**; this is accounting, not a device-availability claim. Four bookkeeping transitions end at the unchanged current loop status. The index and tracked diff match assessment-007; diff check passes. Historical P31-5 limitations and unavailable older status bytes remain explicit.

The next CPU implementation continuation retains **1,800 wall seconds/eight CPU workers**, timing from inspection, and truthful incomplete output on exhaustion. Iteration 4's inspection-inclusive timing remains unknown; the earlier 642.618033546998/518.3559243239979-second measurements exclude inspection and the 848.010818-second bound has an unknown exact start. No historical timing or benchmark counters are repaired/reset.

Only after all CPU gates close may a separately authorized DO stage consider `S1-calibration-recovery-001`: **one attempt**, `min(3600, 93600 - gpu_elapsed_seconds - gpu_reserved_seconds)` positive seconds, with cleanup reserve `min(30, effective_seconds/4)` inside that allocation. Keep one exclusive GPU process group, 22 GiB device memory, eight CPU workers, 150 GiB artifacts, 60 GiB cumulative new downloads, existing 57,600-second preparation/setup ceilings, and zero new setup/download/smoke jobs. Revalidate current bindings and resources at the applicable locks/boundaries. Reservation consumes the identity even without launch. Stop after the single calibration outcome and review; reconstruction requires later separate authorization. R-S and scientific aggregation/report reruns remain excluded.

No source edits, GPU/model/device work, production authorization/controller/ledger mutation, staging, commit or delegation occurred. No focused suite or aggregate was rerun. The eight diagnostic probes establish specific guard behavior only. This review grants no live admission.
