# Iteration 12 PLAN — Bounded exact-source declaration parse reuse

Created [Plan 042](../../../plans/plan_042.md) from [review-012](review-012.md),
[assessment-026-review](assessment-026-review.json), current source/evidence and
the unchanged [objective](objective.md). Latest criteria assessment:
[assessment-027-plan](assessment-027-plan.json).

Scope R12-1: a process-local 16-entry LRU of successful pure declaration parses,
keyed by exact full text and module name, with defensive deep copies on every
public return. Collection always rereads every suite file. No file/hash,
receipt/result or authority caching. Changes are confined to the contract
parser, new DeclarationCacheTests and the runner's focused selector. Preserve
all 169 current methods, 426 callbacks and existing substantive checks.

New IMPLEMENT allocation: **1,800 wall seconds starting before inspection**,
**1,620-second execution/refinement cutoff**, **180-second evidence reserve**,
at most **three focused invocations of 120 seconds** and **two full aggregates
of 300 seconds**. Every launch requires a successfully written, synced and
verified prospective note and a full timeout that still fits. Preserve one
final aggregate; optional repeats need an invalidating change or concrete
concern. Eight CPU workers including owned/native/helper threads, all thread
settings one, serial suites and existing bounded script children. Zero GPU,
models, production API work, setup/download/smoke or scientific reruns.

The final aggregate alone supplies observed outer/recovery timing and headroom
against 258.0717323209974 / 252.30142017400067 seconds. Real parser call-count
reduction is required; there is no promised speedup threshold or extra benchmark.
Skill standing approval covers this new CPU allocation after main records the
unchanged-ceiling comparison and dispatch authority. Historical allocations
stay consumed.

Correction-010 preserves correction-009's full prefix and appends canonical
post-011 directly using its durable IMPLEMENT/REVIEW snapshots, then actual
PLAN/IMPLEMENT transitions. Main freezes IMPLEMENT before dispatch and owns
staging/commits. Fresh validation-011/audit-011 and iteration-012 timing/process
evidence will establish implementation outcomes; planning does not do so.

Plan 041 strict acceptance remains false, with its missing prospective note,
prior child thread values, failed runs and raw-log whitespace disposition
preserved. S1-1/S1-4 retain support; S1-2/S1-3 remain not met. Helper bounds,
durable progress, finalization and remaining matrices/live recovery stay open.
No tests, source edits, production APIs, GPU/device work or git writes occurred
during this direct PLAN stage.
