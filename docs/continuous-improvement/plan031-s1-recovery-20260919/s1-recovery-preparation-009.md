# Non-executable recovery handoff after Plan040

Live admission is **not ready**. This document confers no execution authority.
Plan040 closes local numerical envelopes and six direct script lookups only;
validation009 is current source-bound CPU evidence, not a live authorization.

Remaining prerequisites:

- R9-2: bounded helper startup/transport and fresh resource samples
- R9-3: trusted reservation clocks, effective allocations and first-result time
- R9-4: durable incremental reconciliation progress
- R9-5: structured primary/secondary errors, observed cleanup/finalization, conservative charge and acknowledgment
- R9-6 remainder: A1-A3 native/runtime/envelope matrices, including zero-detection and oversized-box semantic cases
- Actual stages.segment successful progression through all510 identities, first publication/reverified reuse/later-failure preservation
- Full applicable-boundary, synchronized S1 contender, continuation/consumed replay and typed-lifecycle matrices
- Canonical current binding plus production authorization/registration only after implementation acceptance
- Separately authorized single calibration recovery; reconstruction remains separate
- Historical P31-5 and unavailable older timing/status evidence remain unclosed

Only a later authorized DO after all CPU gates close may consider the one
S1-calibration-recovery-001 allocation. Preserve effective seconds
min(3600,93600-gpu_elapsed_seconds-gpu_reserved_seconds)>0 and cleanup reserve
min(30,effective_seconds/4) inside that cap. Reservation consumes the attempt
without launch; charge preparation/loading/qualification/publication/cleanup/
finalization. One exclusive GPU group,22GiB,eight CPU workers,150GiB artifacts,
60GiB downloads and existing57600-second preparation/setup ceilings remain.
No setup/download/smoke allocation is reopened. Reconstruction is separate.

Main must preserve frozen IMPLEMENT status bytes before transitioning and append
a later canonical bookkeeping transition/correction when another current
binding is required. Commit is a checkpoint; continue fresh REVIEW afterward.
