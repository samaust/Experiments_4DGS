# Plan040 implementation result

Local numerical-envelope and six-script compatibility milestones are complete.
[Validation009](s1-recovery-validation-009.json) binds the finalized source to a
fresh exact nine-suite aggregate: **162 methods, 310 typed callbacks**, zero
failures/errors/skips/discovery errors, actual outer returncode0, no timeout,
182.624087055 seconds. The [independent audit](s1-recovery-audit-009.json)
reconciles the source AST/declarations, callback order/types/IDs, all74 source
records in all four snapshots, exact argv/stdin/environment, runner/capture,
closed raw logs, actual tool completion and six serial script children.
[Wrapper check010](s1-recovery-wrapper-check-010.json) passes the real read-only
validation_record guard. No source changes occurred after aggregate launch.

## Implementation and coverage

`s1_evidence` now requires exact built-in finite nonnegative duration values,
exact integer singleton groups, exact nonnegative allocator bytes, allocated <=
reserved, and both peaks <= the configured ceiling. Its bounded result helper
uses fsum and T=(n+1)*ulp(S), rejects overflowing accumulation and requires exact
zero total for zero sum. This adds representation consistency, not a scientific
or reservation-time allowance. Real qualification, terminal validation and
resolved-result paths use the guards. Structural produced-row verification stays
separate; a numerically invalid serialized row remains produced but unqualified.

Truthful synthetic fixtures now carry the stage-emitted fields. Literal cases
cover missing/null/string/bool/nonfinite/negative/foreign/unrepresentable values,
wrong group sizes and byte types, zero/nonzero/exact cap/copied smaller cap,
ordered accumulation and both signs inside/on/outside the ULP boundary, and
non-first-row mutations. The full positive fixture passes actual terminal and
resolver guards. Each resolver mutation has a fresh coherent result/acceptance/
receipt graph while preserving the original event-bound artifacts and events;
target-specific error assertions prove the numerical guard was reached.

All six suites use canonical filename/class/method declaration lookup while
retaining unittest.main. The six prescribed single-method script children pass
under __main__, with10-second execution and2-second cleanup caps, serially.
Raw argv, completion, timing and child outputs are retained in aggregate stdout.
The diagnostic selector now targets NumericalEnvelopeTests; the full aggregate
still runs all receipt tests and all nine prescribed suites unchanged.

## Attempts and preservation

All three focused allocations were consumed and retained. Attempt001 failed
discovery after a malformed literal-table insertion; syntax was corrected.
Attempt002 hit120seconds after the new class and much of the receipt matrix;
it also exposed fixture use of immutable write_json on existing mutation files.
Attempt003 completed in83seconds with four resolver stale-hash assertion failures.
The final correction preserved original event-bound files and wrote separate
mutation graphs. No failed diagnostic is promoted as passing evidence. The first
final aggregate passed; no second aggregate was used.

[Correction008](s1-recovery-baseline-correction-008.json) preserves its predecessor
and adds three canonical actual-byte transitions through the frozen IMPLEMENT
status, linking the preserved noncanonical post009 note. The [bookkeeping audit](s1-recovery-bookkeeping-audit-010.json)
verifies this lineage and all38 frozen records. The [ledger audit](s1-recovery-ledger-audit-010.json)
confirms447 events/332437 bytes, exact original hash and chain head, no active or
recovery job,30 historical GPU attempts and4374.044265462899 charged seconds,
zero reserved, and original events250/255/256 unchanged. Status was not edited.

[Timing010](s1-recovery-timing-010.json) records inspection-inclusive wall time,
all attempt consumption and the unchanged ceiling scopes. Zero GPU/model/live
controller/production-ledger/setup/download/scientific work occurred. Only
permitted disposable fixture APIs ran in collected CPU tests. No git writes,
nested delegation or prompts access occurred.

## Handoff

R10-1/R10-2 and Plan040 acceptance are complete. Wider implementation acceptance,
live admission and the objective remain false. S1-1/S1-4 are supported as met;
S1-2/S1-3 remain not met. Preserve the receipt milestone, historical P31-5 and
unavailable older status/timing limitations. Remaining work is listed in
[assessment022](assessment-022-implement.json) and [preparation009](s1-recovery-preparation-009.md).
Main should inspect and locally commit this task-only milestone, then continue
the authorized loop with fresh REVIEW. Main owns subsequent status snapshots
and successor bookkeeping bindings. This stage created no commit.
