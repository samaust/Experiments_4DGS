# Iteration 13 PLAN

Saved [Plan 043](../../../plans/plan_043.md) from [review-013](review-013.md),
[assessment-029-review](assessment-029-review.json), current helper/supervisor
source and the unchanged [objective](objective.md). Current planning assessment:
[assessment-030-plan](assessment-030-plan.json).

R13-1 is one bounded CPU milestone: one fixed work role and one fixed sample
role, ready before reserve; a retained native ownership thread isolates fixed
`os.posix_spawn` interpreter launches; nonblocking complete 64 KiB framed JSON
controls both directions; trusted sample acquisition and exact request/session
identity establish freshness and a new post-task sample. Setup cutoffs from the
first timestamp are 1 second for readiness, 2 seconds for the initial sample,
3 seconds including failed-setup cleanup. Kernel/native primitive or scheduling
overruns are failures; unknown launch/cleanup ownership stays a stop. No hidden
startup interval or replacement helper may convert failure into success.

The full reconciliation summary stays in an immutable verified evidence file;
the monitor carries its explicit record and publication expands the original
summary in the work helper. This is transport plumbing; incremental durable
progress and complete monitored finalization remain later gates.

New IMPLEMENT allocation: **3,600 wall seconds from before inspection**,
**3,300-second execution cutoff**, **300-second evidence reserve**, at most
**three focused 120-second** and **two aggregate 300-second** invocations.
At most 24 new serial child scenarios per invocation, each 2 execution + 1
cleanup seconds, 72 seconds total. Keep the existing six script-child limits,
176 methods, 426 callbacks, exact nine-suite aggregate and 300-second cap.
Eight total CPU workers including wrappers/owner thread/helpers/descendants,
all four native thread settings 1. Every launch needs a durable fsynced,
closed/read-back-verified prospective note and a full timeout fitting the
cutoff, preserving one required final aggregate. Zero GPU/device/model,
production API, setup/download/smoke or scientific execution.

The skill supplies standing approval for this new plan-specific allocation
after main records its comparison with unchanged applicable ceilings and
dispatch authority. Historical consumption is not reset. Main owns actual
status transitions and commits. Correction-011 will preserve correction-010's
prefix, append canonical post-012 and the existing durable PLAN transition,
then actual IMPLEMENT transitions. Check vacancy: audit-012 already exists;
next expected audit and independent audit are 013, with validation-012.

S1-1/S1-4 retain current evidence; S1-2/S1-3 remain not met. Plan 041 strict
acceptance remains false with its historical exceptions intact. Plan acceptance
requires all A43-1 through A43-5; it does not establish whole implementation,
live readiness or the objective. Remaining durable progress/finalization,
native/full-worker/matrix/lifecycle and live admission/recovery gates are listed
in the plan. This PLAN stage changed no source or status, ran no tests or
production operations, and performed no git writes.
