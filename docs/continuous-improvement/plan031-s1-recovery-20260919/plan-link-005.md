# PLAN 005 — Five remaining S1 recovery safety packages

Created [Plan 036](../../../plans/plan_036.md) from the [objective](objective.md),
[review-005](review-005.md), [assessment-008](assessment-008.json),
[Plan 035](../../../plans/plan_035.md),
[validation-004](s1-recovery-validation-004.json),
[implementation-review-003](s1-recovery-implementation-review-003.md) and
[status](status.md). **Live S1 calibration recovery remains NOT READY.**

Plan 036 retains credited implementation and specifies five CPU packages:

1. Shared canonical request/worker/command/reservation checks at consumed
   lifecycle boundaries, complete dependency mutations and real synchronized
   admission/registration/reservation races.
2. Durable block and terminal outcomes independent of optional corruption,
   typed primary/secondary failures and effective reservation-relative first
   evidence, with CLI/storage/death tests.
3. Structurally verified unique produced rows distinct from qualified rows,
   reconciled partial/result evidence, admitted order and truthful 340/170
   categories, lower bounds and unknown totals.
4. Bounded spawned CPU helpers, strict serialized sampling through cleanup and
   ledger finalization, ownership certainty, conservative charged finalization
   and durable acknowledgment before successful resolution.
5. Exact literal method/subtest membership and execution-metadata checks,
   remaining numerical/runtime/envelope cases and real `segment` progression
   before frame 62 through the full 510 rows.

V1 requires one final current-source aggregate in the prescribed nine-suite
order, actual execution/subtest records and independent reconciliation. The
recorded 13-method focused pass is not aggregate readiness; older 135/79 results
and the eight review probes cannot substitute. No test was run in this PLAN stage.

The later CPU implementation is capped at **1,800 wall seconds from inspection
and eight CPU workers**, using disposable test ledgers only. Production ledger
bytes remain unchanged through validation/preparation. Use next unused correction,
validation, implementation-review and preparation suffixes (currently 004, 005,
004 and 005 respectively), preserving predecessor correction-003 and all history.
End with review and a non-executable handoff; incomplete gates remain NOT READY.

Only a later authorized DO stage after CPU validation and fresh live checks may
consider `S1-calibration-recovery-001`: **one attempt capped at 3,600 GPU seconds**,
`min(3600, 93600 - gpu_elapsed_seconds - gpu_reserved_seconds)`, with cleanup and
finalization inside `min(30,effective_seconds/4)`. Reservation consumes the
identity even without launch. No reconstruction, R-S or scientific aggregate/
report rerun; reconstruction needs separate later authorization after calibration
review. Existing device, CPU, storage, download and setup ceilings stay intact.

The read-only PLAN audit matched all 70 source/config/test records, 38 frozen
scientific records, four bookkeeping transition records and 38 prior loop files
listed in assessment-008. The production ledger's 447-event chain is valid and
its 332,437 bytes retain SHA-256
`2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`,
head `00cab019d73b6a3205f676b54cfb2f745f492fa5cca5c69b4567087cea62172b`.
No active reservation or S1 recovery event exists in that snapshot. Historical
GPU accounting remains 30 attempts / 4,374.044265462899 seconds / zero reserved.
P31-5 and historical timing/status-byte limitations remain explicit.

This stage writes only Plan 036 and this link. No source edits, GPU/model/device
work, controller calls, ledger mutation, staging, commits or delegation. `prompts`
was not read. The explicit PLAN restrictions override standing commit/delegation
guidance. Neither document grants live admission.
