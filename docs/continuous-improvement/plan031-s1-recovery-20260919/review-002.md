REVIEW 002 — Plan 032 S1 recovery, 2026-09-19

The implementation stage remains incomplete and is **not ready for live admission**. Correct the failing fixture, repair the baseline/admission lifecycle and first-result/evidence guards below, then issue fresh passing CPU validation. A fixture-only correction is insufficient. This review grants no execution authority.

Reviewed HEAD: `0184cef74548261d4444ed9ee1e0464da1561b5e`, including the existing unstaged diff and new S1 modules/tests. Read `AGENTS.md`, objective, Plan 032, review-001/assessment-001, current loop status, baseline and failed validation, amendment, current changed source/tests, supporting contracts/runtime code, and the full ledger. The user's REVIEW restrictions supersede the general commit/delegation instructions. No source was edited, GPU/device probed, live admission called, job dispatched, staged change made, commit created, or work delegated. `prompts` was not read. The later instruction to finalize and stop is controlling; no implementation follows this review.

| Criterion | Assessment | Evidence and limit |
| --- | --- | --- |
| S1-1 | **met, source/CPU scope retained** | The amendment and its S0/native parents still hash correctly. The unchanged semantic policy applies period-delimited sigmoid-token sums to every retained query, with first-max ties, preserving native boxes/scores/phrases, thresholds, preprocessing, SAM and tracking. All five semantic tests passed in the current rerun. This does not qualify the new runtime/first-result guards or establish model quality. |
| S1-2 | **not met** | Substantial binding and locked reservation code exists, but validation failed, the real baseline blocks the lifecycle, and no production S1 authorization/admission/reservation exists. Required controller integration and guard tests remain incomplete. |
| S1-3 | **not met** | No recovery ran and no first-result/terminal recovery evidence exists. Durable early failure capture improved, but qualification, partial-result evidence and terminal publication remain incomplete. |
| S1-4 | **met, scoped** | Ledger and frozen scientific records are unchanged, as are original failures, aggregates and report. The mutable loop status changed between stages and is explicitly excluded from this scientific-preservation conclusion. No unrelated allocation was reopened. This does not repair historical P31-5 accounting failures. |

The saved `s1-recovery-validation-001.json` reports **134 tests, 0 failures, 1 error, 0 skips**, exit 1. I reran its exact nine-suite CPU command on the unchanged source: **134 tests in 1.748 seconds**, the same single error, exit 1; subprocess wall time was 1.9462302090032608 seconds. The earlier **122 passing tests** are reported by loop status, not a replacement for the later failed validation; no separate 122-test receipt was located among the loop artifacts. The current failed validation binds exactly 70 source/config/test files, all of whose records still match. Preserve this failed receipt and the older semantic validation; do not relabel either as passed.

The exact correction is in `tests/test_vipe_benchmark_s1_recovery.py:259`. For the `kind='contract'` subtest, line 260 constructs `SegmentationResult` with float32 labels. Its `__post_init__` immediately calls `contracts.instances` (`backends.py:266`), raising `ValueError: instances must be int32 on the image grid` before the mock is installed or `segment` is called. Construct the fixture with int32 labels, then mutate `prediction.labels` to float32 inside the mocked `predict` immediately before returning it for that subtest. This places the deliberate invalid output at the stage boundary being tested. Keep the production constructor and dtype contract intact. Tighten the assertion to the expected error, one adapter invocation, `failure_stage='output_contract'`, persisted available evidence, and no second input/result. The serialization and qualification subtests also need precise stage assertions rather than accepting any `ValueError` or `KeyError`.

The following findings must be addressed before claiming Plan 032 steps 1–4 complete. Locations refer to the reviewed tree.

1. **F2-1 — Blocking baseline/lifecycle contradiction (`s1_recovery.py:99–113`, `:289–295`).** `baseline.preserved_records` contains both the entire live `ledger.jsonl` and the mutable loop `status.md`. The validator rehashes every entry before checking the ledger prefix. Current status already differs: baseline SHA-256 `9816f2324668da37b6673284ff244263b1b41197241485d167c769da224d5b86` / 327 bytes versus current `342d5bbe57138c2edf08f7bb47f43f50cdc78d6654147aaa057dc24856e35dae` / 569 bytes. Even if that mismatch were removed, `common_admission` appends an event, so registration fails the full-ledger hash. A disposable CPU reproduction using a ledger-bearing baseline passed initial binding and failed registration immediately after an admitted event with `changed file: .../ledger.jsonl`. The existing tests hide this by using `preserved_records=[]`.

   Preserve baseline-001 as history. Add an explicit, immutable correction/binding manifest that retains exact hashes for frozen scientific records, verifies the original ledger **byte prefix and chain** while permitting only authorized appended events, and records loop-status transitions separately. Bind the prefix contract to the baseline snapshot, rather than accepting an unrelated authorization-supplied snapshot. Apply the same interpretation at admission, registration, reservation, prelaunch, result resolution and terminal preservation. Do not rewrite status to its old contents, reset ledger history, or broadly ignore mismatches. Add a realistic fixture containing the actual categories of baseline records.

2. **F2-2 — First-result numeric and metadata checks are incomplete (`s1_evidence.py:105–170`).** The new gate correctly verifies identities/provenance, labels/static, logits/probabilities, selected query indices/boxes/scores, caption IDs and recomputed assignments. However, `detector_rgb` is only required to exist and have non-object dtype; its shape, finiteness and native preprocessing relationship are not validated. Detection `box`, `id` and `index` are not checked against the selected query order and pixel conversion; native phrases are reused as inputs to recomputation without a separate native-output consistency check. Read-only CPU probes confirmed that the real `qualify_row` accepts both a processed RGB array containing NaN with shape `[1]` and a detection box changed to `[999,999,1000,1000]` while raw selected boxes remain unchanged. Complete these checks against the frozen native transformation and retained-query records, including skipped/overwritten detections and zero detections. Do not impose new semantic thresholds or reject valid ambiguity.

3. **F2-3 — Runtime and first-result envelope are insufficiently bound (`s1_evidence.py:32–46`, `:173–203`).** `qualify_runtime` checks version/isolation declarations and the outer loaded-runtime file hash but never validates that manifest's schema, interpreter, module/native file records or relationship to admitted E1 sources/runtime. A synthetic empty `{}` loaded-runtime manifest is accepted. Rehash and validate required loaded file/interpreter records, reject forbidden roots/modules, and compare the admitted runtime identity. `validate_result` omits first-result `job_id`, explicit identity, amendment, request, runtime, checks/raw-evidence and timing consistency. Also, `first_record` silently returns any existing record: a CPU probe calling passed publication after a failed record returned normally while leaving `status='failed'`. A failed/conflicting record must stop advancement; only an identical, fully verified passed record may be reused. Preserve a verified passed record on later failure. Add successful and adversarial tests of the real qualification functions.

4. **F2-4 — Failed/partial results and terminal publication need completion (`stages.py:147–197`, `basketball_vipe_worker.py:44–62`, `s1_recovery.py:264–316`, `execution.py:621–645`).** Early detector capture/reset and exception propagation now preserve available arrays without another forward. Nevertheless, completed row metadata after the first result remains only in the in-memory `rows` list until whole-job success. On a later failure, output arrays may survive but there is no durable manifest of all completed rows, their semantics and hashes. The compact writer counts rows only through `finish.result`; a failed job without that result is reported as zero actual rows despite completed outputs. Failure runtime is likewise lost from the compact receipt when `result` is absent. Persist immutable partial row/runtime records and distinguish produced, qualified and complete counts. Serialization failures are currently labeled `output_contract`; set the precise phase before writes and retain the original exception.

   Initial `validate_binding` is outside the controller's `try/finally`, so its pre-dispatch blocks do not produce the advertised blocked receipt. Conversely, a blocked invocation inside the `try` publishes the final fixed receipt filename. A later permitted continuation of the same registered but unreserved identity can run and then fail with a receipt filename conflict. Publication errors in `finally` can replace the original error. Use separate immutable pre-dispatch block evidence and reserve the single terminal receipt for the consumed attempt; reuse only an identical verified terminal record and never hide the primary error. Derive terminal completion from validated evidence, cleanup and preservation, not merely `finish.status`. Test clean failure, partial failure, pre-reservation block/continuation, abrupt death and publication conflict.

5. **F2-5 — Deadline coverage is incomplete (`supervisor.py:128–134`, `:163–175`, `:199–227`).** Reservation correctly narrows the available GPU time and reserves cleanup inside it. However, expensive prelaunch binding can consume that time without a deadline check immediately before `Popen`. After worker exit, synchronous full-result validation blocks the monitoring loop; the new 510-row validator repeatedly loads/hashes the input manifest and decompresses outputs without deadline/resource checkpoints. An overrun is recorded afterward, but the operation is not bounded while it runs. Require a remaining-time check before launch and bounded, interruptible validation/resource sampling through acceptance and evidence publication. Test these S1-specific paths with fake clocks/resources and workers, without GPU jobs. Do not weaken full membership validation or extend the allocation.

6. **F2-6 — CPU coverage does not establish the full lifecycle.** The S1 fixtures synthesize admission events instead of exercising `common_admission`/`execute_s1_recovery`; result resolution mocks both `qualify_row` and `qualify_runtime` (`test_vipe_benchmark_s1_recovery.py:201`). There is no successful unmocked first-result qualification fixture in that suite, and its qualification-error subtest reaches a missing diagnostics key rather than demonstrating rejection of corrupt numerical evidence. The validation guard checks suite-name substrings in command text, allowing a one-test receipt mentioning every suite; the fixture itself uses that shape. Record per-suite collection/results and require the planned suites/cases actually to run without skips. Add an integrated disposable admission→registration→reservation→fake worker→finish→resolution test, mutation/race/replay cases, and real numerical/runtime qualification tests. Keep model construction and device readings mocked, not the guards under review. Produce the missing implementation review with explicit dispositions of these findings.

Working protections should be retained: dedicated calibration-only schema and exact typed scope flags; generic S1 recovery rejection without weakening S2; current source/config binding; original cleaned-up failure and historical-versus-canonical request checks; E1 asset/runtime and annotation-policy binding; registration/reservation using locked event snapshots; duplicate/consumed identity rejection; cumulative deadline narrowing; and exact 510-identity membership/order checks at worker, acceptance and resolution boundaries. These are meaningful improvements, not proof that the untested edges above are complete.

The read-only ledger audit verified **447 events, sequences 0–446**, a valid chain, no active reservation and no S1 recovery events. SHA-256 remains `2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`, 332,437 bytes; head event `00cab019d73b6a3205f676b54cfb2f745f492fa5cca5c69b4567087cea62172b`. Original calibration/reconstruction failures and charges remain at sequences 250/255 with confirmed cleanup; R-S remains skipped at 256. All 39 other baseline preservation records match, including frozen Plan 031/study/protocol/research status, failure artifacts, E1 records, inputs/annotations, S0/native sources, aggregates and report. All five previously verified ledger result records rehash correctly. The loop-status difference is bookkeeping evidence, not a scientific-artifact mutation. Source/index and existing loop artifacts are unchanged by this review.

The concrete next step is a **CPU-only corrective implementation pass**, under the next stage's applicable authority: fix the fixture; correct baseline preservation semantics; close F2-2 through F2-5; implement F2-6 tests; rerun the exact nine suites from Plan 032 plus necessary new cases, `git diff --check`, source-set and preservation/chain checks; then save new immutable validation and implementation-review artifacts. Use `s1-recovery-validation-002.json` (or another unused, explicitly linked successor); keep failed validation-001 intact. Recompute the source set, currently 70 files. Bind the corrected baseline policy and actual artifact paths consistently. A passing fixture alone must not trigger GPU admission.

| Resource | Limit for continuation |
| --- | --- |
| Corrective implementation/CPU validation | At most 1,800 wall seconds, ≤8 CPU workers, serial where practical; record elapsed time and stop with remaining gaps if exhausted. This is a proposed next-pass bound, not a reset of any benchmark budget. The prior implementation recorded 642.618033546998 seconds since baseline, excluding initial inspection. |
| Immediate next pass | Zero GPU jobs/attempts, live admission/registration, setup builds, downloads, smoke probes or reconstruction. |
| Later calibration, only after passing review and applicable execution authority | Exactly `S1-calibration-recovery-001`, one attempt; `min(3600, 93600 − gpu_elapsed − gpu_reserved)` seconds including loading, qualification, serialization, validation and cleanup. Reserve `min(30, effective_seconds/4)` for cleanup inside that time. No second attempt. |
| Existing GPU ceiling | 93,600 cumulative seconds; currently 4,374.044265462899 used across 30 attempts, zero reserved, 89,225.9557345371 seconds remaining in time only. One exclusive RTX 4090 process group, total device memory ≤22 GiB. No device availability was measured here. |
| Existing CPU/setup ceilings | 57,600 seconds each; CPU preparation/scoring/report used 13,753.206793547044; setup used 5,316.549373747828 across 14 historical attempts. Charge actual benchmark preflight work appropriately; do not reopen consumed preparation/import/setup jobs. |
| Storage/download ceilings | ≤150 GiB artifact footprint and ≤60 GiB cumulative new downloads; zero new downloads proposed. Historical last finish: 67,755,470,848 artifact bytes and 26,213,724,097 download bytes; refresh actual usage only in an authorized execution stage. |

Keep all other Plan 032 exclusions: no reconstruction or R-S allocation, S0/S2/S3/S4, D1 or other depth/motion/neighbor/geometry work, finalists/combined arms, aggregation/report rerun, training, calibration regeneration, final-window evaluation, model/settings changes or CPU fallback. Reconstruction needs separate later authority after calibration review and an explicit R-S disposition. Follow AGENTS.md's single safe escalated retry policy if a later operation encounters a genuine or suspected permission restriction; a consumed attempt is never relaunched.

The accompanying assessment contains source/preservation records, the exact CPU rerun receipt, disposable probe code/results and resource accounting. The probes demonstrate guard behavior on synthetic data only. No repaired real-model output, quality improvement or fresh host availability is claimed. Stop after these two review artifacts, as requested.
