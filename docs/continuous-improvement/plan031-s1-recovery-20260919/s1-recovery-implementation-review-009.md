# Plan041 implementation review — reservation clock milestone

The reservation-clock technical milestone is validated by aggregate011-002 and independent audit010: 169 methods, 426 typed callbacks, all passing, actual 258.0717323209974 seconds, no timeout and a completed wait. Source count is 75, including the new clock module. Full implementation acceptance, live readiness and objective completion remain false. Strict Plan041 acceptance is also false because the execution-history exceptions below cannot be retroactively repaired.

The frozen `ReservationClock` value derives T, cleanup reserve, total deadline and work deadline from the captured real reserve event. Its serialized mapping includes the canonical request record, job, sequence/hash and boot. Exact numeric types, arithmetic and typed authority equality are checked. Recorded evidence bounds never read today's boot or clock; live advancement requires the recorded boot and a strict observation before W. Missing helper captured reservations are rejected.

Supervisor transports `VIPE_S1_RESERVATION_CLOCK` from its actual admitted reservation. Worker bootstrap compares it against the active ledger, canonical request, interpreter/script, operation and every CLI argument before importing stages or setting up a model. The controller's old start environment is not authority. Helpers use their captured reservation and config. Acceptance and historical resolution bind first/result/acceptance clocks to the actual active or unique recorded reservation.

First-result construction records actual construction elapsed. Real row/runtime/native/numerical guards run before publication; clock checks run after qualification, after immutable write/hash/read-back/requalification, on identical reuse and before each subsequent input. Tests wrap real operations to cross W during row/runtime/envelope qualification, write, hash, read-back, requalification, reuse and delayed pre-input progression. Adapter calls remain one and frame 62 is blocked in deadline cases. Before-W publication and identical reuse reach the second input only after real persisted verification. Structural produced-row evidence and earlier first/raw hashes survive later failures.

Failure reconciliation verifies newly published supervisor-relative not_reached evidence and preserves verified existing failure time/error/phase/raw bytes. Unavailable, invalid or late clock observations remain explicitly unqualified and JSON-safe; they cannot become verified-first summaries. Worker bootstrap and later failures preserve the original primary exception through best-effort persistence. Existing receipts and capture contracts are unchanged.

Validation retains all failed attempts. Diagnostic001 failed discovery after a malformed declaration insertion. Diagnostic002 reached real guards but seven loader assertions incorrectly ignored qualification rereads of frame 50. Diagnostic003 passed those cases and failed one duplicate-argv fixture output lookup. Aggregate001 completed all suites with one supervisor fixture failure: an old incomplete synthetic reserve failed before its intended cleanup path. The final supervisor fixture now originates from real disposable `Ledger.reserve`, verifies canonical clock transport and all four one-thread settings, then uses a CPU stand-in to retain its cleanup assertions. Aggregate002 passed after these fixes. No source edits followed that pass.

Execution-history exceptions are explicit. Diagnostic001's AST/prelaunch preparation failed, but its capture was nevertheless launched; the prospective launch note is missing. A retrospective note records this without pretending it existed before launch. Its actual capture timestamps, together with the first-action implementation timestamp, still prove the invocation fit the allocation. Diagnostic002 inherited the previous supervisor OMP/OpenBLAS value 4 for a pass-only CPU stand-in; no native/model work was requested there. The final source and tested transport enforce all four thread settings 1. These exceptions prevent an unqualified strict whole-plan acceptance claim despite passing technical validation.

`S1_CLOCK_BARRIER` raw logs contain all loader calls (including real qualification rereads), adapter counts, qualification observations and a field named `completion_observation`. That last field is the final controlled observation after the tested invocation and cleanup. In the pre_input case it is W after first publication already completed earlier. It must not be relabeled as first-publication completion. The real wrapper sequence and qualification observations distinguish the earlier publication from the later input gate. The envelope elapsed is construction time.

Correction009 preserves correction008's full prefix, original baseline, all 38 frozen records and the exact 447-event production ledger. It appends a durable canonical replacement referencing preserved post010, then actual REVIEW->PLAN and PLAN->frozen IMPLEMENT transitions. No status bytes were edited by this agent. Independent ledger/bookkeeping audits and wrapper reconciliation preserve the original 30 GPU attempts, 4374.044265462899 charged seconds, zero reserved, original events 250/255/256 and no recovery event.

Unstaged `git diff --check` exited 0 for tracked changes. It excludes new untracked files and is not a full staged whitespace check. Main owns inspection, explicit task-only staging and local commit. Historical staged-review010's four raw failed/partial log whitespace findings remain preserved.

S1-1 and S1-4 remain met. S1-2/S1-3 remain not met. Remaining work includes helper startup/transport/sample freshness, durable progress, structured errors/cleanup/final charge/acknowledgment and other timing consistency, full native/runtime/lifecycle/race/replay matrices, actual successful 510-row worker progression, current production authorization/registration and the eventual separately authorized single calibration recovery. Historical P31-5 and unavailable older timing/status evidence remain unclosed. No GPU/device/model/production/setup/download operation or git write occurred in this stage.

Evidence: validation010, independent audit010 and its script, wrapper-reconciliation011, process-observation011, ledger/bookkeeping-audit011, timing011, assessment025-implement, correction009, all five launch notes and all five immutable attempt directories. Main should commit the validated technical milestone and continue fresh REVIEW under the resumed loop, retaining the procedural exceptions rather than repeating consumed Plan041 invocations.
