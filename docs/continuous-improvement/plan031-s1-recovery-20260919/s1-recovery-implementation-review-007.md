# Plan 039 implementation review — R9-1 receipt milestone

The receipt milestone is complete. The final exact nine-suite aggregate passed **157 methods and 213 callbacks**, with zero failures, errors, skips or discovery errors. Capture observed actual returncode **0** in **92.21503551599744 seconds**. The shared production validator accepted the completed v2 graph. Overall implementation acceptance, live admission and the objective remain incomplete.

## Changes

All nine suites now carry one AST-readable `SUBTEST_CASES` literal. All 18 original parameterized methods retain their ordered inputs and assertions; primitive receipt fields reconstruct former dictionaries, frame lists and Identity values. The shared contract rejects missing, duplicate, nonliteral, dynamic, extraneous and invalid declarations; it compares typed primitive values and canonical callback IDs exactly. Bool/int/float/null distinctions are retained. Tests without parameterization require empty callbacks.

The runner and production admission share inner v2 checks for collection, every callback, exact integer counters, current source membership/bytes, execution arguments, interpreter, environment, timestamps and closed raw files. A separate capture driver launches the exact four-line stdin/runpy command, waits for actual completion and binds the immutable inner receipt into an outer execution document. The normalized wrapper has one bound receipt/execution entry and separate aggregate, milestone, implementation, live-admission and objective flags. No unsigned document is presented as cryptographic attestation.

The disposable fixture now creates a complete, explicitly synthetic graph through the real guards. Nine new collected test methods add 122 callbacks, covering declaration parsing, primitive identity, callback mutations, accounting, full missing/stale/alias artifact matrix, metadata/time/argv/environment/wait/log failures and legacy wrappers. Each negative begins by validating a fresh positive graph, then reaches a focused rejection; representative mutations also invoke the shared inner guard directly. No fixture bypass exists.

## Validation and independent evidence

- `s1-recovery-validation-008.json`: current v2 wrapper, 74 source records, current amendment/configuration/plan/baseline/correction bindings and clean `git diff --check`.
- `s1-recovery-aggregate-009-002/`: final inner receipt, outer execution, exact stdin, runner/capture snapshots and closed inner/process logs.
- `s1-recovery-audit-008.json` and `s1-recovery-independent-audit-008.py`: separate static collection and typed callback reconciliation, per-suite counters, source/file bytes, timing, argv/environment, actual tool completion and preservation checks. This script never imports the production contract or test modules.
- `s1-recovery-process-observation-009.json`: all four actual execution-tool completions, with sessions and child/driver codes; first aggregate passed but its source binding was superseded by the documented final coverage correction.
- `s1-recovery-wrapper-check-009.json`: read-only acceptance by production `validation_record` after independent reconciliation.

Two focused invocations and two aggregates were used within their respective 120/300-second caps. The second aggregate was justified before launch by a source correction for augmented literal mutation and the full artifact record matrix. The third focused allocation remains unused; no further aggregate is authorized under this plan. Native pools were one and suites serial. No model evaluation, GPU/device operation, production controller or ledger API/mutation, setup/download/smoke, historical scientific rerun or git write occurred.

## Preservation and remaining work

Correction-007 preserves correction-006 and its prefix, adding four actual saved status transitions. IMPLEMENT status remained frozen at SHA-256 `32bacd286038992f433caca8a9915f5c8aaafdef26261946415d83ea08d7258d`, 897 bytes; `s1-recovery-status-implement-009.md` preserves these bytes for main's next status transition. Main owns later status/correction successors and the task-only local milestone commit.

All 38 frozen records and amendment/S0/native source parents remain exact. The ledger remains 447 events and 332,437 bytes, SHA-256 `2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`, head `00cab019d73b6a3205f676b54cfb2f745f492fa5cca5c69b4567087cea62172b`. It retains 30 GPU attempts, 4,374.044265462899 seconds, zero reserved, cleaned-up failed finishes 250/255 and R-S account/skipped event 256. No active job or recovery event exists.

R9-2 through R9-6 and the full applicable-boundary/concurrency/replay/lifecycle matrices remain open. No live recovery authority or production authorization/registration is claimed. Historical P31-5 and unavailable older status/timing evidence remain limitations; current receipts do not retrospectively certify them.

The initial clock command used unavailable `python`, then immediately captured UTC/monotonic with `python3` before inspection. Two read-only convenience inspections initially assumed a `skip` event spelling and amendment/configuration keys in legacy validation-007; inspection established `account`/`skipped` and the incomplete old wrapper. Neither failure was a permission failure, test invocation or production operation, and neither wrote an artifact. All execution and independent final reconciliation passed.
