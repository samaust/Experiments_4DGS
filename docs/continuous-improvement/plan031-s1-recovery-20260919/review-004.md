**Iteration 4 REVIEW complete; implementation incomplete. Live S1 calibration admission is not ready.**

The recorded nine-suite aggregate passed **135 methods and 79 subtests, zero failures/errors/skips, exit 0**. I reconciled its collection against current source AST, per-suite totals and embedded stdout receipt, and rehashed all 70 source/config/test records. I did not rerun the aggregate. Validation-003 correctly remains `status=incomplete`; `test_status=passed` does not establish that the missing required behaviors ran.

Read: [objective](objective.md), latest [status](status.md), [review-003](review-003.md), [assessment-005](assessment-005.json), [validation-003](s1-recovery-validation-003.json), [implementation-review-002](s1-recovery-implementation-review-002.md), [Plan 034](../../../plans/plan_034.md), AGENTS.md, current tracked diff, untracked S1 implementation/tests, correction/bookkeeping records, amendment, and the production ledger. This request overrides standing commit/delegation instructions. No prompts content was read.

**What has closed since review 003.** F3-0's shared-fixture error is resolved in the recorded final 13-test recovery suite and aggregate. The positive fixture uses real AssetBundle, numerical/runtime/first-result qualification and 510-row resolution. Bookkeeping transitions, loaded GroundingDINO/SAM/E1 native bindings, native surviving-index mapping, failed-first-result cleanup reconciliation and primary `failure.json` exception handling improved. F3-7 now requires one ordered aggregate and reconciles method/per-suite/subtest outcomes. These are real improvements; F3-1–F3-4 and F3-6–F3-7 remain partial, and F3-5 remains open. The malformed-label/serialization part of F2-0 remains closed; its numerical-corruption obligation remains open.

**F4-1 — P1: real admission cannot reach registration.** `common_admission` copies its document fields at `execution.py:287` without `baseline_correction`; `admitted_event` requires that field at `s1_recovery.py:315`. Consequently an otherwise admitted real record fails registration with `ValueError: admission evidence changed: baseline_correction`. The existing fixture's `admit` copies the entire authorization (`tests/test_vipe_benchmark_s1_recovery.py:205`) and masks this mismatch. Its `source_paths` is also patched at line 116, and its positive finish is manually appended at line 306. Add the missing binding and the real controller lifecycle; adding the field alone is insufficient for live safety.

**F4-2 — P1: preservation is not yet lifecycle validation.** `preservation` verifies the immutable prefix/chain/scientific files/current status, but its append filter (`s1_recovery.py:289`) does not enforce lifecycle ordering and references. A disposable probe appended a hash-valid `started` event before any admission, registration or reservation; `validate_binding` accepted it. This does not show that the public controller emitted such an event; it shows the required rejection guard is absent. Locking alone does not prove concurrent registration/reservation, block continuation, prelaunch mutation or replay behavior.

**F4-3 — P1: durable failure/partial publication remains incomplete.** Pre-reservation `terminal_receipt` now publishes before parsing authorization, but the CLI still reads/hashes it before entering `execute_s1_recovery` (`basketball_vipe_benchmark.py:164`). After consumption, authorization/result/partial parsing at `s1_recovery.py:400–404` can abort minimal terminal publication. A disposable malformed qualified-row file reproduced `JSONDecodeError: Expecting property name enclosed in double quotes: line 1 column 2 (char 1)` with no terminal receipt. The default expression at line 404 reads partial files even when result rows exist. Counts at line 434 trust rows/filenames without qualification, and absent partial files are insufficient evidence of zero produced outputs after abrupt death. `initial-runtime.json` is now saved before backend construction, but terminal fallback at line 432 considers only result/partial runtime. Normal failed/not-reached reconciliation and local/ledger stop flags are improved, but dedicated regressions are missing. The worker's failure write is guarded; its preceding directory creation remains outside that guard. Keep primary exceptions and record secondary publication failures at every boundary.

**F4-4 — P2, required before admission: numerical/runtime/envelope adversarial coverage remains incomplete.** The 510-row fixture proves real guards can pass, not the complete worker path. No test drives a successful S1 recovery pass through durable first-result publication before frame 62, full controller acceptance and publication. The numerical-failure subcase still expects missing `diagnostics` (`tests/...s1_recovery.py:381`). The `zero`/`skip` fixture options are not exercised by collected tests. Lower-level semantic tie/ambiguity and native refinement tests are retained; they do not replace the required serialized-row/runtime/envelope mutations listed below. New loaded source/native and surviving-id checks need intended-guard regression evidence, not another general success count.

**F4-5 — P1: terminal acceptance/publication still escapes the charged allocation.** `supervisor.py:265` finishes and charges the job; controller `finally` then calls `terminal_receipt` (`execution.py:648`), which re-enters `resolved_result` (`s1_recovery.py:420`) and numerically validates all 510 rows outside the reservation timer. Receipt hashing/writes also occur there. Publication failure can therefore follow an already complete ledger finish. The repeating 50 ms SIGALRM handler (`supervisor.py:73–101`) invokes the resource sampler itself, permitting reentry when sampling takes longer, does not hard-bound blocking native work, and ignores ownership during prelaunch/acceptance/publication. An after-the-fact overrun flag is insufficient. Keep the single reservation open through acceptance, cleanup and durable terminal publication, then commit the final outcome/elapsed charge and bind its receipt. Split pre-finish acceptance from historical resolution so the controller does not repeat an unbounded row pass. Use externally monitored, terminable validation/publication work with serialized bounded sampling and ownership checks; retain all 510-row and current-binding checks.

**F4-6 — P2, required before admission: receipt subtest membership is still not enforced.** The new aggregate/outcome accounting is useful, but `required_cases` only detects that a method uses `subTest`, and `validation_record` merely requires nonempty unique passing subtests (`s1_recovery.py:69–75`). A disposable probe substituted an unrelated subtest and adjusted coherent counters; validation accepted it. Define and enforce exact required parameter identities, then collect the receipt-adversarial cases in the final aggregate. The existing negative test covers source/schema/basic receipt rejection, not the entire prescribed matrix.

**F4-7 — stage gate remains closed.** Validation-003 is deliberately incomplete and cannot authorize recovery under the existing guard. A fresh passing aggregate on final sources is necessary after these fixes; changing a status field or reusing synthetic fixture receipts is not evidence. CPU completion ends at preparation/review. Production DO-stage authority and fresh resource/ownership checks belong to a later authorized execution stage; the current REVIEW authorizes neither.

The exact remaining acceptance criteria are below. Each row is necessary; existing lower-level cases are credited above and need not be rebuilt.

| ID | Remaining criterion | Smallest completion |
| --- | --- | --- |
| L1 | Real admission through controller, supervisor, terminal and resolution, with real source membership/assets/input/annotation/qualification guards and all 510 identities. | Copy `baseline_correction`; promote the existing synthetic evidence helpers into a disposable full lifecycle; fake external execution/resources only. |
| L2 | Bound ordered lifecycle; immutable prefix/chain/scientific bytes and current bookkeeping; boundary mutations; locked races; block/continuation and consumed replay. | Add one shared lifecycle validator at existing boundaries/locks and parameterized mutation/race cases. Reject orphan/duplicate/out-of-order events; retain one consumed identity. |
| A1 | Valid zero/tie/ambiguity/empty/skipped/overwritten row cases; corrupt RGB/preprocessing, logits/probabilities, retained order, pixel boxes, phrase bytes, detection/semantic indices and ids/classes. | Extend the valid numeric fixture; recompute outer hashes so each mutation reaches its intended guard. Replace the missing-diagnostics subcase with actual numerical corruption. |
| A2 | Changed source/native/import/build/inventory relationships, nested mutation, missing required files, wrong interpreter, duplicate/aliased paths/modules, empty manifest and forbidden roots/modules. | Table-driven real `qualify_runtime` cases using admitted synthetic files; no import probes/builds/forwards. |
| A3 | Complete first envelope mutations and timing/resources; durable pass before next input; strict pass reuse; failure stops after one call; later failure preserves pass. | Exercise the real first-result worker path and full 510-row result, preserving 340 fitting/170 selection rows and raw evidence. |
| P1 | Block evidence for invalid authorization; missing/corrupt result/partial/runtime; genuinely qualified partial counts; first/later/abrupt failures; cleanup uncertainty; publication errors at every boundary. | Separate minimal outcome writing from guarded evidence parsing; reference initial/partial/final runtime; qualify readable rows in the reservation, report unverifiable counts as unknown, preserve primary errors. |
| P2 | Full charged acceptance, terminal validation/hash/write and cleanup; hard deadline, non-reentrant sampling and continuous ownership/caps. | Keep publication inside one supervised allocation with bounded monitor/worker design, final charge after publication, no post-finish full revalidation; test expiration, blocking work, foreign PIDs, cap changes and cleanup reserve. |
| A4 | Required subtest identities and actual aggregate execution; zero/missing/extra/duplicate/aliased/stale/failed/skipped/contradictory/disjoint receipts and one-test command-name trick rejected. | Explicit expected case parameters and exact receipt checks, with adversarial cases included in the nine-suite aggregate. |
| V1 | Fresh current-source validation and unchanged scientific/ledger evidence; truthful timing; later live authority and outcome. | Run the prescribed aggregate after fixes, issue fresh immutable validation/review/preparation, stop before live admission. |

L2 specifically includes amendment and parent records, config/source set/validation/correction/authorization, E1 qualification/assets/runtime, inputs/annotation policy/review, original failure artifact/event/cleanup, historical/canonical request and event bindings at applicable admission, registration, reservation, prelaunch and resolution boundaries. Test invalid ledger prefix/head/chain/snapshot, unrelated appends, scientific mutations and missing/broken status transitions. Cover unrelated active attempts, reduced/exhausted cumulative time, typed scope, second identity, simultaneous registration/reservation, identical registered-but-unreserved continuation, and no launch/retry after consumption.

P1 specifically includes existing verified `failed`/`not_reached` records versus actual publication conflicts; missing finish after worker/controller death; raw, first-result, `failure.json` and terminal directory/write/hash failures. Missing evidence must not imply successful cleanup, completion or zero production. P2 specifically includes expiry immediately before `Popen`, slow/blocking hashing/decompression/sampling/writes, foreign GPU PIDs after worker exit and during acceptance, memory/disk/download cap changes, and reduced-deadline cleanup. Test timely interruption and accurate charging, not just an overrun field.

The smallest coherent implementation is: fix L1 and prove the full fixture; enforce L2 and robust P1; move terminal acceptance/publication inside P2's monitored reservation; add A1–A4's exact cases; finish V1. Limit edits to the current recovery/evidence/tests and narrow controller/CLI/supervisor/worker corrections required by those cases. Preserve semantic policy, thresholds, weights, preprocessing, SAM and tracking. This is a recommendation for the next authorized CPU implementation stage, not source work performed here.

| Objective | Assessment |
| --- | --- |
| S1-1 | Supported within the source-bound semantic CPU scope; native behavior and S0 assignment evidence retained. Real synthetic qualification adds support. Adversarial/worker acceptance remains incomplete; no live output claim. |
| S1-2 | **Not met.** Dedicated bindings and limits exist, but real admission/lifecycle/publication are incomplete and there is no production authorization/registration. |
| S1-3 | **Not met.** No bounded live recovery or cleaned-up recovery outcome exists. |
| S1-4 | **Met within audited scientific preservation scope.** All 38 frozen records and the production ledger are unchanged. Future lifecycle preservation tests remain required; historical P31-5 limitations remain. |

Production ledger: **447 events, 332,437 bytes**, valid chain, SHA-256 `2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`, head `00cab019d73b6a3205f676b54cfb2f745f492fa5cca5c69b4567087cea62172b`. No active reservation or S1 recovery event exists. Original cleaned-up failures at sequences 250/255 and R-S skipped at 256 remain. GPU accounting remains **30 historical attempts, 4,374.044265462899 seconds, zero reserved**. All 38 frozen scientific records and the 70 validation source records rehash correctly. The three bookkeeping transition records link to current status; older status bytes remain unavailable for independent reconstruction. Git diff check passed and the index is unchanged.

Any later implementation continuation retains Plan 034's **1,800 wall seconds/eight CPU workers**, with timing beginning at inspection and truthful partial completion on exhaustion. Prior implementation timing remains the recorded conservative 848.010818-second bound, not an exact full-pass measurement; prior 642.618033546998/518.3559243239979-second figures retain their excluded-inspection caveats. No benchmark budget is reset. Future execution remains only `S1-calibration-recovery-001`, one attempt, `min(3600, 93600 - gpu_elapsed - gpu_reserved)` positive seconds; cleanup reserve `min(30, effective_seconds/4)` is inside that allocation. Preserve one exclusive GPU process group, 22 GiB device memory, eight CPU workers, 150 GiB artifacts, 60 GiB cumulative new downloads, 57,600-second preparation/setup ceilings and zero new setup/downloads. Reconstruction, R-S and aggregate/report reruns remain outside this recovery.

Four disposable CPU guard probes completed in **1.1428593200034811 seconds** before finalization: missing admission correction rejected registration; orphan `started` was accepted; unrelated required-subtest replacement was accepted; malformed partial aborted terminal publication. They reused the lower-level fixture's synthetic evidence/source-membership mock and are not a controller integration test or production acceptance. No aggregate rerun or further diagnostic launch occurred after the user's finalization instruction. Exact outcomes, evidence hashes and the remaining-criteria matrix are saved in [assessment-006](assessment-006.json).

Only this review and the assessment are added to the repository. No source edits, GPU/model/device work, production controller calls or ledger mutations, staging, commits, delegation, or scientific aggregate/report reruns occurred. The review does not grant live admission.
