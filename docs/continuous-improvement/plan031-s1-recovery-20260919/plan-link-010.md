# Iteration 10 PLAN — S1 numerical envelopes and direct script compatibility

Created [Plan 040](../../../plans/plan_040.md) from [review-010](review-010.md),
[assessment-020-review](assessment-020-review.json), current source and the saved
[objective](objective.md). Current assessment: [assessment-021-plan](assessment-021-plan.json).

Scope is R10-1 local row/result numerical envelopes and R10-2 canonical case-table
lookup in six suites. Includes typed finite/nonnegative timing, singleton groups,
integer allocator limits, an explicit floating-sum allowance derived from stage
arithmetic, produced versus qualified evidence, terminal/resolver integration,
truthful fixtures and target-reaching mutations. Six real direct-script method
selections run serially inside prescribed CPU invocations with fixed child caps.

New IMPLEMENT allocation: **1,800 wall seconds before inspection**, **180 seconds
reserved for evidence**, execution cutoff **1,620 seconds**, at most **three
120-second focused invocations** and **two 300-second aggregates**; the second
aggregate requires an invalidating change or recorded concrete concern. Eight
CPU workers maximum including native/helper threads, pools one, serial suites.
Script regression children: six per regression, 10 execution + 2 cleanup seconds
each, at most 72 seconds inside the parent timeout; no recursion or standalone
extra probes. Zero GPU/model/production/setup/download/smoke allocation. Existing
cumulative and method ceilings and historical consumption remain unchanged.
The active skill's standing approval covers this review-recommended, finalized
CPU plan; main checks remaining ceilings and records dispatch authority.

Main preserves PLAN status before freezing IMPLEMENT. Correction-008 must retain
correction-007 and its transition prefix, add a canonical Plan 034 transition for
the preserved noncanonical post-009 note, then append actual REVIEW/PLAN/IMPLEMENT
snapshot transitions. Validation-009 binds the new correction; prior artifacts
remain immutable. Main owns status and local milestone commits.

Read-only PLAN reconciliation matches **74 source records, 38 frozen records and
the exact 447-event / 332,437-byte ledger**. No tests, source changes, production
APIs, GPU/model work or git writes occurred in PLAN. S1-1/S1-4 retain met support;
S1-2/S1-3 remain not met. Completing this milestone leaves full clocks, helpers,
progress, finalization, broader native/runtime cases, real 510-row progression,
admission/replay/lifecycle matrices and live recovery work open.
