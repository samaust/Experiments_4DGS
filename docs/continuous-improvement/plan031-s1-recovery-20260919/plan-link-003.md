# PLAN 003 — Shared S1 recovery fixture and remaining CPU validation

Created [Plan 034](../../../plans/plan_034.md) for iteration 3, following the
[objective](objective.md), [review-003](review-003.md),
[assessment-004](assessment-004.json), [Plan 033](../../../plans/plan_033.md) and
the preserved [failed validation-002](s1-recovery-validation-002.json).

The plan starts with the shared `baseline_correction` fixture/evidence mismatch:
nonempty preservation categories and ledger snapshot, matching correction across
authorization/validation/reservation, coherent method/subtest receipts, historical
charges placed before the snapshot, and complete first-result/finish evidence.
It then maps F3-0–F3-7 to the remaining real-guard lifecycle, numerical/runtime,
failure/publication, bounded acceptance and source-bound CPU validation work.
Existing baseline/correction and failed receipts stay immutable; a successor
correction binds the new plan and verified bookkeeping transitions.

The subsequent CPU implementation pass remains capped at 1,800 wall seconds and
eight workers, with disposable ledgers only and no production ledger mutation.
The prescribed nine-suite CPU test aggregate is required; scientific aggregate
and report reruns are excluded. Fresh validation, implementation review and a
non-executable handoff end the pass. CPU success does not authorize live admission.
S1-2's production admission and S1-3's actual outcome remain pending.

Preserve exactly one future `S1-calibration-recovery-001` attempt of at most 3,600
GPU seconds within the existing cumulative/resource ceilings, with acceptance,
publication and cleanup charged inside the allocation. No reconstruction; it
requires separate later authorization after calibration review.

This PLAN stage creates only Plan 034 and this link. No implementation edits,
tests, GPU/model/device work, ledger writes, staging, commits or delegation.
`prompts` was not read. The read-only audit verified 70 source/config/test and
38 scientific records, 447 chained ledger events, no active reservation or S1
recovery event, and the unchanged ledger hash
`2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`.
Source, index, previous plans and existing protected evidence remain unchanged.
