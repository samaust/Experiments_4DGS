**Iteration 6 REVIEW complete. Implementation acceptance remains incomplete; live S1 calibration recovery is NOT READY. The current source needs a narrow helper-integration correction before final acceptance validation.**

Read the objective, latest loop status, assessment-009, s1-recovery-validation-005, s1-recovery-implementation-review-004, s1-recovery-preparation-005, review-005, Plan 036, AGENTS.md, current tracked diff and relevant untracked S1 source/tests, and the production ledger. This review creates only review-006.md and assessment-010.json in the repository. The user's immediate-finalization request ends investigation; no source changes or further runtime checks follow it. No prompts were read.

**Exactly why final validation was not run.** Assessment-009 and validation-005 explicitly record that iteration 5 IMPLEMENT stopped on the user's immediate-finalization request. Its one focused diagnostic passed 13 methods, zero failures/errors/skips, exit 0, in 41.147 seconds, **before subsequent source changes**, including the spawned CPU helper and stricter sampling. The exact intermediate source snapshot and individual subtest outcomes were not captured. The final ordered nine-suite aggregate was never attempted: validation-005 has `tests=[]`, assessment-009 has `aggregate_run=false`, and both retain incomplete acceptance. No recorded aggregate failure or permission denial explains that omission. The fixed source-bound runner, literal parameter tuples and strict execution-metadata contract were also not implemented, and all five safety packages remain open. Running the old tests alone would not close these obligations.

I independently matched the retained focused log to its embedded output, all 13 recorded method identities and its 41.147-second/OK footer. Exit 0 is supported by the prior recorded tool completion, not a rerun. All 71 current source/config/test records match validation-005 and assessment-009 exactly; membership includes s1_cpu_helper.py. Thus validation-005 binds the final bytes but provides no runtime pass for them. Static parsing of 70 Python files and `git diff --check` pass. Static collection still finds 135 methods, including 16 methods containing subtests; these are collection counts, not executed acceptance results.

**Can the current source be validated directly?** It is suitable for isolated CPU diagnostics with fake external boundaries. It is not ready for a direct final-validation/readiness claim, and must not be admitted live. Two concrete integration defects now precede the broader uncompleted Plan 036 gates. Five small diagnostic cases in three CPU-only harness invocations confirmed the following; exact inputs, outputs and timing are embedded in assessment-010. They are not a suite, an aggregate or production evidence.

| Finding | Evidence and implication | Smallest correction |
| --- | --- | --- |
| **F6-1 — Spawn is incompatible with the prescribed stdin invocation as currently wired.** | `supervisor.py:81–99` uses multiprocessing `spawn`. Under `.local/envs/stg-colmap/bin/python -B -`, a real constant-operation helper fails during bootstrap with `FileNotFoundError` for the repository's `<stdin>` path; the parent's `poll()` raises `EOFError`. The identical guarded probe in an importable temporary file returns the constant result. No model or device operation was involved. This is a Python bootstrap integration failure, not a Codex sandbox denial. | Make the CPU helper/runner entry arrangement work under the **unchanged prescribed stdin launcher**, with an importable guarded bootstrap. Use the planned fixed source-bound runner or explicit helper executable entry; preserve spawned isolation. Add a collected regression under the actual launcher and a successful constant-operation control. Do not merely change the acceptance command or restore fork. |
| **F6-2 — Worker sampling treats its own GPU PID as foreign.** | `supervisor.py:255–256` calls `monitored_call(..., phase='worker_sample')` without `worker=process`. At `:163–166`, ownership is therefore empty. A deterministic fake-helper probe with PID 12345 raises `exclusive GPU access lost during worker_sample: [12345]`; supplying the matching worker makes the same reading pass. The outer ownership check is never reached in the failing case. Existing positive fixtures supply an empty GPU PID list and cannot expose this defect. | Pass the captured worker ownership context into worker sampling and preserve shared resource peaks. Collect owned-PID success and foreign-PID rejection cases, including the unreaped exited-worker case. Keep prelaunch ownership empty as intended. |
| **F6-3 — Helper cleanup uncertainty is discarded.** | `_Helper.close()` returns false when its zero-time join cannot establish termination (`supervisor.py:102–117`), but every monitor caller discards that return and can drop the last reference. A fake helper whose `close()` always returns false still yields `work accepted`. The new worker `cleanup_uncertain` flag does not cover helper cleanup. | Retain ownership of unsettled helpers, reap them within the phase deadline, and propagate unresolved helper/descendant cleanup into a consumed stop. No success may rely solely on the worker's survivor list. This is part of existing Plan 036 package 4, not a new workstream. |

The probes' harnesses exited 0 because they deliberately captured diagnostic outcomes; that does **not** make the failing helper behavior a passing validation. Combined subprocess duration was approximately 0.257 seconds. No GPU query, native model import, production controller or ledger transition ran.

**Retain the implemented progress, with its limits.** The shared canonical request/argv comparison is now called from consumed lifecycle validation and reservation, and active binding compares the captured reservation at prelaunch/acceptance. Generic CLI decoding checks object/string types inside its protected path. Runtime mapping guards, optional-evidence isolation, structural unique-row reconciliation, prepared terminal evidence, strict required resource fields, 100-ms S1 dispatch polling and explicit worker-cleanup uncertainty are present. Direct controller `os.fork()` has been replaced. None of these post-diagnostic changes has final-source runtime acceptance evidence.

Plan 036's remaining obligations still apply:

- **Packages 1–3 / L1-L2/P1:** Complete applicable-boundary mutations, real synchronized ledger contenders, continuation/replay cases, corruption/storage/death/CLI tables and exact partial counts. Trusted first-result timing is still derived from a worker-only environment variable; checks still use 3,600 seconds instead of the effective work allocation. Reconciliation returns its summary at completion rather than durably preserving incremental verified evidence across helper timeout. Structured primary/secondary failures remain incomplete.
- **Package 4 / P2:** Helpers are repeatedly spawned rather than a fixed ready set before reservation. Startup and pipe receive can block the monitor; readiness, transport, descendants and reaping lack the required complete bounds. Samples have no freshness timestamps. Cleanup and final ledger lock/append/fsync remain outside continuous sampling and deadline control; elapsed is measured before finish I/O. The prescribed reserve subdivision, conservative finalization charge and durable acknowledgment are absent. Typed helper failures are wrapped without complete supervisor classification. Fixing F6-1/F6-2 alone cannot establish bounded live safety.
- **Package 5 / A1–A4 and V1:** Exact literal tuple declarations, the fixed runner and strict receipt metadata validation are absent. `required_cases` still detects only methods and the presence of `subTest`; arbitrary passing subtest strings are not exact coverage. Remaining numerical/runtime/envelope cases and successful real `stages.segment` progression through 510 ordered outputs are uncollected. The synthetic external-worker controller fixture does not establish that progression. The nine-suite aggregate must follow all final source/test/runner changes, with independently reconciled invocation, logs, outcomes and hashes.

**Smallest next bounded action.** Continue with one **CPU-only helper-integration correction checkpoint**, not a live attempt or an immediate full acceptance run: fix F6-1 and F6-2 together, retain/propagate helper cleanup uncertainty from F6-3, and collect focused regressions using the prescribed stdin launcher, constant/fake readings, owned versus foreign PIDs and unsettled cleanup. Extend the existing code and fixtures; preserve the scientific amendment and native behavior. Bound the continuation to **1,800 wall seconds measured before inspection, eight CPU workers including helper/native threads, with the final 180 seconds reserved for evidence**. Stop with truthful incomplete records on exhaustion. Passing this checkpoint permits continued work on the remaining Plan 036 packages; it does not close them or authorize production.

After all packages are actually complete and source is stable, run one final aggregate with `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 .local/envs/stg-colmap/bin/python -B -`, in order: s1_semantics, s1_recovery, backends, contracts, component_recovery, execution, budgets, supervisor, review_annotations. Record exact stdin, launcher/Python argv, logs, UTC/monotonic times, every method and literal subtest tuple, and reconciled suite/aggregate counters. A subsequent source/test/runner edit invalidates it. Preserve occupied artifacts and use the next unused suffixes; do not rewrite validation-005 or claim old passes cover new bytes.

| Criterion | Iteration 6 assessment |
| --- | --- |
| **S1-1** | **Partial; retain prior semantic CPU support.** The source-bound S0 token-sum amendment and preserved native semantics remain supported within earlier scope. Final-source adversarial/runtime/worker acceptance is incomplete; no live output claim. |
| **S1-2** | **Not met.** Admission code has progressed, but CPU acceptance has concrete blockers and no production authorization/registration exists. |
| **S1-3** | **Not met.** No live recovery or cleaned-up recovery outcome exists. The original cleaned-up failures are historical failures, not the requested recovery outcome. |
| **S1-4** | **Preserved within audited scope.** Frozen scientific records, ledger, previous loop records, source and index are unchanged by this review; historical limitations remain. |

**Preservation audit.** All 38 frozen scientific records, 40 prior-loop records recorded in assessment-009, five bookkeeping-transition records and the requested bound references match their recorded bytes. The current loop status remains unchanged. The production ledger is still 447 valid chained events, 332,437 bytes, SHA-256 `2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990`, head `00cab019d73b6a3205f676b54cfb2f745f492fa5cca5c69b4567087cea62172b`. There are no active reservations or S1 recovery events. Original failed finishes 250/255 and R-S skip 256 remain. GPU totals are unchanged at 30 historical attempts, 4,374.044265462899 seconds and zero reserved; other resource totals also match. This is ledger accounting, not a device-availability check. The tracked diff and index match validation-005/assessment-009. Historical P31-5 limitations, unavailable older status bytes and missing inspection-inclusive implementation timing remain unresolved; no history is reset.

Only a separately authorized later DO stage, after all CPU gates close, may consider the sole identity S1-calibration-recovery-001, one attempt and positive effective allocation `min(3600, 93600 - gpu_elapsed_seconds - gpu_reserved_seconds)`, with cleanup/finalization inside the cap. Retain one exclusive GPU group, 22 GiB device memory, eight CPU workers, 150 GiB artifacts, 60 GiB downloads and existing preparation/setup ceilings. Reservation consumes the identity without launch. No retry, new setup/download/smoke job, reconstruction, R-S or scientific aggregation/report rerun is authorized here.

No source edits, GPU/model/device work, production mutation, staging, commit or delegation occurred. No focused unittest suite or aggregate was rerun. This review is complete; implementation and the objective remain incomplete.
