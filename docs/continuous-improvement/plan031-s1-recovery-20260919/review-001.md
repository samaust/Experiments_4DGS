REVIEW 001 — Plan 031 S1 recovery, 2026-09-19

The objective is incomplete. S1's semantic assignment repair meets its source/CPU criterion, but recovery admission is unfinished and no recovery has run. Continue with a narrowly scoped admission-and-evidence implementation, then the single calibration recovery under recorded applicable authorization. This review grants no execution authorization.

Reviewed repository HEAD: `0184cef74548261d4444ed9ee1e0464da1561b5e`, including its existing unstaged changes. The controlling scope is the user's REVIEW request: write these two review artifacts only; no source edits, GPU jobs, staging, commits, or delegation. It supersedes the general commit/delegation instructions for this stage. `prompts` was not read.

| Criterion | Assessment | Evidence and limit |
| --- | --- | --- |
| S1-1 | **met** | Amendment, S0/native source hashes, current semantic implementation, and 68 passing focused CPU tests support the rule and preserved native behavior. This is source/CPU qualification; repaired real-model output and quality remain unverified. |
| S1-2 | **not met** | A partial amendment-aware implementation exists, but its required validation is absent/incompatible, it has no S1-specific admission tests, and the ledger contains no S1 recovery authorization. |
| S1-3 | **not met** | No S1 calibration recovery reservation, request, job directory, first-result record, or terminal recovery receipt exists. The original failures do not satisfy a recovery-outcome criterion. |
| S1-4 | **met** | Frozen parents and failures match their recorded hashes; aggregate checkpoints and report match ledger records; the ledger is unchanged from semantic validation. The diff contains only the pending S1 repair/admission work and its documentation/tests. No allocation was reopened by this review. |

The objective and loop status were read along with `AGENTS.md`, all of `plans/plan_031.md`, the research run status, the semantic amendment/review/validation bundle, the tracked diff and new source/test files, E1 recovery evidence, original failures, and the full ledger. The research status includes historical headings that still say D1 is pending or blocked; its newest completion paragraph and ledger through sequence 446 establish that D1-fit, R-D, and D1-check already completed. None should be rerun for this objective.

The semantic repair is supported by the evidence. Both original S1 failures are `BackendError: unresolved native semantic phrase: ''`, in `detection_rows` before SAM/tracking. The standalone predictor retains queries whose maximum sigmoid token score is strictly greater than 0.35, but constructs text from tokens strictly greater than 0.5. Empty retained text is therefore possible. The failure traces do not preserve the actual failed query scores, so that explanation is source-derived, not a recovered measurement.

The verified S0 helper skips token IDs 101/102, sums probabilities within period-delimited spans (1012), and picks the first maximum. S1 applies this independently to every retained detection, including detections with already valid native text. It preserves native phrases, confidences, selected boxes/order, thresholds, weights, preprocessing, SAM refinement/merge, and DeAOT behavior. It records the derived class separately with token sums, spans, winner, margin, and ambiguity reasons; S2 is unchanged. The current `backends.py`, `contracts.py`, and S1 semantic test hashes still match validation-001. All referenced S0/native source hashes and original failure hashes were independently checked.

The exact admission/recovery gap has four parts:

1. **The old path cannot truthfully admit this semantic change.** Generic `vipe-benchmark-component-recovery/v1` requires `changes_to_prescribed_configuration=false`. The amendment explicitly changes semantic protocol and authorizes zero attempts. The interrupted work has already added `s1_recovery.py` and changes in the controller, `execution.py`, and `ledger.py`; it is inaccurate to say no amendment-aware code exists. Its dedicated schema is `vipe-benchmark-s1-amendment-calibration-recovery/v1`, restricted to `S1-calibration-recovery-001`, with the change flag true and reconstruction false. Keep that narrow separation; do not relax the generic retry guard.
2. **The existing validation cannot satisfy the new binding.** At `scripts/vipe_benchmark/s1_recovery.py:51`, the guard expects `validation.semantic_amendment`; validation-001 instead has `amendment`. It also requires an exact source set of 68 files: controller, worker, configuration, every benchmark module, and every benchmark test. Validation-001 contains 45 records and omits the configuration, new recovery module, and 21 existing test files. Its hashes for `basketball_vipe_benchmark.py`, `execution.py`, and `ledger.py` are stale. The first failure with that document would be `complete current amendment-bound source validation required`; correcting only its field name would still leave coverage and hash failures. Preserve validation-001 and issue new evidence after implementation.
3. **No validated authorization-to-dispatch lifecycle exists yet for S1.** The draft checks amendment/failure/E1/input/request bindings, and ledger registration checks the cleaned-up failure, sequential identity, prior consumption, and active attempts. However, none of the existing test files exercise `s1_recovery`, its schema, or the S1 recovery identity. The component-recovery tests cover S2. Passing those tests does not establish the new S1 admission path. No S1 authorization file is registered in the ledger, and no GPU slot is reserved.
4. **Early semantic failures can lose available raw evidence.** `GroundingDetector.__call__` captures arrays locally, then performs alignment/assignment checks before publishing `self.diagnostics`. `stages.segment` serializes diagnostics only after the adapter returns and contracts pass. An exception at alignment, token layout, or first-output validation can therefore leave only a traceback. Successful output preservation is implemented; durable evidence for these early failures needs a small, S1-specific exception path and CPU tests before claiming S1-3 readiness.

The verified ledger has 447 events (sequences 0–446), a valid hash chain, and no active reservations. Its SHA-256 is `2a0f66e38911b0ed029eb9dd0cf4bbd5da4a3b67abb577fbfe9ad67e6a888990` (332,437 bytes), exactly matching semantic validation-001. Original S1 calibration failed at sequence 250 after 10.414188402937725 seconds, event hash `102e7c7cc33e8a6d2b4107b69fad0ef577b36a901b59c133d99e811f36665d71`; reconstruction failed at sequence 255 after 10.50047472200822 seconds, event hash `2124a4c0c3a8439e697c04c293ef5f2dfe62cea0dd56a7f638e56f91cdad9b54`. Both record confirmed cleanup. R-S remains skipped at sequence 256. There is no S1 recovery authorization, reservation, or finish event.

| Resource | Recorded consumption | Governing ceiling / next-plan restriction |
| --- | --- | --- |
| GPU | 30 attempts; 4,374.044265462899 seconds; zero reserved | 93,600 cumulative seconds; 89,225.9557345371 remain in time only, not additional attempts |
| CPU preparation/scoring/report | 4 attempts; 13,753.206793547044 seconds | 57,600 cumulative seconds; do not reopen preparation, annotation import, aggregate, or report |
| Setup | 14 historical original/recovery attempts; 5,316.549373747828 seconds | 57,600 cumulative seconds; zero new builds for S1 recovery |
| Motion / neighbors | 3 / 3 attempts; 254.51461822795682 / 36.09751573391259 seconds | Already consumed; no reruns |
| Device / concurrency | Not probed in this review | One exclusive RTX 4090 process group; total device memory ≤22 GiB; CPU workers ≤8 |
| Downloads / storage | Latest finish receipt: 26,213,724,097 / 67,755,470,848 bytes | 60 / 150 GiB total; zero new downloads proposed; refresh actual accounting before execution |

The resource readings are historical receipt values, not a fresh host/device availability measurement. E1 setup recovery-002's result hash is `d983f409c56f6d388fc09aab4be59d1257dc0a3a4da02d982d8b9b3b4d03565d`; its asset-manifest hash is `9aa66c929cce4ece69d482022e3aeb02aad389435d379c7dc159b616f79063bf`. Result, manifest, inventory, imports, lock, and build-input record hashes verify. Its import qualification reports zero forwards and no initialized CUDA context; no fresh runtime/model qualification was performed here. Preserve this E1 runtime and its pins rather than rebuilding it. Exact input/annotation records and all checked hashes are in `assessment-001.json`.

Recommended next plan: finish the calibration-only admission and evidence path.

1. **Complete the existing draft, without changing the semantic rule.** In `scripts/vipe_benchmark/s1_recovery.py`, standardize a new validation schema `plan031-s1-recovery-validation/v1` with `semantic_amendment`, passed test receipts, and the complete current `source_paths()` set. Verify original failure bytes/event/cleanup against the live ledger before labeling admission successful, and recheck attempt state under the existing registration/reservation locks. Verify the amendment's S0/native source records and frozen parents. Bind the exact current configuration and existing resource ceilings, E1 qualification/assets/runtime, input manifest, annotation bundle/policy, and the canonical request hash. Make clear that the current `original_request_sha256` field hashes the canonical request derived for the original job, not the serialized historical request with its extra dispatch configuration field; test that distinction and bind historical request provenance separately if used. Reject missing/extra/duplicate source records and changed bytes. Keep exact typed scope fields, the fixed first recovery identity, and all existing failure/consumption records.
2. **Finish controller/ledger integration and prove it on CPU.** Retain the narrow hooks in `scripts/basketball_vipe_benchmark.py`, `scripts/vipe_benchmark/execution.py`, and `scripts/vipe_benchmark/ledger.py`. The same authorization and validation must bind admission, `component_recovery_authorized`, request construction, and dispatch; changing any after registration must block. A completed recovered result can be resolved only after its supervised finish and full 510-row membership validation. Repeated controller invocation must not reserve or launch the consumed identity again. Do not automatically invoke downstream matrix execution, aggregation, or reporting.
3. **Preserve available evidence on first-result failure.** Add an S1-only structured failure payload in `backends.py` and its serialization boundary in `stages.py` (worker handling only if necessary). Persist non-pickled raw logits, token probabilities, native boxes/scores/phrases, selected query indices, token IDs and assignment metadata already available at the failure point, plus failure stage and hashes. Cover contract failures after adapter return as well. Missing evidence before any detector forward must be explicitly labeled unavailable; never invent scores. Preserve normal successful outputs and stop the job on the original exception. This changes evidence handling only, not thresholds, labels, SAM, tracking, or fallback behavior.
4. **Write focused tests and new immutable validation.** Add `tests/test_vipe_benchmark_s1_recovery.py`; extend the semantic/execution tests only for failure-evidence persistence. Test a valid calibration-only binding; wrong amendment/schema/flags/job/branch; stale or missing sources; changed E1 assets/runtime/config/inputs/annotations; wrong failure hash/event or unconfirmed cleanup; active attempt; duplicate registration/reservation; a second recovery identity; exhausted cumulative time; dispatch-request/authorization mutation; failed or partial result rejection; and preserved generic S2 behavior. Use temporary ledgers, fake workers and injected GPU readings only. Test early alignment/layout/contract errors save available raw evidence without a second forward. Rerun the five suites listed below plus the new suite and the existing budgets, supervisor, and annotation-review suites. Run `git diff --check` and preservation-hash checks. Save `s1-recovery-validation-001.json` and `s1-recovery-implementation-review-001.md` in the research run directory, with exact commands, outputs, source/config/test hashes and the unchanged amendment. Recompute the source set after adding tests; the current 68-file count is not a permanent constant.
5. **Only in a later authorized execution stage, register and dispatch once.** Record the user's applicable approval of the semantic amendment and one additional calibration attempt in `s1-calibration-recovery-authorization-001.json`, binding the new validation and exact records above. Do not fabricate approval or infer an attempt from unused time; reuse an already applicable explicit authorization rather than asking for it again. The current REVIEW request does not permit creating or registering that live authorization. Before dispatch, check ledger sequence/identity, cumulative resources, E1/input/annotation hashes, and exclusive host GPU availability. Use the existing controller `component-recovery --authorization <that-file>` path and supervisor, not a direct worker or broad `execute`. Preserve one atomic reservation and a fresh job directory. First-result qualification belongs inside that same job; no smoke run is allowed.
6. **Review its terminal evidence and stop this allocation.** Successful calibration requires all 510 distorted 960×540 inputs (340 fitting, 170 selection), unchanged identity/shape/footprint semantics, assignment evidence for every retained detection, first-result qualification, raw intermediates, runtime/output hashes, measured resources, and confirmed cleanup. Save a new compact `S1-calibration-recovery-001.json` receipt and a recovery-results review referencing the authorization/admission/ledger events and detailed artifacts under `.local`. A failed attempt instead needs its exact error, preserved available raw/partial evidence, explicit first-result status, charged elapsed time, and confirmed cleanup or an unresolved-cleanup stop. Either terminal result consumes the one attempt; there is no automatic second try.

Bound the next implementation/validation pass to 1,800 wall seconds, serial where practical and at most eight CPU workers; stop with a recorded remaining gap if that timebox expires. Synthetic fixtures are not model evaluations. Any actual benchmark preparation/preflight work must be charged to the existing CPU allowance without reopening a consumed preparation/import allocation. This proposed timebox does not reset existing budgets.

The later calibration recovery has **one** additional attempt and **at most 3,600 seconds**, reduced if the cumulative GPU remainder is smaller. Loading, first-result qualification, serialization and cleanup are included; reserve up to 30 seconds inside that deadline for cleanup. Enforce the existing 93,600-second GPU ceiling, 22-GiB total-device cap, exclusive process group, storage/download limits, and stop/cleanup rules. No setup, new assets, alternative model, precision/resolution/threshold change, CPU fallback, or extra probe is included.

Reconstruction is outside this next allocation. It requires a separate later authorization after calibration review, with its own prospective one-attempt/5,400-second bound and an explicit disposition of the skipped R-S immediate-repeat requirement. Geometry, finalists, combined arms, D1, aggregates, final report, training, calibration regeneration and final-window evaluation remain outside scope. Preserve frozen plan/study/protocol/status/report bytes, original failures, and all ledger history. S1-4's scoped pass does not repair the historical P31-5 budget-integrity failures described in the research status.

Validation performed during this REVIEW: the following five CPU suites ran with `.local/envs/stg-colmap/bin/python -B` using `unittest` discovery: `test_vipe_benchmark_s1_semantics.py`, `test_vipe_benchmark_backends.py`, `test_vipe_benchmark_contracts.py`, `test_vipe_benchmark_component_recovery.py`, and `test_vipe_benchmark_execution.py`. Result: **68 tests, 0.208 seconds, exit 0, OK**. The printed `S1-calibration: 1/1 outputs` is a stub fixture, not a real model job. `git diff --check` passed. No live admission API was called because it probes the device and writes admission/ledger records. Hash/ledger checks were read-only, and the accompanying assessment records their results and limitations.
