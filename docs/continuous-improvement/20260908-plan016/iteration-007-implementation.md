# Iteration 007 implementation — Plan 023

Implemented the six v16 source surfaces, centered residual/analytical objective
pair arithmetic, independent transports, original-index subset admission,
211-entry guarded schedule and 22-group frozen readiness suite. **Readiness is
incomplete; no scientific invocation ran.** [Report](../../experiments/basketball-shared-timing-v16.md)
and [retained summary](../../experiments/basketball-shared-timing-v16/retained-summary.json).

A1 passed: full predecessor provenance and separately owned original cases 12/13,
with all 2064 package leaves verified before +600 seconds. A2 did not pass: group
14 active sequence-5 `frames.y.H` has eleven oracle-tolerance failures; dependent
groups 15/21 lack prerequisites. Nineteen groups passed, including group 22's
exact dyadic congruence/input-rejection checks. The exact complete inventory did
not pass. Source-only inspection found no concrete fixed-contract violation to
justify conditional suite 2. Frozen test/source bytes and gates remain unchanged.

A3 retains the prohibited scientific launch explicitly: zero invocations/entries,
all 44 scheduled outputs absent, all 32 changed-source states unverified.
A4 is partial: failed output/allowance records, partial coverage, immutable
historical hashes, consumed limits and clean process shutdown are retained, but
the final supplemental toy protocol/fixture JSON failed serialization on an
unconverted NumPy array after the result was saved. That artifact is missing;
frozen tests could not be repaired. A5 static validation and
local milestone commit are recorded in [validation](iteration-007-validation.md)
and the parent checkpoint. These outcomes are partial/failure retention, not
successful arithmetic adjudication.

One supervised toy consumed 3.560534628 seconds. Suite 2 was unused; science,
training, fits, rendering, device/network and new scientific states consumed zero.
All previous clocks and attempts remain terminal, and training remains
22523.417254 charged seconds. Final phase includes commits and is reported to the
parent after the milestone; the parent records commit identity and final elapsed
time without rewriting historical consumption.

The package's generic known-required-failure flag lacks the toy-failure stratum;
the immutable supplemental summary explicitly records known readiness failure
and actual 0/32 scientific coverage, distinct from the planned 4/32 subset.
The failed output/oracle matrix is retained, but the partially failed toy adapter
and its new-P transform were not serialized. No exact decomposition or numeric
rerun was performed. The next Review should address this precise prerequisite.
SC-01/SC-03 remain unmet; no objective attainment or legacy replay success is
claimed. No owned jobs remain.
