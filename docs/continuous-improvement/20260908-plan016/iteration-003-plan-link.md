# Iteration 003 Plan handoff

Saved [Plan 019](../../../plans/plan_019.md), the next unused plan after Plan 018,
from [recommendations](iteration-003-recommendations.md),
[objective](objective.md), [status](status.md), and
[post-review assessment](iteration-003-assessment-01-review.md).

The finalized scope is six immutable v11 physical states, two independently
decoded exact rational acceleration formulations, six cost/partial-gradient
bundles per method, and 36 paired reference partials. The plan pins slot/source/
physical identities, binary64 quadrature semantics, endpoint and repeated-knot
rules, recurrence/control-polygon mathematics, archived comparison tolerances,
12 toy cases, entry ownership, cleanup and acceptance. No production evaluator
correction or full data/depth evaluation is included.

Approved-under-standing-rule plan-specific ceilings: 1,800 phase seconds,
including 120 numerical seconds; 12 bundles total; one setup per method; one
single-thread worker plus supervisor; at most two fixed 12-case toy-suite
invocations. Admission/readiness/numerical/phase deadlines are T0+600/+1200/+1320/
+1800 seconds. Parent must check applicable ceilings and record the current
AGENTS.md `b62f2ea` standing-approval basis, authorization and T0 before dispatch.
No implementation clock has started. No historical allocation transfers.

A1/A2 require admitted identity/support evidence and exact agreement. A3/A4
require justified retained accuracy findings and validated bounded publication;
historical floating failure does not itself fail an otherwise complete reference
plan. Passing this plan advances SC-05 and prerequisites for SC-01/SC-03, which
remain not met; SC-02/SC-04 retain existing evidence. Main-objective attainment
still requires all saved criteria, with historical scientific gates unchanged.

Validation: all eight local Markdown links resolve; Plan 019 follows the prior
maximum 018; both new-file diffs and `git diff --check` have no whitespace
diagnostics. The two saved outputs were inspected, including formula bounds,
toy analytic expectations and original journal ownership terminology. No tests
or reference calculations were executed during these documentation checks.

Planning inspected only source text and retained JSON/gzip/bytes/hashes. No
scientific evaluation, suite invocation, GPU/network/install, reconstruction or
optimizer ran. No jobs remain. No sandbox/permission or git failure occurred;
an unavailable `python` alias and a guessed Python-version source path were
ordinary command/path errors corrected using the specified interpreter and the
source-manifest path. No git mutation was attempted; parent owns assessments,
status and plan-stage commits.
