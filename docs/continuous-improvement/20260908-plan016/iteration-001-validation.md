# Iteration 001 validation — Plan 017

All commands ran from the repository root using the existing `.local/envs/stg-render/bin/python`, standard library/Pillow only. No model modules, torch or CUDA were imported by the audit/tests. No historical data was mutated.

1. `.local/envs/stg-render/bin/python -m unittest discover -s tests -p test_selfcap_evidence_audit.py -v` — **10 tests passed**. Covers valid/changed PNG graph, missing component, duplicate/missing membership, incorrect time, production sweep mismatch, aggregate mismatch, unavailable-vs-failed distinction, output overlap/refusal, complete/internal-offline stage predicates, missing-manifest CLI report retention and missing-crop refusal. Required missing evidence produces a saved failed report and nonzero exit. Focused tests reran after implementation corrections only; no broad solver/GPU suite ran.
2. `.local/envs/stg-render/bin/python scripts/audit-selfcap-evidence.py --root . --output docs/experiments/selfcap-evidence-20260908 --assets-output .local/runs/selfcap-evidence-20260908` — first invocation returned **1**, with 6,669 passed, 2 unavailable, 2 failed. The two failures were auditor command-layout assumptions, not failed offline records or artifact mismatches. All image/model bindings passed and 17 sheets were generated. The original [audit](../../experiments/selfcap-evidence-20260908/audit.json) and its original auditor hash remain preserved.
3. `.local/envs/stg-render/bin/python /tmp/selfcap-recheck.py` — **passed**; corrected only affected command-stage predicates, strengthened shared sweep predicates, and added ATGS loss AST / FreeTimeGS runtime-training binary comparisons. Exact recheck script is embedded under `recheck_script` in [audit-02.json](../../experiments/selfcap-evidence-20260908/audit-02.json), with final production auditor hash and original report hash. Effective result: **6,701 passed, 2 unavailable, zero failed**. The original 2,080 image hashes were not unnecessarily recomputed. The supplement supersedes exactly `freetimegs:stages` and `atgs:stages`. Both STG historical binary attestations remain unavailable.
4. Image viewing tools opened all 17 original generated sheets, covering full 4120/4150/4179; full windows 4120–4122, 4148–4152, 4177–4179; every named crop at each window; and poses 0/10/19. [Inspection report](../../experiments/selfcap-evidence-20260908/inspection.md) lists every viewed sheet, all required times and observations. No full-sequence playback claimed.
5. `.local/envs/stg-render/bin/python /tmp/validate-selfcap-docs.py` — **passed**: 123 touched Markdown local links resolve; 699 indexed inspection asset references exist; all 17 sheet hashes match; HTML uses local assets with no external fetch/scripts; 13 Bash blocks / 14 copyable commands in the new workflow pass `bash -n` and their options match read AST/parser definitions. All four final published metric/FPS rows and deltas agree with saved values within displayed rounding precision. No training/render/evaluation command was executed. The temporary checker initially included the separate resource-table rows in its metric-table selection; correcting that bounded selection made the check pass.
6. `git diff --check` — passed. Task paths explicitly staged; staged diff inspected before the separate authorized local commit. Parent-owned status/assessments are excluded. Git status and code review show no historical experiment edits, source restoration or new model files. No generated PNG is staged.

The ledger hash before and after is `d0b4daa1aee79361580af3a1bf8fbc597148db7775b26f169a0a1b2e6ac90957`. Its 22,523.417254-second charge, individual 7,200-second allocations, failed-attempt accounting, zero overruns and no live reservations are preserved. Final-continuation measurement wall time, sampled device baseline/peak and framework allocator peaks remain separate quantities.

No sandbox/device/network failure, outside-sandbox retry, or permission blocker occurred during evidence work. An early editing command used unavailable plain `python` (`command not found`); it made no changes and was corrected to the authorized existing interpreter. This was a command-name error, not a suspected sandbox denial.

For A1–A4 outcomes and unchanged SC gaps, see [implementation results](iteration-001-implementation.md). The local commit ID is returned to the parent for the final run checkpoint. The next necessary numerical phase is blocked on explicit scope/time/attempt authorization, not successful objective attainment.
