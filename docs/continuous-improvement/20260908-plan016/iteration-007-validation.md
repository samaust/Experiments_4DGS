# Iteration 007 validation

- Admission passed for original cases 12/13, four state slots, with complete
  original input equality and v10–v15 provenance. All 2064 predecessor retention
  leaves passed at T0 +562.687496452 seconds, before the +600-second deadline.
  The immutable supplement binds original admission and is rechecked by the
  frozen verifier. No historical source or evidence bytes changed.
- Parent inspected all six modules, the full test and source/count/coverage maps
  before freeze. Source/AST and immutable hash checks passed without numerical
  imports. The exact scientific schedule is 211 unique entries, 141 observed
  operand identities, 44 outputs, and 10 replay records on original indices 12/13.
- Suite 1 failed readiness in 3.560534628 supervised seconds. Groups 1–13,
  16–20 and 22 passed; group 14's active sequence-5 public-frame objective H
  differed from the fixed oracle at eleven entries, and groups 15/21 were
  dependent prerequisite failures. Maximum retained error is
  3.085531830038235e-11. Exact inventory is incomplete. All assertion records,
  actual counts, source/test hashes, stdout and supervisor evidence remain saved.
- After saving the result, final supplemental toy protocol/fixture JSON
  serialization failed on an unconverted NumPy array (`congruence.H`). The
  artifact is missing. This frozen-test retention bug cannot be repaired within
  the current test-byte contract; no rerun occurred. The result, assertion records
  and full stdout/error log remain available.
- No specific source-contract violation was found that justified suite 2.
  No tests, source formulas, fixture bytes or tolerances were changed after
  freeze. No second toy, direct numerical import, scientific invocation, fit,
  training, rendering, device/network or new scientific state occurred.
- The readiness command recorded no applicable passing suite. Retained-only
  packaging passed integrity with no source failures, but is incomplete:
  zero scientific entries/completions and all 44 expected scientific outputs
  missing. Supplemental summary explicitly records known readiness failure and
  zero executed states, avoiding the generic package's scientific-only flag.
- Toy process cleanup passed: worker stopped, live descendants empty, within
  deadline. No scientific worker was launched and no owned jobs remain.
- Historical allocation/training records are unchanged. The initial stdlib-only
  admission command used a mistaken nonexistent package-hashes.json filename;
  it failed before writes, was corrected to retention-manifest.json, and the
  full admission passed. This was an implementation filename error, not a
  sandbox, device, network or Git failure.
- Final JSON/hash/source/local-link/whitespace checks are retained in
  [static validation](../../experiments/basketball-shared-timing-v16/static-validation.json).
  The task-only local milestone commit and final post-commit elapsed time are
  retained in the subsequent parent checkpoint/handoff.

[Plan 023](../../../plans/plan_023.md), [implementation](iteration-007-implementation.md),
[report](../../experiments/basketball-shared-timing-v16.md),
[failure details](../../experiments/basketball-shared-timing-v16/readiness-failure-analysis.json).
Passing static or package-integrity checks does not establish A2, SC-01/SC-03,
arithmetic qualification, or main-objective attainment.
