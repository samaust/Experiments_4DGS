# Iteration 005 Plan handoff

Decision-complete [Plan 021](../../../plans/plan_021.md) implements the
[Review recommendations](iteration-005-recommendations.md) against the unchanged
[objective](objective.md) and [post-review assessment](iteration-005-assessment-01-review.md).

It finalizes one new 5,400-second implementation phase with admission by +900,
readiness by +3,600, all scientific work/cleanup ended by +4,500, and one supervised
900-second numerical invocation. The fixed cohort has 16 cases/32 cold-returned
slots, 96 complete candidate bundles, 16 residual/J cold transforms and SVDs,
32 independent full-component bundles, at most two setup keys per method, and
two invocations of the same frozen 20-case toy suite. Section 6 quantifies every
low-level preparation/component/report/coordinate/comparison entry. Fits, new
scientific states, screens, training/rendering/GPU/network/install remain zero.

The exact returned canonical bytes remain authoritative. Public-y replay is
tested once per fresh adapter; inexact mappings are rejected before evaluation,
retained as explicit public-path failures, and never become replacement states.
Independent canonical evidence continues on the scheduled admitted states.
Actual provenance/ownership violations stop dependent work. An overall evaluator
pass requires both canonical and public-coordinate qualification, and cannot
establish accepted timing or reconstruction completion.

Standing approval is AGENTS.md `b62f2ea` plus the explicit same-objective resume.
No routine execution-budget question is required. Parent must record the plan
hash/link, applicable ceilings/history, one T0 and absolute deadlines in
`iteration-005-plan021-authorization.json` before dispatching implementation.
The new allocation fits existing constraints because it charges zero training
and does not reuse consumed historical work. No phase clock began in Plan.

Readiness is not yet established: implementation must complete the plan's
read-only source/dependency/provenance admission and pass the frozen applicable
toy suite before the single scientific invocation. This is a concrete execution
gate, not a missing routine authorization. No planning blocker is known.

Planning read AGENTS.md, objective/status/assessment/Review, Plan 020, the v13
integration map and relevant v2/v6/v10/v11/v12 source. It inspected retained
JSON/gzip identities and computed byte hashes only, including all cold q/x pins
and saved quadrature-source hash. No scientific imports/evaluation/arithmetic,
tests, fits, GPU/network/install, git mutations or subdelegation occurred. Only
Plan 021 and this handoff were written. No jobs remain. Parent owns assessment,
status, authorization and commits; the next stage is fresh Implement after its
required assessment/allocation check.

Static validation found no missing Markdown targets or trailing whitespace in
either new file. `git diff --check` passed. Two guessed test filenames were
absent during an optional source search; these were ordinary missing-path errors,
not permission failures, and no evaluator was run. A documentation patch context
mismatch was corrected without changing scope or executing numerical work.
