# Plan 023 source-only coverage review before numerical freeze

The six v16 modules and complete 22-group test are bound by
[pre-freeze-source-map.json](pre-freeze-source-map.json). This review performs
no numerical import, evaluator call, fixture execution or count-discovery run.

[Arithmetic contract](arithmetic-contract.json) records every new formula,
lexicographic pair and left-fold reduction order, exact toy/science helper
inventory, source call-site map, and the expression-derived Jet counts. The
unchanged spatial expression makes 32 multiplications, 22 additions, three
reciprocals, two square roots, three variable constructions and 96 initializers.
Group 10 contributes two multiplications, three additions, one reciprocal, one
square root, three variables and 12 initializers. The complete source graph is
saved rather than inferred from a numerical run.

Candidate projection preserves first derivatives and default helper arity,
retains focal before principal addition, and uses direct centered observations.
Full geometry retains errors and spatial gradients once; transform-only skips
curvature/depth paths. HP, HT, Hcam, rig and spatial curvature compute upper
pairs directly. Data assembly reduces retained observations and cameras in the
specified order, then applies offset mixed and diagonal terms. Objective full
sum and q-to-x scaling assign each analytical pair to both positions. Hacc,
Hz and Hg retain their distinct original arithmetic paths.

Reference uses its own scalar Jet formulas, requires supplied Hessians to be
finite and exactly symmetric, and independently accumulates data pairs.
Its unchanged error expression retains consumed errors/loss gradients without
reevaluation. Candidate and Reference congruences have separately implemented
right-first and left-first folds. Both report paths validate physical objective
matrices before pair scaling; transport validation precedes contraction.
Only H/Hdata use the new primitive, including the driver's new-P transports.

[Readiness coverage](readiness-coverage.json) maps stable assertions to all
20 preserved production/kernel/protocol/process groups and two prescribed
stress groups. Group 21 owns one complete call/report per method on the fixed
constant dyadic scene, reuses the inactive group 12 setup keys, compares every
physical and report field against the independent Decimal80/Fraction/closed
Bernstein oracle, and checks consumed errors exactly. Its oracle calls no
production projection, basis, Jet, congruence or inverse. Group 22 owns one
successful and one rejected congruence input per method against nine independent
Fraction double sums. Group 19's asymmetric-output case passes its supplied
reference report directly to the comparator; both symmetry assertions remain.
All actual counter boundaries increment at entry; transform-only curvature and
contraction sentinels reject accidental added work. Frozen inventory comparison
requires exact map equality, not only maxima.

[Expected schedule](expected-schedule.json) has 159 Candidate, 46 Reference and
six driver entries, in original case 12 then 13 order. Archive access is keyed
by original index and follows both calculator closures. Actual audit observers
bind admitted q/x/coefficient/multiplier/setup bytes and retained callbacks;
public rejections retain canonical fallback separately. Science inner counters
belong to the currently active audit stack and exact count agreement precedes
the completed-worker artifact. The selected arithmetic-pass label also requires
intact source/package/schedule evidence and explicitly covers only 4/32 states.

The stdlib admission retains the full original provenance/input chain plus the
four-state selection. [Package supplement](admission-package-supplement.json)
validated all 2064 predecessor retention leaves at monotonic 284014.146701879,
before the admission deadline 284051.459205427. It preserves the original
admission and is hash-pinned and rechecked in the current verifier.

The shared verifier remains on toy/ready/run/worker paths. Markers consume an
invocation before numerical imports; PID/PGID/SID/token/thread checks, source
hashes, absolute deadlines and supervisor cleanup remain mandatory. Science is
capped at 600 seconds including import/setup/I/O, with five seconds reserved.
Package validation uses only retained bytes/JSON/hashes and keeps completeness,
numerical mismatch, public API rejection and historical corroboration separate.

Parent independently inspects the changed source and final test/coverage map
before the immutable freeze; its completion is retained in the freeze record.

## Parent inspection completed before freeze

The orchestrator inspected the six current modules, all changed test paths and preserved group definitions, the independent candidate/reference pair and congruence formulas, exact residual oracle, actual source counters, shared admission/launch/worker/package paths and final source maps. Only source, AST, retained JSON and hashes were read; no numerical import, fixture or evaluator was executed by the parent.

The source-derived Jet initializer and arithmetic counts match the fixed expression graph. The scientific schedule has 211 unique low-level entries, 141 actual-operand observation identities, 44 top-level outputs and 10 replay contracts, with original case indices 12 then 13. Existing 20 groups retain their assertions; added groups21/22 use the prescribed complete calculators and independent oracles. The planned asymmetric-output test routes directly to the comparator, and the new input-rejection test uses both production transport primitives.

Inspected corrections reject asymmetric physical objective matrices before report scaling, preserve and bind the timely predecessor-package supplement, retain inner-work counts before their completion assertion, and require admissible source/package/schedule evidence for selected arithmetic success. Legacy public replay and all original criteria/gates remain unchanged. This clears source inspection only; numerical readiness still requires the complete frozen suite and exact inventory.

Inspected SHA-256 values:
- `scripts/basketball_acceleration_decimal_v16.py`: `1c0791ac9f7f1fd20d771495aa6d49d734b074a2cc6dd83c83d63d5ce273ed93`
- `scripts/basketball_shared_accounting_v16.py`: `bb5a1f2e4cf9637c2430080c4b5de120e523fe15a833ea5f54e4ee3e5ef21791`
- `scripts/basketball_shared_components_v16.py`: `c94e4f56f5c6d1529254e129497dcd4924154dfce1fed5000c306c5c9c507d5e`
- `scripts/basketball_shared_evaluator_v16.py`: `c4a2baef3b4280e45d065d02ad23c15c0d1f3b89e9607beaaa481b3d87ad26ee`
- `scripts/basketball_shared_reference_v16.py`: `fd293436a9a491ea126a872b2bdae037376ded5160f2402a93f79dd0f587d96c`
- `scripts/basketball_shared_solver_v16.py`: `8d1a7051c2cc8af74ed48e2a8482f7360a1cb50d88d460d4f197c05c178f504d`
- `tests/test_basketball_shared_evaluator_v16.py`: `a705efe32c0db9e42cce2ecab87b4a5aa0cd66f7bdbae5f45a48408d58178f7b`

Inspection completed at monotonic 284333.708503403. No consumed toy or science marker exists at this check.
