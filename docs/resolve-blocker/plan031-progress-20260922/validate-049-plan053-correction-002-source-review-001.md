# Independent Plan053 Correction002 exact-source review 001

**Verdict: PASS for Correction002 criteria C1–C4 on this exact nine-path source set. No diagnostic or aggregate clearance.** I reviewed [Correction002](plan053-correction-002.md), its [implementation report](plan053-correction-002-implementation.md), and the previous [Correction001 source review](validate-049-plan053-correction-001-source-review-001.md). This was static source/test/evidence review only: I did not run project tests, launch a process, import project code, open a pidfd, run a diagnostic or aggregate, stage or commit, or read `prompts`.

## Exact inputs and source binding

Correction002 SHA-256 is `bc6bd549483525d4bbee508187d087d0be786a00142796872e237277756bf77d`; the final implementation report SHA-256 is `8391b533a56917ef225acfc1093d5f67d54d8542e3b5e71842cea483ab83533c`; prior review SHA-256 is `c21ed676cbe1b32b0d80b0d263ad40037d1569dfb0f229970572f0d2bef548ff`. The final [nine-path manifest](plan053-postedit-source-003.sha256) is SHA-256 `1ee0013681b3ec112d211afa7ea514920f1396435befad32c79ce575a45fe6d6`. An earlier `sha256sum -c` read reported a transient mismatch on the recovery test during a concurrent final edit; individual rehash and two subsequent complete manifest checks matched all nine final rows. This verdict binds only the stable final values below.

| Path | SHA-256 |
| --- | --- |
| `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py` | `ae8468db5e808609c152739480548520b608f2011dbd5a53d1ad0642d1d8bb94` |
| `scripts/vipe_benchmark/s1_helper_session.py` | `ff6ad2373e9ed4501f7bfb55afd75891f3e17899ce47c10ff7d6bc6665788587` |
| `scripts/vipe_benchmark/s1_validation_capture.py` | `62359a258543731b96690d3abc7bd3ddede4809ae76ae4ec2ff83e5c4ce95fd7` |
| `scripts/vipe_benchmark/s1_validation_contract.py` | `07a397a90aaee7c5951f2f1e2dfb8f50251503ca93fe092d328056373cb57fea` |
| `scripts/vipe_benchmark/supervisor.py` | `1f8fb109c48ab121a232d3348ecaec1644b651119742c1d6339c6ce87faf2878` |
| `tests/test_vipe_benchmark_budgets.py` | `6bac13ad5582f4bd28a86ea5c42989d08d8932f236e3797a288807100edcefa6` |
| `tests/test_vipe_benchmark_s1_helper_fixtures.py` | `faccb5561ad8373d6805b50b0326f0eff8d00e9c9d9760d5829096fca917732b` |
| `tests/test_vipe_benchmark_s1_recovery.py` | `2f2971b444455be1ac50b350a857adb07977b49cc38920a66dd17f85ac87d450` |
| `tests/test_vipe_benchmark_supervisor.py` | `03e41c199a63c21cf754f0ec3f571a85f79ac417457344d83220f31399b9ce36` |

Only `s1_helper_session.py` and the existing recovery test differ from Correction001's final nine-path hashes. The correction adds no source path, method, suite, scenario or selector, and does not change the Plan049/Correction015 numeric caps or deadlines. Main reports AST parse of all nine and scoped `git diff --check` PASS; no broader runtime result is inferred.

## Criterion decisions

| Criterion | Verdict | Source and focused evidence |
| --- | --- | --- |
| C1 — direct child terminal-poll equality | **PASS** | `_job_state` now compares both `pid` and `wait_status` from `descendant-retired.terminal` to the matching completed `terminal_poll` before marking the direct wait complete (`s1_helper_session.py:644-664`). The existing recovery method accepts the matching child path, rejects a changed terminal status and different polled PID, rejects another poll after terminal, and still rejects retirement with an open pin (`test_vipe_benchmark_s1_recovery.py:948-970`). Root and pinned-descendant terminal comparisons remain in place. |
| C2 — poison-marker storage failures | **PASS with explicit uncertainty limit** | While holding the ledger lock, `poison` checks marker write length, fsync, exact readback and lock-file size (`s1_helper_session.py:688-706`). Cap rejection raises a specific durable-stop-unverified error if that persistence fails (`:715-718`). Rollback failure and verified rollback preserve the original append error while adding the marker-persistence failure as explicit exception evidence (`:737-747`). The local attempt stops. If the storage operation fails, cross-process marker state is honestly **unknown**; the source does not claim durable success or acceptance. Focused injected marker write, fsync, readback and rollback-marker faults at test lines863-889 exercise these branches. |
| C3 — fresh-process cap/rollback refusal and ownership | **PASS for the specified bounded check** | For both a successful cap marker and a verified short-write rollback marker, the test starts a fresh Python child through `create_owned_process`, retains its exact `Popen` handle/PID, waits for exit0, calls `retire_owned` on the same handle, checks `child.args` against the exact command, and correlates the child's reported PID (`test lines800-862`). Each child tries `job_ledger_events` and `append_job_event` on the marked sidecar and reports both poisoned refusals plus unchanged before/after SHA-256 and byte count; parent rechecks the committed prefix. These two serial child cases establish that a separate process consults the persistent marker. The test's child is intentionally outside a live S1 job ledger, so its `retire_owned` call is the legacy/no-ledger path; this test establishes physical exact-handle wait and marker behavior, not a new ledger-owned child retirement receipt. |
| C4 — focused checks and scope | **PASS for this correction stage** | Main reports `test_execution_mutations` PASS after the final edit and three related recovery methods PASS before the last assertion-order change, with the changed method rerun afterward. The unavailable supervisor integration trio fails at the explicit `_thread.start_joinable_thread` feature check under the repo-local CPython3.12 interpreter; it was not exercised and no claim of its pass is made. That interpreter limitation does not negate the four focused correction criteria, but broader helper/supervisor integration remains unverified before runtime acceptance. Syntax/diff/hash checks are reported PASS, and the independently repeated hash check above binds the final files. |

The five prior-review issues are closed in this narrow source review: repeated nonterminal `WNOHANG` polls and terminal transition, direct descendant wait replay, persistent marker behavior on successful writes, closed signal/pidfd result schemas with uncertain-close handling, and frozen OS candidate handoff. Correction002 specifically closes the remaining direct-child equality and fresh-process/durable-stop evidence. Static PASS is a source gate only. Main still owns fresh source/artifact/ownership/capacity/output/high-water/session preflight and separately cleared diagnostic work; the CPython3.12 native-thread limitation leaves broader integration evidence unverified rather than silently passed.
