# Plan053 correction 002 implementation

Correction002 changed only `scripts/vipe_benchmark/s1_helper_session.py` and the existing `ReceiptContractTests.test_execution_mutations` method in `tests/test_vipe_benchmark_s1_recovery.py` among the nine authorized Plan053 source paths. It changed no deadline, assertion, numeric cap, selector, suite member, or scenario. No diagnostic, aggregate, staging, or commit occurred.

Direct matching-child `descendant-retired` replay now compares both terminal PID and wait status against its completed terminal poll before consuming the wait operation. The focused test accepts a matching direct retirement and rejects a changed terminal status, a poll for a different PID, a repeated poll after terminal observation, and retirement with an unresolved pin.

Ledger poison now checks exact marker write length, fsync, byte-for-byte readback, and file size while holding the lock. A persistence failure sets the process-local stop and surfaces `durable stop unverified` as an `OSError` for cap rejection or as an explicit note on the original append failure. It does not claim a durable marker when these steps fail. Focused injected write, fsync, readback, and rollback-marker faults cover those paths. Storage failure still leaves cross-process stopping uncertain; that uncertainty is fatal and must not be counted as acceptance.

For each successful cap or verified-rollback marker, the test launches one fresh repository-local Python child at a time through `create_owned_process`. It retains the exact `Popen` object and PID, waits for its return code, calls `retire_owned` on that handle, and asserts that `child.args` equals the exact command. The child reports its PID, read and append errors, and before/after ledger digest and byte count; the parent checks the reported PID against the returned handle, return code zero, both poisoned refusals, and an unchanged committed prefix. Both serial child cases passed. The child command is `(sys.executable, '-B', '-c', <fixed inline marker-check script>, <temporary ledger path>, <canonical terminal JSON>)`; its full dynamic arguments and result are checked by the test, without making a new suite member or persistent sidecar.

Validation:

- `PYTHONPATH=scripts .local/plan053-correction001/bin/python -B -m unittest tests.test_vipe_benchmark_s1_recovery.ReceiptContractTests.test_execution_mutations`: **PASS**, 1 test in 12.627 seconds after the final edit.
- `PYTHONPATH=scripts .local/plan053-correction001/bin/python -B -m unittest tests.test_vipe_benchmark_s1_recovery.ReceiptContractTests.test_positive_complete_fixture tests.test_vipe_benchmark_s1_recovery.ReceiptContractTests.test_accounting_mutations tests.test_vipe_benchmark_s1_recovery.ReceiptContractTests.test_execution_mutations`: **PASS**, 3 tests in 16.068 seconds before the final child assertion ordering change; the changed method was rerun afterward as above.
- `PYTHONPATH=scripts:tests .local/plan053-correction001/bin/python -B -m unittest tests.test_vipe_benchmark_supervisor.HelperIntegrationTests.test_spawn_constant_reaped_under_actual_runner tests.test_vipe_benchmark_supervisor.HelperIntegrationTests.test_owned_live_pid tests.test_vipe_benchmark_supervisor.HelperIntegrationTests.test_owned_unreaped_exited_pid`: **UNAVAILABLE**, all three error at the `s1_helper_session.py` native-thread feature check (`:2114` in the final source) with `RuntimeError: S1 requires native joinable ownership thread`. This repo-local CPython 3.12.3 lacks `_thread.start_joinable_thread`; no sandbox denial or alternate interpreter probe occurred.
- `python3 -B -` with `ast.parse(Path(path).read_bytes(), filename=path)` on all nine manifest paths: **PASS**, `AST parse OK: 9 authorized paths`.
- `git diff --check --` followed by all nine manifest paths: **PASS**, empty output.
- `sha256sum -c docs/resolve-blocker/plan031-progress-20260922/plan053-postedit-source-003.sha256`: **PASS**, all nine paths verified. The manifest SHA-256 is `1ee0013681b3ec112d211afa7ea514920f1396435befad32c79ce575a45fe6d6`.

The [exact nine-path manifest](plan053-postedit-source-003.sha256) is the post-correction source binding. A distinct exact-hash review PASS and fresh Main preflight remain required before any diagnostic.
