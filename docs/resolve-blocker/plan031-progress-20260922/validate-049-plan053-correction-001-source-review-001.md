# Independent Plan053 Correction001 exact-source review 001

**Overall verdict: NEEDS_CORRECTION. No diagnostic or aggregate clearance.** This is a static re-review of the five findings in [post-edit source review 001](validate-049-plan053-postedit-source-review-001.md), against [Correction001](plan053-correction-001.md) and its [implementation report](plan053-correction-001-implementation.md). I read the current nine paths and focused regression source; I ran no project import, test, fixture, process launch, pidfd operation, diagnostic, aggregate, staging or commit, and did not read `prompts`.

## Exact inputs and scope

The previous review is SHA-256 `15594b39109b3aa2454da0976badc7eada7b1cc215b02a68faea9a584f7ea05a`; Correction001 is `f5c35c3a3b2175fee2392596ff25ddef73eaacfaf807c207b765f771fee68591`; the implementation report is `029106fdea90c6c622e134eb282e7985a3af4a6086b87ffcd08f187bab3d9014`. The fresh [nine-path manifest](plan053-postedit-source-002.sha256) is SHA-256 `f9b4cdc1ef8390e0027afb7b680825574c2dad35c818f5642c2ae9ab0608a015`; I independently ran `sha256sum -c` and all nine entries matched.

| Path | Current SHA-256 |
| --- | --- |
| `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py` | `ae8468db5e808609c152739480548520b608f2011dbd5a53d1ad0642d1d8bb94` |
| `scripts/vipe_benchmark/s1_helper_session.py` | `b5a0ef11728468b085eb472e2160f5ead057560725aed7c93949b2869f824af3` |
| `scripts/vipe_benchmark/s1_validation_capture.py` | `62359a258543731b96690d3abc7bd3ddede4809ae76ae4ec2ff83e5c4ce95fd7` |
| `scripts/vipe_benchmark/s1_validation_contract.py` | `07a397a90aaee7c5951f2f1e2dfb8f50251503ca93fe092d328056373cb57fea` |
| `scripts/vipe_benchmark/supervisor.py` | `1f8fb109c48ab121a232d3348ecaec1644b651119742c1d6339c6ce87faf2878` |
| `tests/test_vipe_benchmark_budgets.py` | `6bac13ad5582f4bd28a86ea5c42989d08d8932f236e3797a288807100edcefa6` |
| `tests/test_vipe_benchmark_s1_helper_fixtures.py` | `faccb5561ad8373d6805b50b0326f0eff8d00e9c9d9760d5829096fca917732b` |
| `tests/test_vipe_benchmark_s1_recovery.py` | `3a636c657725b137cd63eee7ecf5f65a5633ebb559a57e1e9c6bf577df97e2af` |
| `tests/test_vipe_benchmark_supervisor.py` | `03e41c199a63c21cf754f0ec3f571a85f79ac417457344d83220f31399b9ce36` |

Only helper-session source and the existing recovery test changed relative to the prior nine-path review hashes. The other seven are byte-identical. No tenth project source path, method, suite, scenario, selector, behavioral deadline, numeric cap or original assertion change is apparent in this correction. Main reports AST parse and `git diff --check` passing for the nine paths; I did not rerun execution-oriented checks.

## Criterion verdicts

| Criterion | Verdict | Evidence and remaining issue |
| --- | --- | --- |
| C1 — repeated/nonterminal `WNOHANG` and terminal binding | **NEEDS_CORRECTION** | `wait_owned_pid` now appends a uniquely sequenced `wait-poll-intent` and `wait-poll-result`, returns `(0,0)` without retiring, and prevents another poll after a terminal result (`s1_helper_session.py:526-547,836-870`). Root `root-wait` and pinned `descendant-wait-result` compare their terminal fields with `terminal_poll` (`:548-558,580-587`). **Direct `descendant-retired` does not make that comparison** (`:644-660`). After a terminal child poll for PID303/status0, a direct retirement row for the same child with a different wait status passes the `fresh_wait` branch, so replay does not bind its retirement to the terminal observation. The focused child case at `test_vipe_benchmark_s1_recovery.py:880-891` begins with `root-wait-claim`, not a terminal poll, and misses this path. |
| C2 — direct descendant retirement replay | **PASS for the previous rejection, subject to C1** | `_job_state` now exempts only the current matching wait operation key from its unresolved-operation check (`:650`); another unresolved operation or open pin remains blocking (`:656-659`). The focused direct replay and open-pin rejection at test lines880-891 exercise those branches. The missing terminal-poll comparison above remains a separate correctness failure. |
| C3 — durable attempt-wide poison | **SOURCE PASS for successful marker write; cross-process test UNVERIFIED** | Cap and verified rollback now call `poison` with its persistent-marker default (`:705-707,731`); readers and appenders take a file lock and reject a nonempty marker (`:462-475,685-688`). The focused test resets the process-local flag and checks refusal (`test lines806-821`), which proves the disk-marker branch within one process. It does **not** demonstrate that a newly launched process opens the same lock, sees the marker, and refuses both read and append while leaving the ledger prefix unchanged. That is the minimum missing regression. The marker write/fsync exception is also swallowed at source lines693-696; if that operation fails, the source cannot claim a durable marker and must explicitly report that uncertainty and stop the attempt rather than treating the local flag as cross-process proof. |
| C4 — closed nested schemas and uncertain close | **PASS for reviewed paths** | Signal and pidfd targets use `_job_proc_identity`; signal receipt is exact `sent/error`, and successful close receipt is exact `closed=True,error=None` (`:219-245`). The uncertain-close exception emits `operation-uncertain` and no `pidfd-closed` success (`:951-960`). Focused malformed result and injected close checks appear at test lines892-919. |
| C5 — immutable OS candidate | **PASS for reviewed paths** | `_prepare_owned_candidate` freezes Popen argv as OS bytes, env as a private mapping, explicit executable and absolute cwd, stdio descriptors, and mutable option sequences; spawn/exec argv and file actions become tuples (`:331-361`). `create_owned_process` rerenders from this candidate after reservation and sends its checked frozen arguments to the selected OS create (`:1530-1549`); capture and driver perform corresponding checks. Focused caller mutation of Popen argv/env and spawn argv/file actions is at test lines768-779. This is source equality, not runtime coverage of every selected caller. |
| C6 — focused regression and preservation evidence | **NEEDS_ADDITIONAL_CHECK** | Main reports one focused mutation test PASS after the final edit, and three related recovery methods PASS before its final uncertain-close assertion, followed by a rerun of the changed method. This is credible local regression evidence for those methods, but it does not include the fresh-process lock test, terminal-poll-to-direct-descendant mismatch rejection, or environment-bound helper/supervisor ownership regressions. Those checks and a new exact-hash source review are required before diagnostics. |

The previous Plan049/current-dispatch version binding remains in the seven unchanged paths and is not altered by Correction001. The hard 8,388,608-byte ledger, 16,384-byte descriptor, environment/host limits and `B+max(1,H)≤8` remain in source. The correction does not itself grant launch clearance. Resolve C1's direct-child evidence mismatch, make marker-failure reporting explicit, and supply the fresh-process marker and relevant owner regressions before requesting another exact-source verdict.
