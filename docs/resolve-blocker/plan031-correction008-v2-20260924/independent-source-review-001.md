# Independent exact-source review 001 — Plan054 / Correction008 v2

**Verdict: NEEDS_CORRECTION for the C5 source gate. No launch clearance.** The current three-file implementation establishes the principal v2 file-proof and driver checks, and the nine selected source hashes match the implementation report. It does not yet satisfy the adopted P4/C4 synthetic-control list: there is no independent original nested tool result versus copied-file mismatch control, no failed/ambiguous/duplicate `ADMIT` tool-send or outer-terminal control, and no target-plus-readback outer-H overlap control. The process-wide integer conversion setting also changes unrelated parsing/formatting in every importer and has no containment or regression control. These are source-review findings; the absence of a live transcript is a separate, later gate.

## Exact review basis

I read `AGENTS.md`, Plan049, Plan053, Plan054, the exact amendment proposal and independent plan review, both adoption status records, the bounded status and implementation report, and inspected the current nine authorized paths. The mutable status records below identify what I read, not a runtime preflight. SHA-256 was computed locally from bytes:

| Input | SHA-256 |
| --- | --- |
| `AGENTS.md` | `3d09a19b0bf8e4bfcf7c25f5b001a769f2cce372a425fc1dd1a14fc299ef620f` |
| `plans/plan_049.md` | `89d1f634ad2407b02558ff9c030d4a4f12f8f4ab010c93e2cf532876564afe7e` |
| `plans/plan_053.md` | `33f7a8a1d292dade248295a3594b0d4bdff451f1685ad35bc104065be7178252` |
| `plans/plan_054.md` | `91d0ac7191d944b8cf2175b74c74aff3d70d4dba4a530431f223cb8aab2df62d` |
| `docs/resolve-blocker/plan031-session-proof-wrapper-20260923/correction-008-trust-amendment-001-proposal.md` | `9a27dbb60afcf358409364987e0b74d219431e4b5a83652ac9c607f658426d84` |
| `docs/resolve-blocker/plan031-session-proof-wrapper-20260923/correction-008-trust-amendment-001-independent-review.md` | `ba80adcab217be4070a2ce6e5fb47ab99c30403565c7cf8d0edabae909665730` |
| `docs/resolve-blocker/plan031-session-proof-wrapper-20260923/status.md` | `8fe808d8adae395fc581bd8cb893544c9357e71655a090bd3b7cf1edac461a31` |
| `docs/resolve-blocker/plan031-progress-20260922/status.md` | `34a491c7e45b579c82948cf18171fc4fd4bc9092c9cd297884c1f24b676bd1b3` |
| `docs/resolve-blocker/plan031-correction008-v2-20260924/status.md` | `66ea06ea8bf2ec98cce9b9a7c08ccfe95f37ba50199422f69bed9a6ac0241c66` |
| `docs/resolve-blocker/plan031-correction008-v2-20260924/implementation.md` | `088294fb7ef9e8a0eba93e9cef556446344622b5a6fddb6bb6080830a6c66a00` |

| Authorized path | Current SHA-256 | Change from `HEAD` |
| --- | --- | --- |
| `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py` | `45f817336d5eb8c41404336d0c497aced3eca1f8087fd07e5814fb6e589aca9f` | changed |
| `scripts/vipe_benchmark/s1_validation_capture.py` | `62359a258543731b96690d3abc7bd3ddede4809ae76ae4ec2ff83e5c4ce95fd7` | unchanged |
| `scripts/vipe_benchmark/s1_validation_contract.py` | `6ac5c3335a73aebccf4ad70be582fd4d827db677d4c48d24101dca5199778323` | changed |
| `scripts/vipe_benchmark/s1_helper_session.py` | `ff6ad2373e9ed4501f7bfb55afd75891f3e17899ce47c10ff7d6bc6665788587` | unchanged |
| `scripts/vipe_benchmark/supervisor.py` | `1f8fb109c48ab121a232d3348ecaec1644b651119742c1d6339c6ce87faf2878` | unchanged |
| `tests/test_vipe_benchmark_s1_helper_fixtures.py` | `faccb5561ad8373d6805b50b0326f0eff8d00e9c9d9760d5829096fca917732b` | unchanged |
| `tests/test_vipe_benchmark_supervisor.py` | `03e41c199a63c21cf754f0ec3f571a85f79ac417457344d83220f31399b9ce36` | unchanged |
| `tests/test_vipe_benchmark_s1_recovery.py` | `8e4739d123679df7262426a83a6d331f78ab40593464f4d3dbccafa21a7b7f70` | changed |
| `tests/test_vipe_benchmark_budgets.py` | `6bac13ad5582f4bd28a86ea5c42989d08d8932f236e3797a288807100edcefa6` | unchanged |

`git diff --check` passed. I parsed all nine files with Python AST and compared them to `HEAD`: all function-name/type inventories are identical; test assertion calls are identical in seven unchanged files, while the recovery test replaces its v1 proof-mode assertion with v2 and adds one maximum-width assertion; wait/join/poll call expressions remain identical. The implementation report records a passing focused synthetic test, unchanged collection of 78 source members, 249 methods, 1,028 typed callbacks and nine suites. I did not rerun that test because the source comparison revealed no newly changed implementation after its run. No Plan049 workload, process probe or live session was run in this review.

## Criterion findings

| Criterion | Source finding | Verdict |
| --- | --- | --- |
| P2 / v2 file contract | `session_proof` at contract lines 521–652 requires exact v2 proof/event keys, roles, contiguous ordinals and indexed paths; `{utc}` only; start ≤ return and prior return ≤ next start; exact command/start options, positive typed same handle in start and all polls, readiness boundaries, mandatory `pre_admission` and `at_admission` empty polls, output SHA-256 and complete bootstrap/readiness text. It rejects non-null exit, present non-false `isError`, present non-null `error`, present non-false truncation flags and output containing `truncated`. `session_json` rejects duplicate keys and nonfinite JSON. CR/LF is kept in the output hash. | **PASS for file/schema source**, subject to actual-return comparison by Main at live review. |
| P2 / driver and authority | Driver lines 129–145 type and correlate the received start frame to its bootstrap and v2 event. Lines 273–303 require one exact `ADMIT\n`, rehash both adopted authority files, parse/recheck the proof and event equality, and bind records into admission/note; lines 319–340 rehash all note bindings and sources before exec. `no_timeout_launch` lines 682–730 repeats strict admission/note and authority binding. | **PASS for source checks.** A driver cannot observe the original Main/tool return or prove file-copy equality with it; that must be established from the visible transcript at the separate live gate. |
| P3 / logical jobs | The unchanged `s1_helper_session` ledger reserves the outer H in `attempt-open`, records the same handle in `main-handle`, validates a prospective row before each append, and charges each live B/H root with `B+max(1,H)≤8`. Its descendant records attach to an exact B root and block that root's retirement while unretired; ownership, wait claims, pidfd operations and ambiguous states remain checked. The unchanged capture owns the runner B through exact `Popen` wait. | **PASS for reviewed in-repository logic.** Main's outer control/readback `exec_command` sessions are outside these nine source files; their actual serial reservation, overlapping H count, terminal and retirement still need live Main evidence. |
| P4 / synthetic controls | Recovery `test_execution_mutations` lines 1125–1345 adds valid v2 and ≤16,384-digit handle controls and negative proof/event/UTC/error/truncation/hash/authority controls. Existing extracted-driver checks reject a preexisting admission, wrong stdin chars and altered local identity. Its lines 1454–1457 expressly leave original send/terminal evidence to Main. No new control supplies two differing objects (actual returned result and checked copy), a failed/lost/duplicated `write_stdin` result or outer terminal, or concurrent target-H plus readback-H admission accounting. A file mutation and rehash is not an original-return mismatch. | **NEEDS_CORRECTION.** Add bounded existing-method synthetic controls for these exact boundaries, explicitly labeled synthetic, without adding methods/callbacks or making a synthetic object a live attestation. |
| P4 / interpreter scope | Contract lines 16–19 call `sys.set_int_max_str_digits(20000)` at import time. This persists across the entire interpreter, including unrelated JSON parsing and decimal formatting in tests and production callers importing this module. It is needed for a 16,384-digit handle with Python's default guard, but the chosen placement broadens every caller's limit and the test checks only acceptance/rejection of handles, not restoration or other-caller behavior. | **NEEDS_CORRECTION.** Scope the increased conversion guard to the specific proof/frame parse and serialization operations with restoration, or document and prove a reviewed process-wide policy with a noninterference control. Keep the inclusive 16,384-digit and overwidth cases. |
| C1 / ordered UTC and trust | The source contract implements ordered UTC and checked file references. The original visible nested call/result and immediate observations are not inputs to the validator. | **NEEDS_CORRECTION as an acceptance gate** until P4 controls pass and an actual visible result is matched to each copy in a separately reviewed live attempt. Static schema alone is not live C1. |
| C2 / same handle | All pre-admission file events and the driver's start frame use one typed exact handle. The proof declares `ADMIT\n`; the driver consumes that input. No source in this review captures the actual Main send result, subsequent same-handle poll, outer terminal, or owned retirement. | **NEEDS_CORRECTION as an acceptance gate** until required controls and live send/terminal/retirement correlation exist. No repeated admission is allowed on an ambiguous send. |
| C3 / capacity and ownership | The source ledger groups registered descendants and checks B/H roots, but it cannot count external Main control sessions. No live overlapping readback H or unresolved outer terminal has been observed. | **NEEDS_CORRECTION as an acceptance gate** until Main's transition ledger includes every outer control/readback session and independent runtime review accepts exact ownership/retirement. |
| C4 / focused controls and preservation | Scoped functions, original wait call expressions and test declarations are preserved; focused test evidence is recorded. Specific P4 controls above remain absent. | **NEEDS_CORRECTION.** |
| C5 / distinct exact-source PASS | The nine hashes and static preservation checks are complete in this review. P4/C4 and the process-wide import side effect prevent a source PASS. | **NEEDS_CORRECTION.** Re-review exact final bytes after correction. |

The parser accepts extra native result members because the result must remain verbatim; a later reviewer must compare the complete original returned object, including any optional tool metadata, with each checked copy. The file validator alone cannot make that comparison. No v1 file is upgraded. The driver’s internal `stamp()` still records monotonic time for its pre-existing launch note and exec record; those values are not used as a v2 Main event-admission criterion or a physical-time inference.

Fresh Main source/authority/artifact/output/high-water/vacancy/ownership/capacity preflight, separate live C1–C3 validation, B1–B4 evidence, diagnostics and aggregate remain open. This review neither clears a launch nor measures ledger size/content.
