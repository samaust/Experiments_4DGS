# Independent exact-source review 002 — Correction001

**Source verdict: PASS for the Correction001 P2–P4 / C4–C5 source gate on the exact bytes below.** Review001's four missing-control categories and persistent import-time integer-limit effect have been corrected within the authorized paths. This is static and synthetic acceptance only. It does not establish the actual visible Main/tool returns, C1–C3 live acceptance, B1–B4, preflight, admission, diagnostic or aggregate clearance.

I reviewed [Correction001](correction-001.md), its [implementation report](correction-001-implementation.md), the immutable [review001](independent-source-review-001.md), and each changed source diff against `HEAD` and review001's candidate. I retained the Plan049/053/054 and adopted trust-amendment interpretation from review001 and rechecked current exact authority hashes. Correction001 is SHA-256 `8e742a9a506b1ca2ba0c7cdc7b52042f4051628f16e9febbce8e1cb55f79508c`; implementation report `4d75ec1693bcb034eb4d0fe5218a3e06ffd3ebe2e39ffeed8c02fb2434b59d72`; review001 `b59d051820ee162744283a1bce5d32f90f86e0990746f429db98550ba3afce93`. Plan054 remains `91d0ac7191d944b8cf2175b74c74aff3d70d4dba4a530431f223cb8aab2df62d`, and the adopted amendment remains `9a27dbb60afcf358409364987e0b74d219431e4b5a83652ac9c607f658426d84`.

## Exact nine-path snapshot

| Authorized path | Current SHA-256 | Change from `HEAD` |
| --- | --- | --- |
| `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py` | `e28b3874d1830c934fae624eee254a25d25517fcc22cb6fa41f4da5191d62af7` | changed |
| `scripts/vipe_benchmark/s1_validation_capture.py` | `62359a258543731b96690d3abc7bd3ddede4809ae76ae4ec2ff83e5c4ce95fd7` | unchanged |
| `scripts/vipe_benchmark/s1_validation_contract.py` | `69058bc74765caa17aa877fc1cbeb7c2b9e892f4304daf273ff96a3c5b78fd78` | changed |
| `scripts/vipe_benchmark/s1_helper_session.py` | `bb6ea896a73ab3bbdf98e4ac4a9e15edf769a4484995dae44e2d4fca3e34f44d` | changed |
| `scripts/vipe_benchmark/supervisor.py` | `1f8fb109c48ab121a232d3348ecaec1644b651119742c1d6339c6ce87faf2878` | unchanged |
| `tests/test_vipe_benchmark_s1_helper_fixtures.py` | `faccb5561ad8373d6805b50b0326f0eff8d00e9c9d9760d5829096fca917732b` | unchanged |
| `tests/test_vipe_benchmark_supervisor.py` | `03e41c199a63c21cf754f0ec3f571a85f79ac417457344d83220f31399b9ce36` | unchanged |
| `tests/test_vipe_benchmark_s1_recovery.py` | `10ac98a8316231b3ec45478e83bc5fdcaaa9335958598e7d0b820c1adc1f7edd` | changed |
| `tests/test_vipe_benchmark_budgets.py` | `6bac13ad5582f4bd28a86ea5c42989d08d8932f236e3797a288807100edcefa6` | unchanged |

`git diff --name-only` over the nine paths returned only the four marked paths. `git diff --check` passed. Local AST parsing and `HEAD` comparison found identical function-name/type inventories in all nine paths and identical wait/join/poll call expressions. The recovery test changes its authorized v1-to-v2 assertion and adds 20 assertion calls; no prior assertion outside that version change was removed. The implementation report records the passing focused `test_execution_mutations` test (one synthetic method, 10.218 seconds) and unchanged 78 source members, 249 methods, 1,028 typed callbacks and nine suites. I did not rerun that unchanged passing test; no Plan049 workload was launched.

## Finding and criterion disposition

| Criterion | Independent finding | Source verdict |
| --- | --- | --- |
| P2 / C1, v2 schema and UTC | Contract `session_proof` still requires exact v2 proof/event keys, event roles/ordinals/paths, `{utc}` stamps with each start ≤ return and previous return ≤ next start, exact bootstrap/readiness bytes/hash, same-handle start and polls, and both mandatory live polls. Duplicate/nonfinite JSON, non-null terminal, tool errors, truncation, changed result/output hash and mixed v1/v2 evidence remain rejected. Driver start-frame and pre-exec checks remain bound. | **PASS, static source.** Live C1 still requires original returned objects and read-back copies. |
| P2 / C2, exact handle and scoped conversion | The contract has no import-time `set_int_max_str_digits`. Driver frame parsing/handle width and start-event serialization, contract evidence parsing/width/frame parsing/start hashing, and helper ledger parsing/serialization/prospective-row parsing each save the prior limit and restore it in `finally`, including exception paths. The test accepts exactly 16,384 digits, writes a 16,385-digit proof token for rejection, checks unrelated default `json.dumps` still fails beyond the prior guard, and checks restoration after parser and serializer exceptions. | **PASS, static source and synthetic coverage.** The guard is temporarily interpreter-wide while a bounded operation runs; there is no persistent import-time change. Live retained handle, send, terminal and retirement remain unproved. |
| P2, exact authority | Driver and contract still bind exact Plan054 and amendment file records in proof, admission and launch note and rehash all note bindings and sources before exec. Original authorization/Correction008 and ordered correction/addendum records remain. No new source path or selector appears. | **PASS, static source.** Fresh current authority hashes are still a Main preflight duty. |
| P3 / C3, logical jobs | Existing job ledger still reserves outer H and registers exact handle, charges B/H roots under `B+max(1,H)≤8` on each prospective append, attaches descendants to their launched B root, and prevents premature root retirement. Added existing-method synthetic states charge target H then overlapping readback H at `B=6,H=2,charge=8`, reject `B=7,H=2`, retain ambiguous readback, retire readback to H=1, then target to H=0 with the reserve floor still charged. | **PASS, static source and synthetic transitions.** Main must record the real target/control/readback outer sessions and exact terminals; in-repository source cannot establish them. |
| P4 / C4, original return versus checked copy | The new control mutates a checked poll copy's native metadata, rehashes the file reference, confirms the v2 file validator still accepts the internally consistent proof, then rejects equality with a separately retained original result. It explicitly labels this a synthetic Main boundary, not live attestation. | **PASS for requested synthetic distinction.** No source can attest the visible nested tool return; Main's live transcript comparison remains mandatory. |
| P4 / C4, send and terminal ambiguity | The new existing-method loop rejects absent/lost/error/wrong-handle/duplicate `ADMIT` sends and absent/lost/live/wrong-handle/error outer terminal cases; only one matching send and matching terminal satisfy the synthetic predicate. The driver's unchanged exact `ADMIT\n` read and preexisting-admission rejection are also covered. | **PASS for requested synthetic controls.** These predicates are in the test, not a production Main tool wrapper; actual send/terminal evidence must be reviewed separately. |
| P4 / C4, preservation | Four source/test paths changed, and AST function and wait inventories remain exact. No deadline, selector, original error precedence or workload creator was altered by Correction001. Existing suites and counts remain as recorded. | **PASS, source.** |
| C5, distinct final source review | All nine current hashes independently match the implementation report; review001 findings above are closed for the authorized source stage. | **PASS, source gate only.** |

The synthetic send and original/copy checks use direct predicates inside the existing test method because the Main/tool transcript is outside the repository's callable source. Their PASS proves coverage of the reviewed decision rule, not that a future Main capture actually follows it. The scoped integer guard temporarily changes the interpreter setting during its `try` body; the source restores the exact prior value on normal and exceptional exit. Real concurrent callers during that short interval are not measured by these synthetic tests and must not be inferred from them.

**Remaining gates:** Main's fresh source/authority/artifact/ownership/capacity/output/high-water/vacancy preflight; separate live original transcript versus file-copy comparison; same-handle `ADMIT`, subsequent poll, outer terminal and retirement review; C1–C3 runtime acceptance; B1–B4 diagnostics/aggregate and measured ledger content/size. This report grants no launch clearance.
