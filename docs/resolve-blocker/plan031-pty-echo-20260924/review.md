# Independent Review — Plan031 PTY start-frame echo

Review finding: **Yes, the exact observed echo can be recognized without weakening the exact-transcript contract.** Acceptance of a correction remains unverified. This is a read-only diagnosis of the preserved attempt008 evidence, not source review or runtime acceptance.

## Findings

1. The original visible Main `write_stdin` result for ordinal 1, copied in [`event-001`](../plan031-progress-20260922/main-session-049-diagnostic-008-event-001.json), contains exactly one visible JSON start-frame echo followed by `\r\n`, then the readiness JSON line followed by `\r\n`. The sent `arguments.chars` is a one-line JSON frame ending in `\n`; the echoed body matches those sent characters before the final newline. The PTY rendered that final newline as `\r\n`. Ordinal 0 contains the sole bootstrap announcement; ordinals 2 and 3 are same-handle live empty-output polls. All four event paths, byte counts and SHA-256 records are bound by [`main-session-proof-049-diagnostic-008.json`](../plan031-progress-20260922/main-session-proof-049-diagnostic-008.json), and Main's readbacks report equality to the original visible objects. The copies and readbacks are secondary evidence: the original visible tool result is the stipulated finite trust boundary, not something a file hash independently attests.
2. [`session_proof`](../../../scripts/vipe_benchmark/s1_validation_contract.py) already checks the exact sent frame's parsed content, same handle, event order, live returns, untruncated output and each output's UTF-8 SHA-256. Its final transcript comparison permits only `bootstrap + readiness_line + (LF or CRLF)`. The extra echoed frame therefore explains the recorded exit-2 `ValueError: complete bootstrap/readiness announcements; unexplained output rejected` in [`proof-validation-003`](../plan031-progress-20260922/main-session-049-diagnostic-008-proof-validation-003.json). This is an observed compatibility gap, not evidence that arbitrary extra output should be accepted.
3. No `ADMIT\n` was sent. The retained handle 76028 was interrupted at the driver's admission read, with outer exit code 1 and `KeyboardInterrupt` in [`abort-001`](../plan031-progress-20260922/main-session-049-diagnostic-008-abort-001.json). Attempt008 is historical and consumed. Its saved proof cannot be used to resume or admit that session.

## Supported correction and testable C1 criteria

Derive the only permitted echo from the **actual ordinal-1 `arguments.chars`** after the existing exact structured-frame check. Require exactly one terminating sent `\n` and no embedded literal CR/LF in the JSON frame body. For the observed PTY case, the echo bytes must equal `arguments.chars` without that final LF, followed by exactly `\r\n`. Compare the entire concatenated original output to `bootstrap + echo + readiness_line + its already permitted LF/CRLF terminator`, while retaining the individual original outputs, event boundaries, complete-line/readiness-event correlation, and their existing hashes. Do not strip, parse and reserialize, normalize the whole transcript, or search for a matching substring. If support for another terminal rendering is proposed, it needs separate observed evidence and review.

C1 is testable when the exact preserved attempt008 event values are accepted **only** under that full-byte expectation; omission, duplication, altered JSON spelling/spacing/order, changed handle or payload, changed echo newline, prefix/suffix bytes, extra readiness or later output, truncated/terminal/error response, reordered event or failed hash/readback must fail. The same checks must reject a file copy that differs from Main's original visible result even if the copy is internally self-consistent. Synthetic existing-method controls can exercise these negatives; they do not prove a later live session. The historical event files and proof must remain unchanged. Exact Plan054 and adopted amendment hashes, strict paths and source hashes must continue to be checked; the current `s1_validation_contract.py` byte hash observed in this review is `69058bc74765caa17aa877fc1cbeb7c2b9e892f4304daf273ff96a3c5b78fd78`, which requires fresh verification at the implementation gate.

The correction must stay within [Plan054](../../../plans/plan_054.md)'s reviewed nine source/test paths and preserve the Plan049/Plan054 ordered UTC, exact-handle and original-result/readback trust model; CPU-only serial ownership `B+max(1,H)≤8`, the 150 GiB artifact, 64 MiB memo and 8,388,608-byte ledger caps; all source/test collection, selectors, assertions, W/C/protocol deadlines and error precedence. Operational duration has no new ceiling. Main must obtain the planned independent source and runtime reviews and fresh preflight before any new attempt. No test or diagnostic payload was run for this review; reviewer CPU time, elapsed wall time, token use and any unmeasured overhead are **unknown**, not zero.

**This Review grants no implementation, admission, test, diagnostic, aggregate or runtime clearance.**
