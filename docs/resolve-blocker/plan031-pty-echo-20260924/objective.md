# PTY echo blocker resolution objective

Resolve the pre-admission Plan049 diagnostic008 proof failure without admitting diagnostic008 or weakening Plan049 trust, resource, scope, deadline, or evidence rules. The Main `write_stdin` result for session handle 76028 contains the terminal's byte-for-byte echo of the structured start frame before the driver's readiness line. Existing `session_proof` currently rejects that complete visible return as unexplained output.

Preserve the exact original tool result and its hash. A bounded correction may recognize only the exact echo derived from the recorded `arguments.chars`, including the observed PTY newline rendering, and must still reject changed, missing, duplicate, truncated, or unexplained bytes. Add regression controls through existing test methods only, stay within Plan054's reviewed nine paths, preserve all other behavioral assertions/deadlines/order/resource caps, obtain independent plan and source review, and revalidate before a fresh attempt index.

Never read `prompts`. Do not reuse attempt 008 or its identity request. Diagnostic005 remains failed historical evidence. Any new launch requires fresh source/authority/artifact/ownership/capacity/path/high-water checks, the same-handle Main transcript, independent live-proof review, and serial CPU-only authorization already in force.
