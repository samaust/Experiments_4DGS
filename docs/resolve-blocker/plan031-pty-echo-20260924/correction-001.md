# Correction001 — one observed PTY echo before readiness

Prepared 2026-09-24 as the focused addendum to [Plan055](../../../plans/plan_055.md). **Proposal only:** no source/test edit, launch, admission, diagnostic, aggregate or runtime clearance is granted here. This addresses the exact pre-admission attempt008 failure recorded in [initial evidence](initial-blocker-evidence.md) and diagnosed in [review](review.md); attempt008 stays terminal and unadmitted.

## Exact comparison contract

`session_proof` already checks the v2 start frame sent at ordinal 1, its event-0 value and exact handle, all event roles/timestamps, live tool results, UTF-8 output hashes, truncation, strict file records and the readiness identity. Preserve those checks and their order. Let `sent = events[1].arguments.chars`, using that actual validated string. The new branch is eligible only if `sent` is one JSON line with exactly one final LF and no literal CR. Its one allowed echoed output is exactly `sent[:-1] + '\r\n'`; no byte in the body changes. Let `bootstrap` be the exact original event-0 output and `line` the exact proof readiness line. Preserve the preexisting no-echo choices `bootstrap + line + '\n'` and `bootstrap + line + '\r\n'`, including split readiness. Add only `bootstrap + echo + line + '\n'` and `bootstrap + echo + line + '\r\n'`, and for that new branch require the entire ordinal-1 result output to equal `echo + line + terminator` with readiness-completion ordinal 1. Compare the complete concatenation of original event outputs against those exact alternatives, retaining the original readiness-completion check for both branches. Bootstrap and readiness retain their existing LF/CRLF treatment; the echo's terminal CRLF is fixed by attempt008 evidence. Do not strip, search, parse-reserialize or normalize the output. No echo is permitted in another event position or twice.

Keep every original Main tool argument/result object and its per-event output hash. The event file is a read-back copy; acceptance still requires Main's original visible result to equal that copy, including non-output fields, even if a modified copy and its hashes agree internally. Any derived echo is an expectation for comparison only; it is not a replacement result or a new attestation. No proof may be built solely from historical files for live acceptance.

## Negative-case decisions

The bounded existing-method synthetic controls must reject changed, partial or duplicate echo; body spelling, whitespace, field order or handle changed **only in the echo** while the sent frame stays fixed; an LF-only, CR-only, doubled-CR or otherwise changed echo terminator; output prefix/suffix; second readiness line, extra bytes on a later poll or wrong completion event; wrong/failed/terminal/truncated tool result, wrong handle, missing/reordered/duplicated event or required poll; wrong output hash; and a changed copy/readback against the preserved Main result even when rehashed. A changed sent frame must still satisfy the exact structured frame/event-0 equality and handle checks; reserializing a semantically equal frame is not an independent failure if the PTY echo still exactly matches the bytes actually sent. This distinction prevents the test from silently imposing a historical JSON-serialization format on future Main calls.

A complete omission of the echo with exact readiness matches the existing accepted no-echo branch, so `session_proof` alone cannot reject that string without invalidating the original positive case. Treat omission of the observed attempt008 echo as a **Main-original versus checked-copy mismatch**; a partial omission is independently rejected by whole-output comparison. Preserve the existing no-echo and split-readiness positive assertions. Do not add an echo-present declaration or widen the proof schema to make absence distinguishable: that would be a different trust change requiring separate authorization.

## Scope and gates

Expected source delta: `scripts/vipe_benchmark/s1_validation_contract.py` and the existing `ReceiptContractTests.test_execution_mutations` in `tests/test_vipe_benchmark_s1_recovery.py`. The hard path ceiling is the nine reviewed Plan054 files listed in Plan055. No new method, callback, file, selector, assertion removal, deadline shift, error-precedence change, wrapper, writer, process or thread. Preserve the **78/249/1,028** collection, nine suites and six direct-script selectors, serial CPU-only ownership `B+max(1,H)≤8`, 150 GiB artifacts, 64 MiB memo and 8,388,608-byte ledger caps. Original W/C and protocol/scenario deadlines remain unchanged. No operational duration ceiling is added.

Sequence: exact-byte independent plan PASS; scoped implementation and bounded focused tests; fresh nine-path hashes and independent exact-source review; fresh Main source/authority/artifact/ownership/capacity/output/high-water/vacancy preflight; independent runtime review of a newly captured same-handle original transcript and checked copies before any separately cleared serial diagnostic. Attempt008's index/path/identity request cannot be reused. Historical diagnostic005 remains failed. If original-result equality, handle, retirement or resource state is unknown, stop admission and retain the charge. All later B1–B4 and final aggregate gates remain open.

Planning used read-only source/evidence inspection and wrote only this addendum and Plan055. No test, payload, admission or diagnostic ran. Tool CPU/token use and unmeasured overhead are unknown.
