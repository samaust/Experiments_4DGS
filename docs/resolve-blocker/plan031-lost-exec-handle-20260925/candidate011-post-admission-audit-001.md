# Candidate011 post-admission diagnostic and retirement audit 001

**Verdict: NEEDS_REVISION for Plan049 diagnostic acceptance and complete ownership retirement.** Candidate011 was admitted and its CPU-only diagnostic actually ran; it failed. The prior computer restart does not retire any candidate011 process: kernel boot ID `072f8a59-03c2-4df4-af1f-c44aa0407253` and boot time `2026-09-25T05:08:02Z` precede its 07:17:59Z launch and match its saved process identities. The user's administrator statement and the old reboot evidence establish retirement only of the previously reviewed pre-reboot local process trees. They do not resolve those old native session identities/H charges, and do not establish post-candidate011 process-group retirement.

The driver published the launch note and exec-start, then `execve`d the capture. Admission is `approved=true`, no-timeout, diagnostic index 11; the note binds the admission and 78 unchanged source records. Capture started the actual runner PID 13352 in PGID 13352 under driver/capture PID/PGID 10687, boot ID above. `execution.json` records 140.178602031 seconds, `wait_completed=true`, `timed_out=false`, runner return code 1, and a matching `Popen.wait` retirement with `absent_after=true`. Main's exact handle 49924 has a saved terminal `write_stdin` result, exit code 1. The structured diagnostic receipt records 73 collected/executed tests, 568 subtests, 26 passed cases, 12 failed cases, 35 error cases, no skips, and `passed=false`; raw unittest stderr counts 18 failure events and 40 error events because it includes multiple failing subtests. Thus this was a real failed suite, not a pre-admission or no-run failure. The original behavioral W/C and equality-is-late assertions are not accepted merely because the outer no-timeout invocation completed; stderr includes named before/equal/after deadline failures.

The dominant early cause is exact and reproducible from saved bytes without running code. `s1_helper_session.py::owned_workload` requires the launch note bindings to contain the immutable dispatch's original Plan049 record, SHA-256 `a27329ee5c9d9439515093e6797ad72f61bd6faf1e43c5f45f2b2cf4c013cc1f`. The note contains the adopted current Plan049 record, SHA-256 `89d1f634ad2407b02558ff9c030d4a4f12f8f4ab010c93e2cf532876564afe7e`, and correctly binds the dispatch record separately; the note's status bytes/hash equal the dispatch status snapshot. Therefore the predicate fails solely on the original-versus-adopted plan digest, raising `ValueError: owned dispatch/plan/status binding` in helper startup. L01/L02, many L/P cases, and their failed cleanup assertions record that error. This mismatch is in frozen `s1_helper_session.py` (SHA-256 `bb6ea896a73ab3bbdf98e4ac4a9e15edf769a4484995dae44e2d4fca3e34f44d`); Correction015 expressly does not authorize an unreviewed edit to that file. The older immutable dispatch and adopted revision both remain authoritative in their respective roles.

The mismatch does not explain every failure. The saved stderr independently shows L03 `native owner thread identity unresolved`; the Plan046 launch-deadline before control fails an original assertion; Plan047 next-input controls raise `TypeError: string argument without an encoding` while serializing observation evidence; `ack_receipt` equal/after controls fail the named-successor assertion; and the runtime-guard interior path reports a pending primary with a secondary iterator-close error. These are distinct observed failures requiring bounded causal review under Correction015's unchanged assertions/deadlines. The diagnostic cannot establish F1–F10 or Plan049 B1–B4, and no aggregate is cleared.

The 211-row job ledger (SHA-256 `d2355c03799a06a95a07b6d4bdf84268a18a78440971c16dddd6a89008719121`, 134,753 bytes) passed independent contiguous sequence/previous-hash/row-hash checking. It has 45 `root-reserved`, 41 `root-bound`, 41 `root-wait`, and 41 `root-retired` events, no descendant event, and no `session-terminal` event. The registered B runner token `eb59857b3f0a4701b2a8acb30af47a0e` was retired at row 210 by the same child wait, PID 13352 return code 1. Four H `Session owner thread` reservations, all made by runner PID/PGID 13352 on this boot, have no matching bind or retirement event:

| Ledger row / UTC | Token | Supported association |
| --- | --- | --- |
| 5 / 07:17:59.857742Z | `e175041be92c4058975845554e211121` | Before L01's recorded constructor start; exact case identity/handle return is not in the ledger. |
| 16 / 07:18:02.927932Z | `92d4f7ad24ba4b029d30b09921ebc028` | Time aligns with L03's constructor and its native-owner-identity error; no bind/handle return. |
| 177 / 07:18:06.946461Z | `814e338743244c76b5a5e82583a0a46e` | Time aligns with L36's constructor; no bind/handle return. |
| 178 / 07:18:07.098992Z | `c467215da3c543bb951179f2cd53f2fa` | After L36's recorded end and before P01's recorded start; exact case identity/handle return is unavailable. |

The source reserves each H immediately before `_thread.start_joinable_thread`; an unbound row therefore cannot prove creation succeeded or failed. These threads cannot survive termination of their creator process, so the exact runner wait establishes their local thread lifetime ended, although the ledger remains logically unresolved and must not be rewritten or described as internally retired. The outer native Main session has a terminal exit result, so its live H is retired. A read-only current `/proc` observation saw three namespace rows (PIDs 1–3), no PID/PGID 10687 or 13352, and no read errors, but this namespace is too narrow to prove host-wide absence of detached descendants. The suite's failed cleanup confirmations also prevent a blanket claim that every process group was retired. Preserve candidate011 as consumed and conservatively retain any uncertain descendant/ownership charge until a reviewed, same-host retirement reconciliation; retain the two older unknown native H=1 charges separately.

Key SHA-256 records: Plan049 `89d1f634ad2407b02558ff9c030d4a4f12f8f4ab010c93e2cf532876564afe7e`; Correction015 `a3242ed06131aa9ca00bc05180805c8d5bf5ea8d965919b92054154f10a00bab`; dispatch `d7177d2dae0d553321863481af72647f210557087c54c3b802c8317f572937be`; admission `61454a0df03e79651c547e75216134c046022c8d5dbcc2ad9eab0dcff06f3d79`; launch note `ef38a898781ffa41f1bd8f4ae8ca47f8bfe693b9b713eea6a187acf33d05d7c8`; driver exec-start `6f50638de7ed1fd0d9663aadb916c2f345ea3a0e38fca70b9b19403669d8f7a9`; execution `bba8e5fded7b50e7dfad318b3a76c39441e3f6b89306942c969f00a6a4e5a5c4`; receipt `76c9d0dea6ee7850abb468ff845d3a959553c30ed0281e2687bda1475cbd32c3`; process stdout `09abf29ca1a850cdde21c1135dd552ed651499bf50b5a378931bb7926c864b5c`; process stderr `0bed457e145184a77c6651b935afdfb9f9cd1bf119bef9160d5ea0350088a62c`; driver stdout `62652d69caaaca297b0fb7832fbeeb48bababa3b5eca2758c225296381ad7245`; driver stderr is empty, SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; Main terminal event `725cd841181008fc55aed6254daa75f8b958e6076c20a93e29169c483562ed44`. The earlier reboot evidence is `988f1a3b2e94cb583ae93dbcb2ebe1aeb52b1a42cb2b6930d48b3aff225083b0` and records its 05:10:21Z observation for candidate009 only. Execution's receipt/log/interpreter/source/note references and note's admission reference matched current file bytes; all 78 execution source records agreed before and after.

Next safe action: preserve all candidate011 artifacts, resolve the four H reservation rows and possible detached-child ownership under the existing exact-retirement gate, then prepare a narrow reviewed plan/source correction for the original/adopted Plan049 binding mismatch and separately triage the independent deadline/evidence failures. Do not reuse index 011 or start another diagnostic/aggregate on this failed evidence. This audit used read-only `rg`, `sed`, Python 3 standard-library JSON/SHA-256 and current kernel `/proc` reads. It did not import project code, run tests, contact any tool session, or launch/admit work. Diagnostic wall time was measured by its own execution/receipt; reviewer wall/CPU time, token use, and host-wide process counts are unknown.
