# Candidate013 bootstrap start-proof independent review 001

**Verdict: PASS for the exact saved bootstrap return and one structured start-frame send only.** Main may send precisely one newline-terminated JSON `plan049-main-start-frame/v1` object with exactly `schema`, `session_id=56200`, and `event` equal to the **entire saved event000 object**, through `write_stdin` on the same returned handle **56200**. Main must preserve the original full send return before any further action. This review clears no poll, identity admission, `ADMIT`, test, diagnostic payload, or aggregate.

The original Main [raw return](candidate013-main-exec-start-001.json) is 1,758 bytes, SHA-256 `9a62cd6530f440532747f4a30ad428ee8c4b386b78e5084cb7107474f008197e`; its `raw_result` and `arguments` equal the respective objects in [event000](../plan031-progress-20260922/main-session-049-diagnostic-013-event-000.json) field for field. Event000 is 1,960 bytes, SHA-256 `98893bcd97511ff0c90438364195f535c3997a827b6f4a9d10b093a2b5e3bc53`, with exact `plan049-session-tool-event/v2` fields, diagnostic index 13, ordinal 0, role `start`, and tool `exec_command`. The raw result has chunk `817c7a`, wall return 1.000697535 seconds, original token count 117, and positive live session handle 56200. Its five fields contain no exit code, error, or truncation marker. The capture stamps match exactly and are ordered: 2026-09-25T09:21:13.928Z through 09:21:20.149Z. This is tool-return evidence, not a measured process lifetime or cross-namespace kernel identity.

The complete 467-byte result output is exactly one JSON bootstrap announcement plus CRLF, with no other line or truncation text. Fresh SHA-256 of its UTF-8 bytes is `3e36ed8e49d3083aa8a15ee750f39dbc51993f162bc8e7bbeb20387c33f307f9`, equal to event000's `output_sha256`. The announcement has only `awaiting_main_start_proof`, `index=13`, and `kind="diagnostic"`. Its sidecar object has exactly the fixed `.job-ledger-diagnostic-049-013.jsonl` path, matching `.lock` path, schema `registered-process-tree-job/v1`, and 64-hex-character initial SHA-256 `7d8d0cca2b017c091b03de4dd37fc112ae06e17991b4632d7133d7639896fa9d`. The ledger file itself was **not visible in this reviewer's filesystem view**, so I did not independently hash its first line or use its absence to infer session termination. Sidecar and identity verification remain for later evidence stages.

All ten `exec_command` argument fields equal the [candidate013 launch intent](candidate013-launch-intent-001.json), SHA-256 `7e14a63c23fe1ee63d55b3273a3d8bf8eca308dfc9b7bb15c64af4ad136913f6`, including the exact seven-token `shlex.join` command, `/bin/bash`, non-login TTY, repository workdir, `require_escalated`, 1,000-ms yield, 10,000-token budget, four-token prefix, and the validator-required literal justification `Run the reviewed Plan049 CPU-only diagnostic after the exact AF_UNIX datagram socket retry succeeded.` The launch intent's driver SHA-256 `bdf68e0eaef6648d4097c5f819527fff94573bacc202176309c5832a915f59b8` and frozen validator SHA-256 `2f522e8636b486ad7141daef105cb3980f11dc03b971728b68c9ec2cf245d2c0` match current bytes. The later [boundary002](candidate013-launch-boundary-002.json), SHA-256 `582bf00190d87203bd3862fe7b9481f68d38fb2d0c3f4cb3095e1c9da216ef92`, binds the same launch intent and reports PASS before the bootstrap; the [launch-readiness review](candidate013-launch-readiness-review-001.md), SHA-256 `fc3046412b2695424bb26e05810968a5bf77fb4ced1e6ad189dc6752d7604291`, cleared only that bootstrap.

Checks used read-only Python 3 standard-library JSON, SHA-256, UTC, and `shlex` comparisons plus static inspection of the frozen `session_proof()` start contract. I did not contact handle 56200, send a frame, poll, admit, run tests, or edit source or plan. The required next observation is the original full return from the exact start-frame send; no later session action is authorized by this review.
