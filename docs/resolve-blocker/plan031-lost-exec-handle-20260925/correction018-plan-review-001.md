# Correction018 plan 001 — independent review

**Verdict: NEEDS_REVISION**, exact plan SHA-256 `6192b3b3b63b70d367b9b6cdc666eff59076593ea0b11b3f4d38435486ae2a4e`. The production defect and narrow `qualify_runtime` direction are supported, but the proposed verification is insufficient for a new interpreter-symlink authority rule. This review gives no source-edit, test, diagnostic, admission or aggregate clearance.

The ELOOP diagnosis is correct. `s1_evidence.qualify_runtime` compares the manifest interpreter record to its `file_record` alias, `s1_progress.checked_file_record`, on the exact requested interpreter spelling. That reader deliberately uses `O_NOFOLLOW` on the final component. Candidate011's actual interpreter spelling `.local/envs/stg-colmap/bin/python` is a symlink, while `files.file_record(sys.executable)` used by loaded-runtime capture records the canonical target. Thus the focused `runtime_guard/before` error is reachable in production; equal/after are cascades.

The proposed pre/post strict resolution and guarded no-follow read of the canonical target are directionally narrow. Two requirements must be explicit before adoption:

1. Compare the **complete** guarded target file record (canonical path, byte count and digest) to the complete manifest interpreter `executable` record. Keep exact `invoked_executable == request.runtime.python`, inventory spelling, loaded-manifest strict record, and fresh W checks around each resolution and guarded target read. Reject a pre/post target mismatch; do not allow generic aliases, generic symlink following, or a resolved-path-only equality.
2. Extend the exact test scope to the existing `tests/test_vipe_benchmark_supervisor.py` runtime-guard fixture portion for bounded positive and negative controls against the **unpatched production** `qualify_runtime` path: unchanged admitted symlink succeeds before W; a changed link target, substituted target bytes, wrong invoked spelling/alias and mismatched manifest record fail; equal/after still reject at W without unauthorized successor work. The current plan says no test source edit and relies on the 114 existing cases. Those cases expose ELOOP and W boundaries, but the `first_guard`/`reuse_after_read` alias and substitution controls use a fixture-only `e.file_record` adapter; they do not verify the new production resolution rule. Add only a narrowly reviewed existing-test portion and preserve the same method, subcase count, assertions, deadlines and fixture cleanup. If a helper is necessary, name its exact path/portion before adoption.

The plan otherwise preserves CPU-only serial execution, `B+max(1,H)≤8`, 150 GiB artifacts, original W/C and 2/1 behavior, historical failures, independent source review before focused execution, and the no-diagnostic/no-admission/no-aggregate boundary. Main should adopt a corrected exact plan/review hash pair before editing; `s1_evidence.py` is outside Correction017's frozen source scope.

SHA-256: focused005 stderr `3ce9aae7a669a754f0bb550d98c0bc7c586ccf5b180ca9937b6f2c193fabcde7`; terminal `46c39fd5d7056ce83c4483a028ca708f127b072097c6649c2fedb74f283d8c62`; independent triage `f8473c646f3a11a62500d24d7c3e95c758afdd352eeef4aa37d6a931efaee0ee`; `s1_evidence.py` `14ea47c7cc56418ae7a8e0e7aa442fb7664774d8a8f15b27c558ddcf69c679db`; `s1_progress.py` `252185ab023e034c1884ffa370fae78af68b238752277f7fdaae934793d0271d`; supervisor test `6a302f9793dab7350f95da6de80a77cc201304a42aaecdf1ba6c1735b88ae0e6`.

Read-only checks: `cat`, `rg`, `sed`, `ls -l`, `sha256sum`. No imports/tests, source edit, process/session contact, launch or admission. Reviewer wall/CPU/memory and current live B/H were not measured.
