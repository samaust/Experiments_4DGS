# Correction017/018 focused 006 — independent saved-result review

**Verdict: PASS for this one focused deadline-step method**, not for a full Plan049 diagnostic or aggregate. Main's saved terminal for exact session 11994 records exit 0 and 84.362 seconds, and stderr independently says one test ran `OK` in 84.362 seconds. The saved output is 50,001,344 bytes (matching the terminal), parses as JSON, and has exactly 114 unique `(operation, when)` rows: 38 operations × before/equal/after. No row reports incomplete retirement or a forbidden successor entry.

Across the 38 before rows, all named targets are present; 37 have a named successor and no primary, while the one intentional `worker_popen` case has its expected `RuntimeError` primary. All 38 equal and 38 after rows have `TimeoutError`, no named target or successor, and no forbidden entry. Specifically, `runtime_guard/before` reports “real operation completed before W,” a completed target and successors, no primary, no forbidden entry and complete retirement. Its equal/after rows report `progress original work deadline`, empty targets/successors and complete retirement. Thus the previous ELOOP and missing-before-control cascade are absent in this saved focused result. The three new runtime-guard negative controls are asserted in the reviewed source and the method passed; they are not individually serialized in these 114 evidence rows, so their exact per-control observations are inferred from the executed assertion path rather than a separate receipt.

Current `s1_evidence.py` SHA-256 `b46124282c5aae4d5a28b43b5b4440bfe6ec7868f66a39cceda1ed8be717b27c` and supervisor test SHA-256 `86aa7cc3235f8b35b87008ab5078fd495d166bf0e8de4902963f7bc2155656f3` match independent Correction018 source review-001 SHA-256 `f5de480a1c32bfa4c8c277a74a7badb8d1153fbc3ecba9c6f33b0a8e0b9e231e`; `s1_progress.py` remains `252185ab023e034c1884ffa370fae78af68b238752277f7fdaae934793d0271d`. Correction018 adoption SHA-256 `0c3aa6a94b1f3f1730acfc47e65c96fb0474e7bf7f606fe39888731926457cbf` binds the reviewed plan and source-review gate. The focused 006 directory has no independently frozen execution-time source manifest, so these current hashes and the saved terminal do not alone prove the precise source bytes loaded at execution; later full-diagnostic gates still require fresh source/authority snapshots.

Evidence SHA-256: `plan047-deadline-steps.json` `14a09077865f817c2458f083f97970988041d61e396fd2b04092c120655ebec9`; `main-exec-terminal-002.json` `a439221d55b8db3ab58fdc45a43f4d5cba12b3cc2db0c22907041add26db53ac`; `stderr-001.log` `26fcdd5cd732b6955d8548a5f1229dbc5b29c3d73373753e7e51fefd0e0c5a05`. Prior failed focused runs remain immutable and do not become passes. No diagnostic launch/admission or aggregate is cleared by this focused result.

Read-only checks used `rg`, `cat`, `head`, `stat`, `sha256sum`, and Python 3 standard-library JSON parsing of the saved output; no project import, test execution, process/session contact, source edit, launch or admission. The focused test's elapsed time and output bytes are measured in its saved records; review CPU, memory, elapsed time, live B/H and artifact-capacity use were not measured.
