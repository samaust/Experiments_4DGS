# Correction017 follow-up plan 001 — independent review

**Verdict: NEEDS_REVISION** for proposal SHA-256 `1feaad63c2feff7ecf75c76932212a257e12931856e7ad50582272e282822e68`. The technical diagnosis and proposed local-error distinction are supported, but the edit boundary must be made internally consistent before Main adopts the extension or edits source.

The one primary focused error is `runtime_guard/before` at `test_vipe_benchmark_s1_helper_fixtures.py:1563`. This is the later **child-descriptor** `crossing_secondary` control, not the earlier `scandir_close` control. The fixture holds an unrelated pending `RuntimeError`, moves the clock to C immediately after the child FD is acquired, and injects a single `close_child_descriptor` `OSError`. `RetirementGate.boundary` records the crossing and switches to safety retirement without raising. In `OwnedTemporaryDirectory.cleanup`, the child's `finally` uses `sys.exception()` and mistakes the ambient caller error for a local child-traversal error; it suppresses the close failure, allowing the owner to be marked resolved even though the descriptor is in `uncertain_descriptors`. Consequently the pending primary has no `s1_temporary_owners` field. The assertion is valid; `runtime_guard/equal` and `/after` fail only because the required before control was never saved. The terminal record for exact Main session 76078 reports exit 1, 83.850 seconds, 111/114 subcases passed, two cascading failures and one error. No full diagnostic acceptance follows.

The minimal safe production change is the same local-error distinction used for iterator close, applied only to the existing child traversal/close block: a close failure is secondary only to a **locally active** child traversal exception; without one it propagates to the outer cleanup failure path. Keep the caller's original primary, one close attempt, the retained uncertain descriptor and unresolved owner evidence, and all C/W/equality and setup/cleanup assertions. The owner must never resolve while any close effect is uncertain. This is a production cleanup defect; weakening the fixture's ownership assertion would hide it.

Two plan edits are needed:

1. The Finding says no source path other than `s1_progress.py` changes, but work step 2 permits “add/update a focused fixture.” The existing `backend_retirement_transition_controls` already reproduces the fault and asserts original-primary identity, one close attempt, uncertain descriptor, and unresolved owner. Prefer reuse of that unchanged fixture; otherwise specify the exact fixture path and allowed portion as a second source-scope extension for independent review.
2. State that Main adopts the corrected, independently reviewed follow-up scope before editing, with a fixed proposal/review hash pair. Independent review by itself does not amend Correction017's frozen iterator-specific authority. Do not rewrite the existing adoption record.

After correction and adoption, independent exact-source review should precede a fresh-accounted focused method; its PASS would remain development evidence only. Do not change historical artifacts, deadlines, assertions, resource caps or the diagnostic/aggregate gates.

Evidence SHA-256: focused stderr `f11ba42f3b269f609bd8cd937d398ed3e556821c7c48342ebf5e3621951916b5`; session-76078 terminal `c7f4dbea70460e40adf1cfa075b0e0e7b0278898286b7757c5c3e74b1b767001`; `plan049-retirement-resource-scandir_close-True.json` `77a0533077c69e8ed65398031453f6e2e44e502f86d3df3fc627280f50f93889` (shows that earlier iterator control retained uncertainty, but is not the failing child-control receipt); production `s1_progress.py` `b5b23a6949a95806d3b80bde2de6c43669a6cbf527ac6067db70cf59d5d933ba`; fixture `test_vipe_benchmark_s1_helper_fixtures.py` `16ab88762078e06825b1f9c175524ce50fedb2e89bd96a63c951cc825aa9cbf0`; supervisor test `6a302f9793dab7350f95da6de80a77cc201304a42aaecdf1ba6c1735b88ae0e6`.

Read-only checks used `rg`, `sed`, `cat`, `nl` and `sha256sum`; no project import/test, source edit, process/session contact, launch or admission. Reviewer wall/CPU/memory use and current live B/H were not measured.
