# Correction017 follow-up plan 001 — independent re-review

**Verdict: PASS for the exact bounded follow-up plan**, SHA-256 `cc5eb522733f126c719185e60dbe75e7318a66f6c624ca221bda9ca0d284d541`. This is plan review only; it neither records Main adoption nor clears source edits, a focused invocation, a diagnostic, admission, or aggregate by itself.

The two findings in review-001 are resolved. The current plan limits edits to the child traversal/close portion of `OwnedTemporaryDirectory.cleanup` in `scripts/vipe_benchmark/s1_progress.py`; it explicitly reuses `backend_retirement_transition_controls` unchanged and forbids fixture/test edits for this follow-up. It also requires Main to adopt the exact independently reviewed plan hash before editing. That is a narrow extension of Correction017's iterator-specific local-error repair, not an implicit expansion of its seven-file implementation authority.

The failure mapping is sound: the real `RetirementGate.boundary` records C crossing without raising; an unrelated caller `RuntimeError` is visible through ambient `sys.exception()` in the child's `finally`; a single injected child-FD close failure is then suppressed, letting cleanup resolve despite `uncertain_descriptors`. The existing fixture's assertion that the original primary carry unresolved-owner evidence is valid. The reviewed repair distinguishes only a locally active child traversal error from that ambient primary: close failure with no local error must enter the outer cleanup failure path, while close failure after a local error remains ordered secondary. The one-attempt/no-blind-retry rule, uncertain FD and owner retention, original W/C/equality behavior, and all existing deadlines/assertions stay mandatory. The equal/after failures remain downstream of the absent before control and cannot be counted as independent passes.

After Main adoption, independent exact-source review is required before any further focused test. A later focused result needs a fresh source/hash, artifact, process/thread/capacity and output-path preflight; even a pass is development evidence only. Historical focused failure and Candidate011 remain immutable. The no-launch/no-admission/full-diagnostic boundary remains intact.

Evidence SHA-256: focused stderr `f11ba42f3b269f609bd8cd937d398ed3e556821c7c48342ebf5e3621951916b5`; session-76078 terminal `c7f4dbea70460e40adf1cfa075b0e0e7b0278898286b7757c5c3e74b1b767001`; `s1_progress.py` at review `b5b23a6949a95806d3b80bde2de6c43669a6cbf527ac6067db70cf59d5d933ba`; unchanged fixture `test_vipe_benchmark_s1_helper_fixtures.py` `16ab88762078e06825b1f9c175524ce50fedb2e89bd96a63c951cc825aa9cbf0`. Review used read-only `cat`, `rg`, `sed`, `nl`, and `sha256sum`; no imports/tests, process/session contact, source edit, launch or admission. Reviewer wall/CPU/memory and current live B/H were not measured.
