# Candidate012 pre-admission terminal and consumed-index review 001

**Verdict: PASS for bounded candidate012 terminal/retirement and index consumption.** Main must **not** send a start frame on session 97601: the saved exact same-handle EOT returned terminal exit code 1, and the original launch arguments cannot satisfy the validator's exact start-event contract. Candidate012 is consumed; the driver's next high-water index is **13**. This review grants no new launch, admission, diagnostic, test, or aggregate.

The preserved Main bootstrap return `candidate012-main-exec-start-001.json` (SHA-256 `773eeb4d95881ffd16ba43ff3b9503d6aef294a0b6679a9de239f2955ba95af6`) records exact `exec_command` arguments, positive native session handle **97601**, chunk `6fc2cf`, no terminal/error result, and the complete one-line `awaiting_main_start_proof` output with CRLF. That line binds diagnostic index 12 and the proposed `.job-ledger-diagnostic-049-012.{jsonl,lock}` paths, with initial digest `7dcb9c6e0e0b9d890e8be1d756e98dc46dcd4702a5aa0f5612c4f4ced357bd13`; it is a bootstrap proposal, not a created ledger or process identity. `candidate012-main-event-000.json` (SHA-256 `42e52f6ab68a6edeba79930765dc14e3ea518c74669214110c6dc1922cbb2d37`) has identical full arguments/result and matching start/return stamps. Independently hashing its exact output string gives `9d395d6e04f9fc865ddfacd2e52ba59a730227d8bab509ddcd14c8770badb1e5`, equal to its recorded output SHA-256.

The actual `justification` in that raw call and event is `Run the independently reviewed Plan049 CPU-only serial diagnostic bootstrap for candidate012; retain the exact returned session handle before any further processing.` The frozen launch intent `candidate012-launch-intent-001.json` (SHA-256 `e3348a098bbf8c754549f8bbe82620e10996367d537412ac274b83b63daba671`) and current validator require `Run the reviewed Plan049 CPU-only diagnostic after the exact AF_UNIX datagram socket retry succeeded.` All other saved launch argument fields match the intent. Current validator SHA-256 is `2f522e8636b486ad7141daef105cb3980f11dc03b971728b68c9ec2cf245d2c0`; driver SHA-256 is `bdf68e0eaef6648d4097c5f819527fff94573bacc202176309c5832a915f59b8`. Since the validator requires exact argument equality, the actual event000 cannot become a valid Main start proof by changing later metadata.

`candidate012-main-abort-001.json` (SHA-256 `ae768a36634053e511df251aa5ab63b409d292749b7c1779ab773892b5f2fe22`) records one `write_stdin` on that same handle with `chars="\u0004"` and the complete terminal return: chunk `a5d2cb`, `exit_code=1`, `ValueError: Main start proof JSON frame`. The traceback reaches driver line 132, where JSON parsing of the awaited start frame fails. Static driver inspection confirms that the bootstrap line and stdin read precede all session identity, admission, ledger mutation, capture-child creation, and diagnostic execution. No valid start frame was sent; the EOT is not `ADMIT`. The exact native outer session and its local bootstrap process are terminal by the preserved same-handle tool result; no candidate012 B child was created. No self-reported PID was used to substitute for the opaque tool handle. This conclusion is bounded by the preserved raw tool receipts and driver flow, not a host-wide process census.

`main-launch-attempt-start-012.json` (SHA-256 `ad0e5d0f897da1a474233fda9fbeb60542046c691bdf93658551386e8ab5ee95`) has schema `plan049-main-launch-attempt-disposition/v1`, kind `diagnostic`, exact index 12, the actual/required justification mismatch, and seven evidence links whose current bytes all rehash correctly. Its embedded raw bootstrap return equals the preserved return exactly. It is outside the candidate's 45 output paths, and matches the driver's legacy high-water grammar `main-launch-(?:attempt-start|terminal)-(\d+)\.json`; the record's index/kind validate the parsed 012/diagnostic identity. Direct-directory inspection found no candidate012 identity request, admission, ledger, driver exec-start/log, attempt directory, or any other candidate012 driver output. The previous source/high-water was 11; the consumed marker now forces **12**, so index 12 must never be reused and index 13 is next. The record's no-test/no-payload/no-aggregate statements are supported by the pre-frame failure and vacant driver outputs; this review did not execute tests or claim unseen host-wide activity.

The preserved launch boundary002 (`7009c53b6cdd6458debf326b7c91f83c3d95823ea7befc8b979fe7b926cec51a`) and independent final review (`e32f5e25215252a03485a1c75742c08bcbd3a1acfbb899fa4e6c657fa42bc928`) authorized only the *different*, exact intent arguments; the actual call was outside that clearance. Current next action is to keep candidate012 closed, retain all raw evidence and the consumed marker, and prepare a fresh candidate013 preflight and independent boundary review before considering another bootstrap.

Checks used read-only `rg`, `sed`, `sha256sum`, Python standard-library JSON/SHA/regex/path comparisons, and direct-directory file enumeration. No project import, test, diagnostic, process/session contact, signal, admission, source edit, or launch occurred in this review. Reviewer CPU/wall time, peak memory, and host-wide process visibility were not measured.
