# Candidate013 pre-admission abort and attempt retirement review 001

**Verdict: PASS for bounded operational retirement and index consumption; session-proof/admission remains NEEDS_REVISION.** The saved exact Main handle **56200** returned terminal `exit_code=1` after a same-handle EOT, before `ADMIT` or child creation. Do not contact that terminal handle or fabricate a missing `chars` argument. Candidate013's local driver B and outer native H are released for *live operational* capacity, while the two-row ledger remains historical and must not be rewritten to claim a `session-terminal` event. Its identity request/ledger/event paths consume index **013**; the next high-water index is **014**, subject to fresh gates. This review grants no launch, admission, test, payload, or aggregate.

The preserved bootstrap return `candidate013-main-exec-start-001.json` is 1,758 bytes, SHA-256 `9a62cd6530f440532747f4a30ad428ee8c4b386b78e5084cb7107474f008197e`; it records the exact seven-token command, validator-required literal justification, chunk `817c7a`, and positive returned `session_id=56200` with the complete CRLF bootstrap line. `main-session-049-diagnostic-013-event-000.json` is 1,960 bytes, SHA-256 `98893bcd97511ff0c90438364195f535c3997a827b6f4a9d10b093a2b5e3bc53`, and exactly preserves its argument/result objects and UTC stamps. The boot line proposes `.job-ledger-diagnostic-049-013.{jsonl,lock}` and initial digest `7d8d0cca2b017c091b03de4dd37fc112ae06e17991b4632d7133d7639896fa9d`; no self-reported PID substitutes for the opaque handle.

The same-handle start-frame send receipt `candidate013-start-frame-send-001.json` is 4,852 bytes, SHA-256 `40821fef380f679329e187a3dc67b1cd0f7d03c9ee392cb1de1e78e9de34b25f`; event001 is 5,054 bytes, SHA-256 `52db89a87be6da965293b5eb8aa5a5edab38c6d6962a2dea0632ab810d277c5d`. Event001 exactly matches the saved send arguments/result and shows the driver echoed the start frame, then reported an `awaiting_main_admission` path and the 39,535-byte identity-request record, SHA-256 `39dd6008e2dd93375d77a990ecd6c7a55c8622bb6455e0be2589f6f20ebecf8b`. The identity request exists and consumes index013; its PID/ancestry fields are prospective local evidence, not proof of the Main tool handle.

The original first-poll receipt `candidate013-poll-001.json` is 409 bytes, SHA-256 `4a286e1559c41d517ff2e57c32f2a0551aadd5a972df3eac6c313b63a238ff36`; event002 is 619 bytes, SHA-256 `94e03d313f6bf194fa250aabeafea391956fba1c3ad22dbe097e2c74d0af4e70`. Its arguments/result/stamps exactly match the receipt, but its argument object **omits `chars`**. The frozen validator requires `type(args.get('chars')) is str` and, for this later poll, `args['chars']==''`; the empty returned output proves neither sent argument. The independent event002 review (`efac035a55d63de5abc13ab61698a4217b174edade8db4e6b5b7fb5c99dec94f`) correctly says NEEDS_REVISION. All three event outputs rehash to their recorded SHA-256, all returned handles are 56200, and the timestamps are ordered. No later poll or valid session proof/admission was sent.

`candidate013-abort-001.json` is 1,307 bytes, SHA-256 `f04ba4ca4d52cd5e625695efd779a177e987c895a17f9f1bc8d7e16dfbf1f0a0`: `write_stdin(session_id=56200, chars="\u0004")` returned chunk `cfd030`, terminal `exit_code=1`, and `ValueError: explicit Main admission handoff required` at the driver's exact `sys.stdin.readline()!='ADMIT\n'` gate. This proves the same retained Main native session ended; the driver had reached the admission wait but did not cross it. No admission record, launch note, driver exec-start/log, capture/runner child, diagnostic attempt directory, receipt, execution record, test, or aggregate exists for index013. The identity request lists 12 prospective output paths; every one is absent. The preserved tool result and driver gate, plus the ledger's lack of child reservations, support releasing candidate013's live B/H; the visible-process absence reported by Main's current census is corroborative and namespace-scoped, not independently re-censused in this no-contact review.

The `.job-ledger-diagnostic-049-013.jsonl` is 744 bytes, SHA-256 `57c5bf57652440a648e29f986a612ec09de7cb2bcf3a4916ab04e99337a5a4c7`. Its two canonical row hashes independently verify: sequence 0 `attempt-open` hash `df357a9f210b6eefff029c189071914fc7912df83902fd3da1eae3e9a9a13fbf`; sequence 1 `main-handle` hash `0eff406f7a67761e518088a78ea04ebf2d014b0f34b2e07cd300b9dddd6eb1d1`, linked to row 0 and handle 56200. The first-line byte hash equals the bootstrap `initial_sha256`. There are **no B child-root or descendant events**, no ledger terminal event, and the lock file is empty. Static driver source (SHA-256 `bdf68e0eaef6648d4097c5f819527fff94573bacc202176309c5832a915f59b8`) places capture/runner creation after the admission gate.

I reapplied the frozen driver's direct-directory filename/legacy-marker scanner without importing it. The current high-water is **13** from the exact identity, ledger, and event000–002 paths; next index is **14**. Candidate013 is consumed even though its driver/capture output paths and attempt directory remain vacant. The historical diagnostic006 and candidate009 native H uncertainties remain separate and unresolved; this review releases only candidate013's exact terminal operational charge.

Checks used read-only `rg`, `sed`, SHA-256/JSON/regex/path comparisons and static source inspection. No project import/test, process/session contact, signal, launch, admission, or source edit occurred. Reviewer CPU/wall time, peak memory, and host-wide live process visibility were not measured.
