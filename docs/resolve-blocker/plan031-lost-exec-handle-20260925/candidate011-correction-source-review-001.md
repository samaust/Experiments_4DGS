# Candidate011 Correction017 — independent exact-source review 001

**Verdict: NEEDS_REVISION.** Read-only review of the seven working-tree source diffs against adopted proposal SHA-256 `794ffe897c608bd05a9200be6e8fd73a8c63b8b5531f7c3dfec10f4d04a2a415`, plan review-003 SHA-256 `6b28016c2f35da98ef0a7575444204ee78851073973237924b873a47a4941e30`, and adoption record SHA-256 `dfedea92504f7117b7b20c22a7276351375f7d316cd433b1196062baab8bb985`. This does not clear focused tests, a diagnostic, admission, launch, or aggregate.

## Exact defect

The adopted ack-receipt requirement is to identify the named publisher-diagnostics encode **and record unrelated encodes separately**, with distinguishing negative controls. The edit at `tests/test_vipe_benchmark_s1_helper_fixtures.py:1757-1761` filters `p.encode` to `value is state.publisher.diagnostics` after the ack target, but the shared `bind()` wrapper at lines 1689-1703 immediately calls the original function for a nonselected encode without recording it. No new ack-specific negative assertion or separate unrelated-encode evidence was added in `tests/test_vipe_benchmark_supervisor.py`; its generic target/successor assertions remain. Therefore the source cannot establish that other `encode` calls were observed and correctly excluded, as the proposal requires. Within the same existing fixture method, record nonselected encode identity/stage separately and assert the named successor's object identity and causal order against those negative controls. Preserve the exact 114 named deadlines and existing before/equal/after assertions. Re-review the resulting exact bytes before any focused test.

## Other source findings

- `s1_helper_session.py:1843-1852` now compares the immutable dispatch to the fixed original Plan049 path/SHA and requires the note to bind both the dispatch file record and complete current Plan049 file record. It retains full status equality and the later `no_timeout_launch` validation. No historical dispatch/note rewrite appears in the working diff.
- `s1_progress.py:1555-1584` distinguishes a locally active traversal error from ambient `sys.exception()`. If iterator close fails, `close_attempted` remains true, `closed` remains false, and the enclosing `failure` keeps the owner unresolved; the existing final cleanup does not retry that iterator. The production change is confined to this cleanup path.
- `test_vipe_benchmark_supervisor.py:3721-3734` writes through to the original stream, records only paths under the fixture production root, and encodes accepted text with that stream's encoding before storing byte content. This is a static design check; byte fidelity and any text-stream position behavior still require focused verification.
- `test_vipe_benchmark_s1_helper_fixtures.py:17-21` publishes real `_job_creator()` and `_thread.get_ident()` identity before L03's unchanged 0.15-second sleep. Production `Owner.run` and handle binding remain in place. Plan046 timing/assertions are untouched and remain an open later diagnostic result.
- Driver, validator and positive recovery fixture each append exactly the same adoption record after the existing addenda (`launch-049-exec.py:296-297`, `s1_validation_contract.py:738-739`, recovery fixture lines 1089-1090). The additions change no admission schema, command, resource cap, or historical attempt artifact. The adoption record itself binds the exact proposal/review hash pair.

The working diff changes only seven adopted source paths. No `def test_`, assertion, named deadline declaration, original W/C boundary, setup/cleanup number, selector, thread/process creation route, or resource-limit expression is edited in that diff. This is a diff-level finding, not a full runtime count or acceptance proof. The stated 73 methods, 602 callbacks, 114 named deadlines, 42 L/P scenarios, six P outcomes, CPU-only serial `B+max(1,H)≤8`, 150 GiB artifact cap, 64 MiB memo cap, and 8,388,608-byte whole-ledger cap remain governing constraints. Existing Candidate011 failure evidence stays failed and immutable.

## Current exact hashes and resource accounting

SHA-256, in proposal order: `scripts/vipe_benchmark/s1_helper_session.py` `112e9ebba6199b68b2e090bb036891e50f3fc1194a3c48b5f7c33569e6b65612`; `scripts/vipe_benchmark/s1_progress.py` `8ea60934eabee61102617c0d0f1eed2109240f19d5d2641a4fe2f1f735f37bef`; `tests/test_vipe_benchmark_supervisor.py` `4ced7cf8afac61b9acbbcd1c906b63d2dcdad852f899b860361a2430bfb42e3f`; `tests/test_vipe_benchmark_s1_helper_fixtures.py` `a25fae73c3dfb26143161aafddf1ed6c994ba3feda16f551569e58377183e197`; `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py` `bdf68e0eaef6648d4097c5f819527fff94573bacc202176309c5832a915f59b8`; `scripts/vipe_benchmark/s1_validation_contract.py` `2f522e8636b486ad7141daef105cb3980f11dc03b971728b68c9ec2cf245d2c0`; `tests/test_vipe_benchmark_s1_recovery.py` `bb44070fe3e476187bbaa153e952d8fa02fe3a95822b80ece95b90b00fc63d18`.

Checks: `git status --short`, `git diff` on the seven paths, `rg`, `sed`, and `sha256sum` on named source/authority files. No project import, test, source edit, process/session contact, launch, or admission occurred. Review wall/CPU/thread/memory/storage use was not measured; live B/H and artifact capacity are unknown and require later fresh gates.
