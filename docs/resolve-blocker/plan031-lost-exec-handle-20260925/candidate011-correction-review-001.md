# Candidate011 correction proposal 001 — independent plan review

**Verdict: NEEDS_REVISION.** This is a static, read-only assessment of proposal SHA-256 `c89c28fdf8244d973bb549d2161f138ac05b5f42ddb217150919954fe3a83b4f`. It does not adopt Correction017, clear source edits or tests, or authorize a diagnostic, admission, or aggregate.

The proposed seven-file change scope and failure mapping are otherwise bounded and supported by the frozen Candidate011 audit/triage: (1) the owned-workload predicate conflates the immutable dispatch's original Plan049 digest with the adopted current digest; (2) the next-input text observer raises `TypeError` before observing the intended boundary; (3) the ack-receipt witness confuses an unrelated encode with its named successor; (4) runtime-guard cleanup loses uncertain iterator ownership after an unconfirmed close; and (5) L03 publishes native thread identity after its intentional block. The proposal correctly defers the Plan046 before-deadline result until the authority fix is rerun, retains that deadline and all assertions, and keeps Candidate011 failed historical evidence. Its proposed driver, validator, and recovery-fixture changes are limited to ordered future-authority propagation, with no intended session, admission, command, ledger, or scientific semantic change. The specified 73 methods, 602 callbacks, 114 named deadlines, 42 scenarios, six P outcomes, CPU-only serial execution, `B+max(1,H)≤8`, 150 GiB artifact cap, 64 MiB memo cap, and no operational time ceiling remain the governing limits.

Two exact corrections are required before a PASS or Main adoption:

1. The sentence requiring “both original and adopted identities in the launch note” is inconsistent with the current authority contract. `s1_helper_session.py:1846` erroneously searches the note bindings for the original dispatch Plan049 SHA. `s1_validation_contract.py:668-675` instead checks the **original identity inside the immutable dispatch** and the **adopted current Plan049 file record in the note bindings**; the note also binds the dispatch file record. `launch-049-exec.py:315-316` constructs that latter binding set. Specify this exact transitive/direct relationship, retaining the full dispatch, status, current-plan, and `no_timeout_launch` checks. Requiring a second direct original-plan note binding would change the note's authority graph and exceed the stated mechanical scope.
2. “One-entry ledger append ≤8,388,608 bytes” misstates the resource rule. `s1_helper_session.py:729-734` rejects an append when **existing raw ledger bytes plus the new line** exceed `JOB_LEDGER_LIMIT = 8 * 1024 * 1024`; the validator checks the total raw ledger size. State the hard **whole-ledger** cap of 8,388,608 bytes. A per-entry interpretation would permit an oversized ledger.

After those textual revisions, take a new proposal hash and independent plan review. If accepted, Main should fix the new Correction017 record's exact path and ordered position, record the proposal/review pair before any source edit, and propagate that immutable record consistently through the driver, validator, recovery fixture, and future launch authority. The final source reviewer must verify each named positive/negative control and unchanged declarations, timers, and assertions. Fresh attempt-index, output-vacancy, source/authority, storage, and B/H checks remain prerequisites; Candidate012 is only the earliest possible later index. No present launch clearance follows from this review.

## Exact inputs and checks

SHA-256: proposal `c89c28fdf8244d973bb549d2161f138ac05b5f42ddb217150919954fe3a83b4f`; static triage `4cea6a58f1ebf0cb261f1466d97362b99de258e054a7343fda8c5f930f7d477d`; post-admission audit `37c347c6028a41b2c995569653b0419513c97009af58228e4c1e65784a33fd0a`; Correction015 `a3242ed06131aa9ca00bc05180805c8d5bf5ea8d965919b92054154f10a00bab`; Plan049 `89d1f634ad2407b02558ff9c030d4a4f12f8f4ab010c93e2cf532876564afe7e`.

Seven scoped source hashes in proposal order: `s1_helper_session.py` `bb6ea896a73ab3bbdf98e4ac4a9e15edf769a4484995dae44e2d4fca3e34f44d`; `s1_progress.py` `d53a203c64c1b78a19a720ba72aaa28efbe427333c704f81166a4151e485b89d`; `test_vipe_benchmark_supervisor.py` `03e41c199a63c21cf754f0ec3f571a85f79ac417457344d83220f31399b9ce36`; `test_vipe_benchmark_s1_helper_fixtures.py` `faccb5561ad8373d6805b50b0326f0eff8d00e9c9d9760d5829096fca917732b`; `launch-049-exec.py` `e28b3874d1830c934fae624eee254a25d25517fcc22cb6fa41f4da5191d62af7`; `s1_validation_contract.py` `860a18d066b8b13999f1cf21b6b89c1a1e9003ce06e00e6f013dbd3fca782a55`; `test_vipe_benchmark_s1_recovery.py` `205a76a29bf43b38d440638f4123a76509ed5003d3f77ae121d62fb50ffe430b`.

Checks used `cat`, `rg`, `sed`, and `sha256sum` on the named files only. No project import, test, process/session contact, launch, admission, or source edit occurred. CPU/thread/memory and live B/H use for this review were not measured; the shell/hash commands completed, and no runtime resource claim is made.
