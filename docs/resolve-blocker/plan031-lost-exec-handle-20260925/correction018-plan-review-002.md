# Correction018 plan 001 — independent re-review

**Verdict: PASS for the exact bounded plan**, SHA-256 `0cd24b3982e148cd5c961ee5fd831a78ffafd9b1956abe5a91e744c1055dd396`. This plan review does not adopt Correction018, clear source edits or tests by itself, or authorize a diagnostic, admission, or aggregate.

The two review-001 findings are resolved. The production edit is confined to `s1_evidence.py::qualify_runtime`; its algorithm keeps the exact admitted invoked spelling, checks that a fresh strict resolution reaches the manifest's canonical executable path, uses the existing guarded no-follow reader on that target, compares the **complete** path/bytes/digest record, and strictly re-resolves the invoked spelling afterward under the original deadline gate. The plan expressly retains inventory and loaded-manifest bindings and does not relax generic evidence reads or accept another alias spelling. It is a narrow remedy for the real canonical-manifest versus symlinked-invocation ELOOP.

The test edit is confined to the existing `runtime_guard/before` portion of `tests/test_vipe_benchmark_supervisor.py`, with no new method, subtest or deadline case. It requires unpatched production `qualify_runtime` to accept the exact admitted symlink and reject (1) an alternate invoked spelling even to the same target, (2) a mismatched manifest record, and (3) target substitution between the two resolutions. The original `runtime_guard` before/equal/after checks and all 114 cases, W/equality rejection, 2-second work/1-second safety behavior, original assertions and cleanup remain fixed. Independent exact-source review must inspect the actual implementation and these controls before any fresh-accounted focused execution. A focused PASS is development evidence only.

Plan049 CPU-only serial scope, `B+max(1,H)≤8`, 150 GiB artifact cap, conservative historical H=2, immutable failed focused evidence, and the no-diagnostic/no-admission/no-aggregate boundary remain intact. Main must adopt this exact reviewed plan hash before editing. No further plan-level defect was found.

Inputs: revised plan SHA-256 `0cd24b3982e148cd5c961ee5fd831a78ffafd9b1956abe5a91e744c1055dd396`; independent focused005 triage `f8473c646f3a11a62500d24d7c3e95c758afdd352eeef4aa37d6a931efaee0ee`; focused005 stderr `3ce9aae7a669a754f0bb550d98c0bc7c586ccf5b180ca9937b6f2c193fabcde7`; terminal `46c39fd5d7056ce83c4483a028ca708f127b072097c6649c2fedb74f283d8c62`; frozen `s1_evidence.py` `14ea47c7cc56418ae7a8e0e7aa442fb7664774d8a8f15b27c558ddcf69c679db`; supervisor test `6a302f9793dab7350f95da6de80a77cc201304a42aaecdf1ba6c1735b88ae0e6`.

Read-only checks: `cat`, `rg`, `sed`, `sha256sum`. No project import/test, source edit, process/session contact, launch or admission. Reviewer CPU/memory/elapsed time, live B/H and artifact capacity were not measured.
