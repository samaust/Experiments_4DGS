# Correction018 — independent exact-source review 001

**Verdict: PASS for the current two-file Correction018 diff.** This is static source review only. It clears neither a test result nor a diagnostic, launch, admission or aggregate; focused execution still requires fresh source/authority, artifact, process/thread and output-path accounting.

The production edit is confined to `s1_evidence.py::qualify_runtime` (`s1_evidence.py:782-799`). It first preserves exact `manifest.interpreter.invoked_executable == request.runtime.python`, strictly resolves that exact request spelling through `operation(Path.resolve,...,strict=True)` under the inherited original work deadline, and requires the manifest executable's canonical `path` to match. It then compares the **complete** path/bytes/digest record returned by the existing no-follow `file_record` alias on the canonical target. A second strict, deadline-gated resolution must equal the first. The subsequent `strict_record` and inventory/version/invoked-spelling checks remain. The generic checked reader and other record APIs are unchanged; no arbitrary alias spelling or target bytes are accepted by this branch.

The bounded test edit is inside the existing `runtime_guard/before` branch of `test_vipe_benchmark_supervisor.py`. It calls unpatched production `e.qualify_runtime` and checks three rejects: a different symlink spelling to the same target, a changed manifest executable digest, and a substituted second resolution. The latter also asserts exactly two resolution calls. The real admitted symlink then proceeds through the original positive action; equal/after keep their unchanged W rejection and the method keeps its 114 declared cases. The previously noticed `observing[0]` accesses before initialization were removed from current bytes; instrumentation is constructed later and these controls do not require that flag. No new method, subtest, deadline, or resource-limit edit is present.

The adoption record SHA-256 `0c3aa6a94b1f3f1730acfc47e65c96fb0474e7bf7f606fe39888731926457cbf` binds plan SHA-256 `0cd24b3982e148cd5c961ee5fd831a78ffafd9b1956abe5a91e744c1055dd396` and independent PASS review SHA-256 `9eb80ab8a4011e6be171303b09896f187be703df5b731e0d6afd237fba39c2dc`. Current source SHA-256: `scripts/vipe_benchmark/s1_evidence.py` `b46124282c5aae4d5a28b43b5b4440bfe6ec7868f66a39cceda1ed8be717b27c`; `tests/test_vipe_benchmark_supervisor.py` `86aa7cc3235f8b35b87008ab5078fd495d166bf0e8de4902963f7bc2155656f3`. All other earlier Correction017 source portions are outside this two-file review and retain their separate source reviews. Historical focused005 remains failed.

Checks used read-only `git diff`, `rg`, `sed`, `nl`, and `sha256sum`. No import, test, source edit, process/session contact, launch or admission. Reviewer CPU, memory, elapsed time, live B/H and artifact capacity were not measured; no runtime outcome is claimed.
