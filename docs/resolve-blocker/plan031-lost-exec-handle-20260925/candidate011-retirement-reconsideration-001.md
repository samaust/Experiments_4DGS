# Candidate011 retirement reconsideration 001

**Verdict: PASS for candidate011's bounded operational local process-tree and outer-session retirement.** This separately reconsiders the conservative retirement limitation in `candidate011-post-admission-audit-001.md` without changing that sealed report. It does not convert the failed diagnostic to a pass, rewrite the ledger, or clear any historical unknown native session.

The admitted capture PID/PGID 10687 created one exact B child, runner PID/PGID 13352, through its explicit reserve/bind route. `execution.json` records its matching `Popen.wait` result 1, `wait_completed=true`, `creation.retired.complete=true`, and `absent_after=true`; ledger row 210 records the same B token `eb59857b3f0a4701b2a8acb30af47a0e` retired by matching child wait. Main's exact session handle 49924 returned terminal exit code 1. Static examination of the selected diagnostic's actual process-creation paths found the supervisor worker, helper `Owner.run` role, fixture descendant, direct script, L18 sentinel and other fixture launches call `create_owned_process`; that gate durably reserves B before its creation syscall. The capture's own runner launch is separately reserved before `Popen`. The 211-row hash-valid ledger has no other B reservation or descendant event. The repeated `owned dispatch/plan/status binding` failure occurs in the fresh ownership gate before helper process creation, explaining the failed scenario cleanup flags without showing an unregistered child. No evidence supports a live detached child from this route.

Four unbound `Session owner thread` H reservations (rows 5, 16, 177, 178) were made by runner PID 13352. Their source callsite reserves H immediately before `_thread.start_joinable_thread`; they may represent started or non-started threads, but any started thread is confined to PID 13352 and cannot outlive its exact terminal wait. The ledger has no `session-terminal` event and retains these four reservations as unresolved historical rows; they must not be relabeled as ledger-retired or edited. For *live operational* capacity, candidate011's local B, co-resident H threads, capture process group, and outer native H are retired by the matching runner wait and exact Main terminal return. A read-only three-entry current `/proc` namespace showed no PID/PGID 10687 or 13352, corroborating but not independently proving host-wide absence. The user's process-group-retirement assertion is consistent with this case-specific chain. The 05:08:02Z reboot predates candidate011 and is not used as its retirement proof.

The older diagnostic006 and candidate009 native H=1 charges remain separately unresolved; their pre-reboot local B trees had distinct adopted retirement evidence. Candidate011 index/output paths stay consumed. Any future work still needs source correction, exact source/authority review, fresh process/thread/capacity/artifact/path checks and independent launch readiness; this finding grants no test, diagnostic, aggregate or admission.

Evidence SHA-256: prior sealed audit `37c347c6028a41b2c995569653b0419513c97009af58228e4c1e65784a33fd0a`; execution `bba8e5fded7b50e7dfad318b3a76c39441e3f6b89306942c969f00a6a4e5a5c4`; ledger `d2355c03799a06a95a07b6d4bdf84268a18a78440971c16dddd6a89008719121`; Main terminal event `725cd841181008fc55aed6254daa75f8b958e6076c20a93e29169c483562ed44`; frozen creation-gate source `s1_helper_session.py` `bb6ea896a73ab3bbdf98e4ac4a9e15edf769a4484995dae44e2d4fca3e34f44d`; capture `s1_validation_capture.py` is bound among execution's unchanged 78 source members. This review used read-only source searches, ledger/receipt inspection, and a scoped `/proc` read. No project import/test, process/session contact, launch or source edit occurred. Reviewer CPU/wall time, token overhead, and host-wide process census were not measured.
