# Independent Plan053/Addendum021 C1 source-closure audit 007

**Verdict: PASS for the fresh, read-only C1 gate on the exact pre-edit source baseline.** The selected source/import, path and process/thread creator graph is closed for the fixed Plan049 runner, diagnostic focus and six direct-script selectors described below. This PASS permits Main to proceed only with the previously authorized **narrow implementation in nine existing paths**, subject to the adopted Plan053/Addendum021 sequence. It is not a declaration that missing guards exist, a C2/C3 numerical or event-detail proof, an exact final-source review, a focused-test clearance, an admission, a diagnostic clearance or an aggregate clearance. Any selected source/input drift or newly found creator/path reopens the applicable review.

The adopted Addendum021 changes the pre-edit gate deliberately: it removes the prediction-only cumulative ledger sum, universal future-input command/environment byte maxima, and demand for the *finished* event table before edits. Actual descriptor/environment/host caps, complete strict events, exact source review and all runtime gates remain. I therefore audited **C1 alone**; I neither recycled Audit006's FAIL as a PASS nor reinstated its superseded C2/C3 prediction requirements. Current missing high-water, renderer, environment, strict event and handle guards are mandatory implementation and distinct post-edit review criteria, not C1 failures.

## Exact input and adoption binding

Fresh SHA-256 reads matched `plans/plan_053.md` `33f7a8a1d292dade248295a3594b0d4bdff451f1685ad35bc104065be7178252`; `addendum021-proposal.md` `935e4aedcf7481ac8e9418ca8eaef3729f2604822dcaf6c005dfa43a6474b0b5`; the full verbatim user-direction note `authorization-049-measured-ledger-001.md` `d673898b474818c578ed7d8d0d2ccb934e5471502dd7a2a951fd13dd11a13d51`; and independent plan review `addendum021-independent-plan-review.md` `43bf2f813840aa10c21c3fb13a42f0d5417a4f434f5c176e253860bb033d3f65`. `addendum021-inputs.sha256` is `0df1347af8d0a88ce51b4371a7227407d80810b4e731f51d8f6884aed5c82b9e`; 19/19 immutable rows matched. Its two expressly mutable status rows had advanced, so I freshly rebound them at this read: Plan049 `docs/resolve-blocker/plan031-progress-20260922/status.md` `bd26dd9911825b461b255cdfefe0aed8b83fe2ab371c592cd5dd67e0b662368d`, and this resolver's `status.md` `af46c6ffab58f054a87e0292c7dac6fde795f69a8c5d6358c9f0f4efda6d158d`. Both record Main's 2026-09-24T20:27:37Z adoption of the exact Plan053/Addendum021/review/user-note hashes under the user's standing autonomous-choice instruction. The mutable old snapshot hashes are historical, not failed immutable inputs.

The same fresh snapshot matched Plan049 `89d1f634ad2407b02558ff9c030d4a4f12f8f4ab010c93e2cf532876564afe7e`, Correction015 `a3242ed06131aa9ca00bc05180805c8d5bf5ea8d965919b92054154f10a00bab`, adopted Addenda011/013/014/015/016 respectively `18e0d807a074fdb11817251b459db54ea3b0725161222da824b86c5ebfe52d65`, `14398e0974258104018a366f5d9a9d653e69c9efb894baefe73b498542b1c78c`, `0d630af7c3da1274efc78fb30d859212d3cedbf851bbeabc814d0865ccb82ea1`, `f679493e758957003c6af14668a15168d8b3a5f2c6357c972a1b252f6401ad5a`, `0ac62d42a7a35d7c2e4ab93750b6c7e670e037e11ef0bb219fc7b88100bc946d`, and the selected session-proof authorization `6ee2d0f899c6ee20de96415079d9aa07900b51f2321ef37d20af5e7e171bd755`. Earlier Addenda004/005 are rejected history, 007-010/012 unadopted historical proposals. Addendum019/Plan052 were adopted but their retained *prediction-stage* C2/event-table gates are narrowly superseded by Addendum021. Audit005/006 remain immutable FAIL under their then-operative gates. A hash match on a proposal never creates adoption by itself.

The exact [79-row selected-source manifest](addendum018-selected-source-sha256.txt), SHA-256 `e3cd82db4cb127005761346bc589ff0706e38c1db906b3efbc29feac32f06478`, matched **79/79 current files**: the driver's pre-edit SHA-256 is `be9cdd762b866204eb77aa58e18ca8ed4e48b68df625313fda52610caa90bc1e`, and `s1_validation_contract.py:32-36` expands to exactly **78 unique existing source members**. The older conservative transitive project-import appendix in Audit004 independently rehashed **69/69** current `.py` rows with no mismatch; its reach classification is conservative, not an assertion every import-reachable definition executes. A stdlib AST read of the nine fixed test modules recomputed the preserved collected totals: `5+41+35+15+3+10+31+101+8=249` methods and `6+372+24+8+4+0+0+614+0=1,028` literal typed callbacks. The diagnostic `HelperSessionTests` class alone contributes 73 methods/602 declared callbacks. These are static collection counts, not runtime success evidence.

## Selection and exact creator closure

The selected entry is the contract's `ARGV=['.local/envs/stg-colmap/bin/python','-B','-']` and 200-byte stdin SHA-256 `4a05322d55f9a6e13343c93e971be5b202b242d296453f7d80005db8533b8fd4`, which calls `runpy.run_module('vipe_benchmark.s1_validation_runner',run_name='__main__',alter_sys=True,init_globals={'STDIN_PYTHON_ARGV':tuple(sys.argv)})`. Runner `s1_validation_runner.py:105-123` discovers, in exact order, `s1_semantics,s1_recovery,backends,contracts,component_recovery,execution,budgets,supervisor,review_annotations`; diagnostic focus discovers only `supervisor.HelperSessionTests`. It patches both selected GPU-reading entrypoints before tests. The six **atomic** direct-script pairs in `test_vipe_benchmark_s1_recovery.py:2300-2311` are, in order:

| ID | Suite / exact selector |
| --- | --- |
| D1 | `s1_semantics` / `S1SemanticsTests.test_contract_rejects_missing_or_tampered_assignment` |
| D2 | `s1_recovery` / `ReceiptContractTests.test_typed_primitive_callbacks` |
| D3 | `backends` / `AssetAndDetectorTests.test_phrase_ambiguity_and_capacity_fail_before_casting` |
| D4 | `contracts` / `AccessTests.test_heldout_final_window_wrong_branch_and_pair_rejected` |
| D5 | `component_recovery` / `ComponentRecoveryTests.test_scope_validation_and_cumulative_cap_are_not_relaxed` |
| D6 | `supervisor` / `HelperIntegrationTests.test_cleanup_failures` |

Each D row selects `ROOT/tests/test_vipe_benchmark_{suite}.py`, its paired selector and `-v` under the existing `direct_script_launch`; no suite/selector can be substituted independently. D6's method patches Session with a fake class in its `self.call()` loop; this is a real direct-script process, but that patched loop is not proof of additional real Owner worker children. Other focus/aggregate Session callsites and nested children below remain selected independently.

I statically scanned all 79 listed Python source files for `Popen`, `posix_spawn`, `execve`, `subprocess.run/check_output`, `Thread`, joinable-thread start, fork/spawn and the project create wrappers, then read the selected enclosing callsites and fixed runner/test branches. The **selected real creation graph** is:

| Edge | Exact source callsite/route | Create and group/session disposition |
| --- | --- | --- |
| Main H to driver | Plan049 exact Main tool command; driver `launch-049-exec.py:53-78` binds returned handle | Existing outer H session; no guessed handle or added helper. Main tool command is a separate owner edge, not driver `execve`. |
| Driver replacement | driver `:129-133,:265` | `os.execve` to `-B -m vipe_benchmark.s1_validation_capture`; replacement preserves process/group/session rather than creating an extra PID. |
| Capture to stdin runner | `s1_validation_capture.py:58-68` | One selected `subprocess.Popen(ARGV,cwd=ROOT,env=...,start_new_session=True)` with retained matching handle/wait. |
| Owner worker roles | `s1_helper_session.py:1011-1014,:1031-1055`; fixture override `test_vipe_benchmark_s1_helper_fixtures.py:22-28` | Existing `work|sample` role loop uses common `create_owned_process:718-739` and `os.posix_spawn(...,setsid=True)`, with production `-m vipe_benchmark.s1_cpu_helper` or fixture `-m test_vipe_benchmark_s1_helper_fixtures`. |
| Fixture-mode child | helper fixtures `:103-108` | Existing work-role descendant `os.posix_spawn` of fixed `-B -c 'import time; time.sleep(10)'`, with fd-close action, through common ownership gate. |
| Supervisor worker | `supervisor.py:411-447`, selected `SupervisorTests.run_worker:28-39` and HelperIntegration/Session paths | Existing `create_owned_process(...,subprocess.Popen,command,...,start_new_session=True)`; `command(code)` source routes include fixed completion/cache/timeout literals and nested variants. Stop/cleanup remains the same `supervisor.stop_group` path. |
| Probe and fixture wrappers | `test_vipe_benchmark_supervisor.py:198-211,:856-865,:895-903,:1569-1580`; `test_vipe_benchmark_s1_helper_fixtures.py:879-906` | Existing race worker, `-B -m vipe_benchmark.s1_validation_runner --helper-probe`, helper-test `-c`, and scenario sentinel `-c` use `fixture_process_launch`/common ownership gate. Scenario/direct-script Popen uses `start_new_session=True` where source specifies it; inherited options remain explicit absence, not an invented flag. |
| L18, nested, L35 | helper fixtures `:605-617,:899-907`; supervisor tests `:87,:1042-1043,:1655-1692` | Existing L18 `posix_spawn(...,setsid=True)` sentinel; only `nested_worker_code(20,False)` and `(10,True)` embed fixed `scripts`/`tests` paths and create a nested `-c` descendant via fixture wrapper; L35's retained temp `ready`/`release` paths coordinate its existing detached `-c` descendant and pidfd witness. No new creator is implied. |
| Six direct scripts | recovery test `:1983-2010` and helper fixtures `:588-603` | Exactly six selected iterations of existing `[sys.executable,'-B',test_path,selector,'-v']`, `cwd=ROOT`, `start_new_session=True`, under `direct_script_launch`/common gate. Child code may reach its own already listed selected wrappers; each is independently accounted. |
| Saved-original-Popen seams | recovery test `:394-414`; supervisor test `:1104-1112` | These patches retain original `subprocess.Popen` and actually create respectively `-c pass` and `-c 'import time; time.sleep(10)'` under the selected supervised work path. They are real selected OS calls and must be mediated in the nine-path implementation; the patch does not erase them. |
| Threads | `s1_helper_session.py:1249-1256`; budgets test `:342-379`; L36 supervisor test `:1694-1709` | One native joinable Session owner per selected Session, two transfer `threading.Thread` starts plus one lock-helper start in the fixed budgets cases. L36 patches the native start to call the **original** before raising an ambiguous-start error, so its created owner thread is selected and remains charged. These threads have no invented process argv/environment vector. |

The scan also found `subprocess.run/check_output` in `basketball_vipe_benchmark.py`, `vipe_benchmark/execution.py`, `runtime.py`, `proxy_diagnostic.py`, `test_vipe_benchmark_annotations.py` and `test_vipe_benchmark_runtime.py`; `Thread` in `transfer_proxy.py` and its separate unselected test; and `supervisor.gpu_reading`'s `nvidia-smi`. They are **source/import hits, not additional selected OS creates** under this fixed runner: setup/qualification/main CLI routes are not selected; the selected `test_vipe_benchmark_execution.py:90-95` qualification call patches `_qualify_existing` before its subprocesses; annotations/runtime/transfer-proxy test modules are outside the nine discovered suites and six D selectors; the runner patches supervisor/execution GPU-reading functions. Existing pure creation-control seams use patched `Popen`/`posix_spawn`/fake syscalls, including helper-fixtures `:623-705,:911-991` and supervisor `:2824,:3074-3079,:3470-3474`; `fixture_process_launch(['pure'])` at fixtures `:967` occurs with patched Popen. The in-process `exec(compile(...))` driver/readiness slices in selected recovery/supervisor tests use mocked OS creators and are not extra process launches. This classification does not assert OS-wide containment or that imported native/third-party code cannot independently create a process; any new selected project-code creator revealed by implementation or exact-source review reopens C1.

## Eight typed path domains and exact direct-`R` grammar

Let `R=ROOT/docs/resolve-blocker/plan031-progress-20260922` and `A=R/{kind}-049-{index:03d}`, where minimum width three never truncates a larger positive index. Slot type follows exact producer, canonical parent and phase, never an overlapping physical prefix:

| Domain | Selected slot/provenance |
| --- | --- |
| `attempt_root_path` | Exactly `A`, formed by driver `:109` and supplied as capture positional output directory at `s1_validation_capture.py:24-29`. |
| `attempt_path` | Strict `A` descendants: capture copies `stdin.py,runner.py,capture.py`; process/stdout/stderr logs; `receipt.json,execution.json` and selected attempt-owned output records. |
| `repository_path` | Exact fixed interpreter, module/test files, direct-script selector paths and `sys.path` entries beneath resolved ROOT; includes nested worker's `ROOT/scripts` and `ROOT/tests` and L35's test-module path. It cannot name `A` or its descendants. |
| `fixture_temp_path` | A retained `TemporaryDirectory` owner/root or canonical descendant: `SupervisorTests.setUp` retains `self.temporary` for `self.root/'output'`; race/scenario fixture roots are retained at their selected test callsites; L35's context owner retains its concrete root for `ready`/`release`; helper fixture's `dir=root` case retains its parent/owner. The actual random suffix and effective base are bound at use, not guessed from `/tmp`. |
| `run_evidence_path` | Exact canonical direct-`R` current or read-only historical evidence names below, including driver prefix/logs and ledger `.jsonl/.lock` that Audit003 incorrectly placed beneath `A`. |
| `run_status_path` | Exactly `R/status.md`, written only by Main; no child command/environment slot. |
| `run_readonly_input_path` | Addendum015's 34 fixed Table 1 direct-`R` names/hashes, the Addendum016 `authorization-049-session-proof-001.md` row, and separately adopted self-references; read-only hash-bound role does not make a historical proposal operative. Fresh 34/34 original row hashes and the added row matched. |
| `repository_authority_path` | Exactly ROOT `AGENTS.md` and `plans/plan_049.md`, each fixed-path/hash read-only authority, distinct from executable `repository_path`. |

Current direct-`R` `run_evidence_path` names are the three `launch-{identity,admission,note}-049-{kind}-{index}.json`; `driver-049-{kind}-{index}-{exec-start.json,stdout.log,stderr.log}`; `.job-ledger-{kind}-049-{index}.{jsonl,lock}`; `main-session-proof-049-{kind}-{index}.json`; `main-session-049-{kind}-{index}-{event,send,poll,terminal}-{sequence}.json`; the **eight** typed `main-launch-attempt-{start,terminal}`, `main-launch-{start,terminal}`, `main-attempt-{start,terminal}`, `main-{start,terminal}` forms with `-049-{kind}-{index}.json`; `main-admission-preparation`, `main-launch-preparation`, `main-process-preflight`, `main-preflight-...-final`, `main-launch-state`, `main-cleanup`; diagnostic-only `main-diagnostic-outcome` and `main-diagnostic-timestamp-correction`; and new `main-storage-049-{kind}-{index}.json`. Here `kind` is exactly `diagnostic|aggregate`, index is positive decimal minimum width three, and session sequence is Main's exact nonnegative ordinal. Each family is phase-specific and requires canonical direct parent `R`, exclusive current-output vacancy and later exact-file readback. The only **two** selected legacy untyped scan names at current driver `:94-101` are `main-launch-attempt-start-{legacy_index}.json` and `main-launch-terminal-{legacy_index}.json`, opened read-only for narrow historical kind/index inference. Existing kindless `main-storage-049-{index}.json` is *read-only diagnostic history*; new storage is kind-qualified. Direct-`R` fixed input/status names and evidence names remain disjoint by exact basename/phase. Addendum021 is outside `R`; its adoption hash is authority context, not an automatic Table 1 read slot. If source later reads it, exact reviewed typed path/hash binding is required.

Read-only direct-`R` enumeration under that grammar found diagnostic indices 001-007, no aggregate current family and no diagnostic008 name. Diagnostic007 has only launch identity, launch preparation/state, process preflight and final preflight records. The two legacy untyped index001 names indicate diagnostic kind/index through their recorded fields/driver command. Thus diagnostic008 is an **observed uncleared candidate only**. Current driver `launch-049-exec.py:58` creates its sidecar before its incomplete `:80-102` high-water scan, which omits session send/poll/terminal, preparation/preflight/launch-state/cleanup/diagnostic/storage families. This is a required implementation correction, not a path/source-closure unknown or present gate. The future driver must scan all current/legacy families and check typed content, canonical parent, kind/phase and output vacancy *read-only* before its first mutation; the exclusive ledger sidecar is then first, fsynced/read back before a B child/admission. Current `:64-78` and contract `:521` positive-only Main-handle checks likewise need the exact returned positive at-most-16,384-digit guard while H stays charged on failure and Main retires through the same returned handle.

## Scope opened and later gates

This C1 PASS opens **only** implementation in these existing paths and inherited narrow portions: `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py` (first-mutation/high-water, authority/handle and selected exec binding); `scripts/vipe_benchmark/s1_validation_capture.py` (existing capture B reservation/Popen/wait and legacy timed-mode preservation), `s1_validation_contract.py` (versioned exact ledger/sidecar/authority checks), `s1_helper_session.py` (locked root/descendant/owner/operation receipts and L35 path), `supervisor.py` (existing `stop_group` and integration); `tests/test_vipe_benchmark_s1_helper_fixtures.py` (existing named helper/cleanup controls), `test_vipe_benchmark_supervisor.py` (existing L35 and two named nested-descendant tests plus bounded relevant ownership assertions), `test_vipe_benchmark_s1_recovery.py` (existing `ReceiptContractTests.test_execution_mutations` synthetic positive graph and mutation controls), and `test_vipe_benchmark_budgets.py` (existing named thread controls). Addenda001/006's method/portion restrictions survive Addendum021's nine-path list; this is not blanket permission to rewrite any path. If source work needs a tenth path, added method/callback/process/thread/sidecar, changed selector/suite/scenario or weakened assertion/deadline/order/error precedence, stop for a separate reviewed authorization.

Implementation must materialize the actual source-keyed typed command descriptor and complete immutable final POSIX byte environment for **every selected real create** above, validate the actual `<=16,384` descriptor, `<=1,048,576` environment bytes/`<=4,096` entries, NUL-inclusive per-string and whole-vector fresh host limits before reservation, then compare the same bytes and fresh limits after durable reservation immediately before the single OS create. It must implement complete strict required events, session terminal, one-shot process-incarnation ownership and Addendum021's actual `S+L<=8,388,608` committed-prefix/short-write/uncertain-tail protocol and exact permitted Main terminal cap report, with no silent data loss. These are **post-C1 implementation tasks** and must be verified by focused checks and a distinct independent exact-source and post-edit hash/field-preservation review before any diagnostic. Main then needs fresh source/artifact/ownership/capacity/output/high-water/session preflight and separate serial CPU-only diagnostic clearance; an accepted diagnostic and new clearance precede aggregate. Preserve 78/249/1,028, all original W/C/equality-is-late/protocol/scenario deadlines and assertions, exact six selectors/nine-suite order, `B+max(1,H)<=8`, 150 GiB artifacts, one <=64 MiB memo, hard ledger/descriptor/environment caps, exact Main handle, no operational time/attempt ceiling and no GPU/model/production/real-ledger/setup/download/scientific work. Diagnostic005 is failed,006 lost/unused,007 retired,008 uncleared.

Method: read-only `rg`/numbered static reads, SHA-256 rehash of all 79 manifest rows and all 21 input-snapshot rows, fresh 34-row Table 1 checking, stdlib AST scans of selected/imported source and literal test declarations, and narrow direct-`R` filename/legacy-content parsing. No project import/execution, test, fixture, live process/pidfd inspection, admission, diagnostic, launch, aggregate, source/status/authority edit, or `prompts` read occurred. The only write is this independent Audit007 artifact. Its exact whole-file SHA-256 is reported externally after final write.
