# Independent exact-byte plan review — Plan053/Addendum021

**Verdict: PASS for the exact plan wording only.** The complete user direction supports a practical implement/use/inspect/review cycle in place of prediction-only sizing gates. Plan053/Addendum021 state the broadened interpretation explicitly: remove the pre-edit cumulative ledger sum, universal numerical maxima for not-yet-materialized command/environment values, and a finished pre-edit event table; retain actual hard caps, complete evidence, selected source/path/creator closure and independent post-edit verification. This is a consequential gate change, not a finding that Audit006 passed. This review grants no source edit, test, admission, diagnostic, aggregate or launch clearance.

## Exact evidence

I rehashed `plans/plan_053.md` to `33f7a8a1d292dade248295a3594b0d4bdff451f1685ad35bc104065be7178252`, `addendum021-proposal.md` to `935e4aedcf7481ac8e9418ca8eaef3729f2604822dcaf6c005dfa43a6474b0b5`, the corrected user-direction note to `d673898b474818c578ed7d8d0d2ccb934e5471502dd7a2a951fd13dd11a13d51`, and `addendum021-inputs.sha256` to `0df1347af8d0a88ce51b4371a7227407d80810b4e731f51d8f6884aed5c82b9e`. The manifest has **21 rows; all 21/21 match current bytes**, including both mutable status snapshots at this review. The selected-source manifest is `e3cd82db4cb127005761346bc589ff0706e38c1db906b3efbc29feac32f06478`; **79/79 source rows match**. These are review-time snapshots; Main must rehash mutable statuses and affected sources at adoption and later gates.

The corrected note and both revised proposals reproduce the full user message, including its opening proposal, the repeated proposal sentence, “in unbounded,” and the final sentence about the practical approach taking less time. The user requested a specification change and an implementation/measurement cycle. Extending that direction from the whole-ledger estimate to prediction-only future command/environment maxima and pre-edit event-layout completion is a reasonable **stated interpretation**, provided the actual limits and complete evidence remain enforced. Plan053/Addendum021 satisfy that condition and require independent exact-byte review before Main may adopt the interpretation under the user's standing autonomous-choice instruction. They do not treat unbounded input as permission for an over-cap create or append, nor promise that every run will complete under a finite ledger cap.

Adopted Plan052/Addendum019 used the narrower interpretation and remain historical authority until a new adoption. Addendum019's citation of the earlier incomplete user-note SHA-256 `6df04560fcb84ea2dad46768a4db3531fcbc08ef28316513ae36a7de4e78340d` records what that prior proposal reviewed; it must **not** be silently rewritten or represented as the corrected note. Addendum021 binds the corrected `d673898b...` note and explicitly supersedes only the named pre-edit gates after adoption. Audit006 remains an immutable FAIL under the former C2/C3 gates; its prospective C1 inventory and source matrix are useful history, not current edit clearance. Unreviewed Addendum020 is only a candidate descriptor grammar unless separately reviewed/adopted; an alternative final grammar requires distinct source review.

## Gate and technical review

The pre-edit source gate remains substantial: a **fresh independent, read-only, exact-hash** closure must enumerate selected source/import/callsites, eight typed path domains and direct-`R` current/legacy grammar, each process/thread and nested/L35 creator, nine suites, focus and six atomic direct-script selectors. It must reject an unclosed path/import/creator or unauthorized tenth edit path. The earlier 79-row match does not by itself pass that new C1 gate. Numerical template and future environment maxima and the finished event table are implementation obligations, not facts this review declares complete.

Before **each actual OS create**, the proposed implementation renders a source-keyed typed descriptor, materializes the complete final POSIX byte environment in one immutable mapping, compares executable separately from `argv[0]`, full argv/inline code or final-hash-bound module, cwd/options and path provenance, and checks the actual 16,384-byte descriptor, 1,048,576-byte/4,096-entry environment, NUL-inclusive per-string limit and full vector with documented host overhead. It does this **before reservation**; after a durable reservation it rechecks identical bytes and fresh host limits immediately before one OS call. The driver must complete fresh read-only high-water, candidate/vacancy, authority, source, host and exact positive ≤16,384-digit Main-handle checks before its first filesystem mutation; the exclusive sidecar is first, with bootstrap fsync/readback before B/admission. Failure retains the exact H/uncertain B charge and same-handle retirement rule. The current driver performs none of that ordering yet; it cannot be used as passing runtime evidence.

The hard ledger rule is technically reviewable without a theoretical lifetime sum. Under the exclusive lock, verify the canonical hash-chained committed prefix of actual length `S`, render a whole strict-schema line of length `L`, reject `S+L>8,388,608` **before append**, allow equality, and count a row committed only after one write, fsync and exact `S+L` readback/chain verification. A short/ambiguous write is not a committed event. A verified rollback to the last prefix still stops the attempt; failed rollback/readback preserves the file, stops append/admission/retirement and reports verified facts while marking unverified physical tail facts unknown. A cap hit or uncertain tail fails acceptance. The permitted Main terminal family, only at its terminal phase with exclusive vacancy/readback, or the live transcript if durable reporting fails, carries cap/error facts without inventing a sidecar or an over-cap row. The finished conditional terminal schema and Main writer binding must be reviewed before diagnostic clearance. No required row, identity, failure receipt, census fact or command descriptor may be silently truncated or dropped; any field removal/digest substitution needs a field-by-field necessity proof and distinct review.

The finished closed versioned event table, exact selected emitters, required/optional keys, bounded types/enums, operation keys, one-shot wait/signal/pidfd/replay/uncertainty transitions and field-preservation map are mandatory **post-edit exact-source review evidence before diagnostics**. Current `_job_state` accepts arbitrary extra nested `data`, has no selected `session-terminal` emitter, and current append lacks the specified short-write/uncertain-tail protocol. Those are implementation defects, not grounds to claim the proposed design is already running. Focused boundary/fault checks follow implementation within separately cleared authorized scope; distinct independent source/hash review follows those checks and precedes any diagnostic. Tests do not replace that review. Main then requires fresh source/artifact/ownership/capacity/output/high-water/session preflight and separate serial CPU-only diagnostic clearance; a later aggregate needs its own clearance and accepted diagnostic evidence. Observed byte/content inspection after each stopped or completed run is measurement, not a universal future-size proof.

The nine editable paths and their inherited narrow portions remain exact: `launch-049-exec.py`; `s1_validation_capture.py`, `s1_validation_contract.py`, `s1_helper_session.py`, `supervisor.py`; and the four helper-fixture, supervisor, S1-recovery and budgets test files named in Addendum021. The proposal retains 78 source members, 249 methods, 1,028 callbacks, all original W/C/equality-is-late and protocol/scenario deadlines, assertions/order/error precedence, nine-suite/six-selector scope, serial CPU-only `B+max(1,H)≤8`, 150 GiB artifacts, one ≤64 MiB memo, all command/environment/host caps, the 8,388,608-byte ledger cap and exact Main/session/ownership gates. It adds no operational time, attempt or correction ceiling. Diagnostic005 remains failed,006 lost/unused,007 retired and008 uncleared. Other independent Plan049 session-proof blockers are not resolved by this plan review.

**Recommended next action:** Main may record its choice under the standing autonomous-choice instruction, binding these exact proposal/review/user-note hashes and the broadened interpretation, then commission the fresh independent C1 closure. Only its PASS can open the narrow implementation stage. Any missing C1 source/path/creator, failed exact-source review, unavailable terminal/cap report binding, changed hard cap/behavior/scope, or failed fresh preflight stops the dependent stage. The implementation-first relaxation is limited to the expressly named prediction/design gates; it is not a waiver of actual-input checks or complete evidence.

This review used local documentation/static-source reads and SHA-256 checks only. It performed no project import, test, fixture, process/pidfd inspection, source edit, admission, diagnostic, launch, aggregate or `prompts` read. No operational time/token ceiling was imposed; unmeasured CPU and model-call usage remain unknown.
