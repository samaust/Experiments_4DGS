# Addendum021 proposal — actual-input validation and measured evidence

**Unreviewed, non-authoritative plan text.** The user's complete practical direction, as supplied to Main, is:

> I propose a change in the specifications.
>
> Trying to exactly estimate the size is a waste of time because input data in unbounded.
>
> I propose a change in the specifications. Make a reasonable attempt at optimizing the implementation plan to minimize size while retaining enough valuable data, implement, use, inspect the size used, review if changes are recommended or not.
>
> This practical approach will take less time to plan and implement than a purely theoretical approach.

It is recorded verbatim in the corrected [authorization-049-measured-ledger-001](authorization-049-measured-ledger-001.md), SHA-256 `d673898b474818c578ed7d8d0d2ccb934e5471502dd7a2a951fd13dd11a13d51`. Plan052/Addendum019 adopted the narrow ledger-sum interpretation; subsequent Audit006 FAIL (`842cfcc9f7ff9e540183869a6ec1564b2d98ec2ec1a5c5f9d5851837e3d21ff3`) and the source-keyed [matrix](audit006-c2-c3-source-matrix.md) show that prediction-only per-caller maxima and pre-edit exact event-detail design continue to prevent the user's requested implement/use/inspect cycle. Main now proposes the explicit, broader **implementation-first guarded-size interpretation** below. It is a change to adopted pre-edit gates, not an inference that Audit006 passed. Only an independent exact-byte plan PASS followed by Main's recorded adoption under the user's standing autonomous-choice authorization can make it operative. This document itself permits no edit, test or runtime step.

The [21-row exact input snapshot](addendum021-inputs.sha256), SHA-256 `0df1347af8d0a88ce51b4371a7227407d80810b4e731f51d8f6884aed5c82b9e`, includes Plan053, the corrected user statement, the checked 79-row selected-source manifest, active/historical authority and both mutable status files. The status rows are preparation-time snapshots, not permanent hashes; rehash them at review and adoption.

## Explicit gate amendment and retained closure

Supersede **only** these prediction/design-stage clauses: (a) Addenda014–016/Plan051's theoretical finite cumulative `count × line maximum` whole-attempt ledger-byte sum, already displaced by Plan052/Addendum019; (b) Addendum014/016 C2 and Plan052's *universal pre-edit numerical* per-template fixed-byte plus finite future slot/environment/host maximum proof where it predicts values not yet materialized; and (c) the demand for an exact finished event-key/field/width/emitter table **before** edits. Clause (c) moves to implementation criteria and distinct post-edit exact-source review, not to runtime discretion. The source/edit gate still needs independent, read-only, exact-hash exhaustive selected source/import/callsite/path/creator closure, current/legacy direct-`R` grammar, exact six atomic direct-script pairs, fixed nine-suite/focus selection, typed slot/domain/source provenance, nested and L35 creators, and no bypassing OS create. Audit006's C1 PASS is a prospective inventory for its then-current hashes; a fresh C1 confirmation and plan adoption are required before edits. Any unclosed selected creator/import/path or unauthorized new source path remains FAIL. The [79-row source baseline](addendum018-selected-source-sha256.txt), SHA-256 `e3cd82db4cb127005761346bc589ff0706e38c1db906b3efbc29feac32f06478`, must be rehashed at that gate.

The amended rule is **materialize, validate and use the same actual bytes**. It does not admit over-limit commands/environments, reduce evidence, weaken ownership, or defer validation until after an OS create. Use the [Addendum020 candidate descriptor grammar](addendum020-proposal.md) only if independently reviewed and adopted as a concrete implementation choice; its unclosed numerical claims are not promoted to authority. A final implementation may choose another equally unambiguous versioned length-delimited grammar after independent source review, but it must encode separate executable and `argv[0]`, every argv/`-c` byte or final-hash-bound `-m` module, resolved/inherited cwd, API/file actions/group/session options, typed path provenance, command template ID and complete effective environment digest/count/bytes. Do not use generic helper acceptance as proof that all selected actual callsites are mediated: driver execve, capture, production/fixture Owner, supervisor, helper/race/scenario/nested/L18/L35 and D1–D6 must each pass through a reviewed gate. Selected threads retain H ownership without an invented process vector.

At every process create, render from its selected source-keyed template and typed substitutions exactly once, including actual temporary paths, candidate width and inline-code escaping. Construct the **complete final POSIX byte environment** from ambient plus overlays/removals in source order as one immutable mapping. If the original call inherited ambient values, pass this captured mapping explicitly. Reject unsupported bytes, invalid/duplicate keys, `=` in keys and NUL. Immediately **before ledger reservation**, compare renderer and actual executable/argv/options/cwd/module bytes and enforce existing limits on that materialized candidate: canonical command descriptor `≤16,384` bytes; final environment key/value aggregate `≤1,048,576` and count `≤4,096`; each NUL-inclusive argument and `key=value\0` within freshly acquired supported host per-string limit; and actual `Σ(arg bytes+NUL)+Σ(key=value+NUL)+documented host/API pointer/count/platform overhead≤fresh ARG_MAX`. A missing/unsupported host rule or byte mismatch fails closed. Store exact command descriptor and environment digest/count/bytes in the durable reservation as Addendum013 requires. **After** reservation is fsynced/read back, recompare the same descriptor and immutable environment bytes and reacquire/apply host limits immediately before the one OS create. No changed value, extra command, inherited unbound env, or second syscall is allowed. A byte-limit rejection before reservation creates no child/reservation; after reservation, a changed/unsupported pre-create check keeps the reservation charged until exact no-create evidence permits the existing Addendum013 cancellation route, otherwise leaves it unresolved under existing error precedence. It never pretends a child was made or retired.

Separately, before the driver's **first filesystem mutation**, use read-only exhaustive current/legacy high-water and exact typed content/path checks to derive fresh `{kind,index}` and `d(i)=max(3,decimal_digits(i))`; verify current output vacancy, active authority and source hashes, candidate-specific command/env/host feasibility, and exact returned Main positive handle of at most 16,384 decimal digits. There is no attempt-count or operational time ceiling. The exclusive ledger sidecar remains the first mutation; fsync and read back bootstrap before any B child/admission. The same exact returned handle is used for H retirement, and failed/uncertain work stays charged. Exact final post-edit module/source hashes replace the pre-edit source baseline at independent source review. No Main/tool command limit is weakened.

## Complete useful ledger and hard cap

Implement a closed, versioned event/field table in the existing `s1_helper_session.py` and selected callers, then require independent post-edit source review of that actual table and **every selected emitter** before diagnostic clearance. The table must preserve all current valuable data keys and selected terminal variants catalogued in [source matrix §B](audit006-c2-c3-source-matrix.md#b-exact-current-ledger-emitters-and-missing-prospective-schema): `attempt-open`, `main-handle`, `session-terminal`, root/descendant reserved/bound, `root-wait`, root/descendant retired, creator/L35 acknowledgments. It must add Addendum011's exact owner-incarnation/nonce/handle operation keys, durable one-shot wait claim/result, signal intent/result, pidfd pin/close, replay and uncertainty receipts. Every literal name gets required/optional keys, primitive/closed nested types, enum values, bounded field encoding, selected emitter, state transition and matching operation key; unknown keys or arbitrary nested objects fail. A field may be removed or replaced by a digest only after a field-by-field proof that selected assertions, recovery and ownership do not need it and a distinct reviewer approves. No raw environment-value copy is required in ledger when the complete immutable snapshot digest/count/bytes is bound as Addendum013 specifies; no required command descriptor, failure event, terminal identity or census datum is silently truncated or omitted. The detailed finished schema is **post-edit review evidence**, not a pre-edit theoretical event-count/byte-sum demand.

Under the existing ledger lock, reread and verify the actual canonical hash-chained committed prefix. Render one whole strict-schema compact deterministic JSONL event in memory. If actual verified size `S` plus complete encoded line length `L` exceeds **8,388,608**, reject **before** opening for append or writing; equality may append. Hold the lock through one write attempt, fsync and exact `S+L` readback and chain verification; only then count an event committed or consume its receipt. An ambiguous/short write is uncommitted. Under the same lock, attempt rollback only to last verified `S`, fsync and reread identical prefix; even verified rollback stops the attempt and reports the failure. If rollback/readback fails, preserve file without further mutation, stop all append/admission/retirement and report last verified `S`/hash, attempted `L` and observed errors. Physical tail size/content/hash are reported **only when actually read and verified**, otherwise `unknown`; never call a partial tail committed or continue from it silently. Tests inject partial writes and rollback/readback failure. No append can intentionally cross the cap; an OS write error can transiently leave an uncommitted physical tail, so the implementation and report must distinguish committed-prefix guarantee from unknowable physical state.

At cap, there may be no space for an emergency ledger row. Use the already authorized exact Main terminal family `main-launch-terminal-049-{kind}-{index}.json` only at its terminal phase, with canonical direct parent `R`, exclusive vacancy and same-file readback, to report a reviewed conditional failure schema: exact kind/index/Main handle; last verified ledger S/hash; attempted L and operation; cap/short-write/uncertain result; actual observed write/truncate/read errors; tail facts verified-or-unknown; charged B/H and required stop/next action. Existing proof/admission/send/poll/outcome/child fields are included only when those events actually occurred, without false success defaults; the source reviewer must verify the branch against historical `plan049-main-launch-terminal/v1` semantics and active direct-`R` rules. If durable terminal reporting is unavailable, Main uses its live transcript to state verified facts and unknowns and stops; it does not invent a sidecar or over-cap ledger row. Main reconciles exact same-handle tree evidence; unknown outcomes keep B/H charged and prohibit false retirement or a new attempt. A cap hit or uncertain tail **fails** acceptance. A fixed cap cannot guarantee completion of an unbounded workload; if measured work needs more than 8,388,608 bytes, Main must seek explicit numeric authorization rather than silently increasing it.

## Implementation, review and observed use

After independent plan PASS, Main may select the reviewer-recommended option under the user's standing autonomous-choice instruction, recording exact proposal/review hashes and this interpretation; only then request a fresh independent read-only C1 exact-hash closure. On C1 PASS, implement the narrow delta in **exactly** the existing nine paths: `docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py`; `scripts/vipe_benchmark/s1_validation_capture.py`, `s1_validation_contract.py`, `s1_helper_session.py`, `supervisor.py`; and `tests/test_vipe_benchmark_s1_helper_fixtures.py`, `test_vipe_benchmark_supervisor.py`, `test_vipe_benchmark_s1_recovery.py`, `test_vipe_benchmark_budgets.py`. Retain Addendum001/006's named portion limits; no new project source, method, callback, process, thread, sidecar, selector, suite, scenario or broad rewrite. The driver fixes first-mutation/high-water/host/handle ordering; capture/contract and every selected create bind exact template, final module hashes, immutable environment and same-byte OS call; helper-session/supervisor implement strict events/one-shot ownership/cap/error handling; four named test files provide focused coverage without changing original test intent.

After implementation, run focused checks in authorized scope: actual ledger `S+L` below, equal and above 8,388,608 with no over-cap or partial committed append; concurrent append lock; missing/extra event keys, each terminal/failure variant and one-shot replay; injected short write with verified rollback, failed rollback/readback with unknown-tail stop; command descriptor at/over 16,384; environment at/over 4,096 entries and 1,048,576 bytes; one overlong `arg\0` and `key=value\0`; full host vector at/over limit; mutated environment/argv after reservation; exact Main handle width; source/path/creator mismatch; charged unknown outcome and same-handle retirement. Verify original assertions, deadlines, order and error precedence. A **distinct independent exact-source reviewer before any diagnostic** then checks final nine-path scope, all actual selected emitters/schema and field-preservation map, renderer/OS equality, limits and failure branches, plus a fresh independently verified post-edit source/authority hash manifest. Tests do not substitute for that review.

Main then performs fresh source/artifact/ownership/capacity/output/high-water/session preflight and separately clears one serial CPU-only diagnostic; only a reviewed accepted diagnostic can justify a later separately cleared aggregate under the original nine-suite order. After each completed or stopped run, inspect actual ledger byte length when readable, every verified canonical line/hash and required evidence field/state, event content against selected assertions, external cap/uncertainty report and retained ownership. Mark unreadable physical size/content/hash unknown. Review measured bytes and content to decide whether further **safe** compacting is recommended; an observed successful run is not a universal future-size proof. No diagnostic, aggregate or other runtime action is authorized by this plan-only proposal.

Preserve all Plan049/Correction015 active direct-`R` domains/Table1 roles and current/legacy grammar, original W/C/equality-is-late behavior, protocol/scenario deadlines, assertions/order/error precedence, 78 source members/249 methods/1,028 callbacks, nine suites/six direct selectors, serial CPU-only `B+max(1,H)≤8`, 150 GiB artifacts, one ≤64 MiB memo, hard 8,388,608-byte ledger, 16,384-byte descriptor, final effective 1,048,576-byte/4,096-entry environment, fresh supported host string/vector limits, exact Main handle and job ownership. No operational wall/CPU/stage/invocation, attempt or correction-count ceiling is introduced. Diagnostic005 failed,006 lost/unused,007 retired and008 remains uncleared. No GPU/scientific/production/real-ledger/setup/download work is authorized. Never read `prompts`; report resource use unknown where unmeasured.
