The runner now implements all S0–S4, D0–D4, M0–M2 and N0–N2 routes, the nine isolated geometry arms, three prescribed repeats, finalist freeze, four conditional combined arms, and the single staged aggregation/report transaction. The implementation passed [204 CPU checks](implementation-validation-003.json); real native compatibility remains pending allocated execution.

The source review's handoff and accounting findings are resolved: results and nested artifacts bind successful ledger hashes; staged aggregation preserves one attempt; interruption/exclusivity/cleanup stops override worker errors; completed reports return without another pass; failed read-only preparation is charged; JSON publication is atomic and refuses replacement. Human import retains the reviewed bundle. Final static masks are assembled once from the accounted motion outputs, with 6,750 primary rows and 2,700 additional motion rows distinguished.

Each of seven setup recipes builds its prescribed environment once, pins the resolved dependency lock, verifies source/weight origins, builds required native extensions and qualifies imports without model forwards. Incidental CUDA initialization during native imports is recorded and monitored under the existing setup allocation; setup and inference both require exclusive host-visible GPU process ownership and the 22-GiB ceiling. Loaded model workers additionally hash the actual interpreter, imported source and mapped native libraries. Exact asset/dependency license assessments have a hash-bound admission path; unresolved grants remain unverified.

Transfers share a cumulative allowance. Direct response-body bytes and UV opaque TLS bytes retain durable uncertain-read reservations after errors or process kills; UV uses a single CONNECT-tunnel thread without terminating TLS or recording request contents. The charged UV amount is the larger of metered bytes and retained package/interpreter files. Storage counts unique inodes conservatively, so hardlinks do not duplicate physical consumption. These accounting scopes do not claim packet-level network measurement.

The [historical adapter review](historical-adapter-review.md) found that temporary checkpoint paths would defeat the native S0 weight cache. Stable per-process bindings now retain fresh predictor/tracker states without repeated weight retention; a regression fixture passes. S0 diagnostics capture native returned float32 boxes. D0 records its native float16 autocast separately from the float32 invocation.

[Full existing runtime inventories](existing-runtime-full-inventory.json) supplement the preserved [original qualification](existing-runtime-qualification.json). [GPU availability](gpu-access-002.json) records the successful prescribed host retry after a sandbox query failed. The original protocol, source assets, preparation and annotations remain preserved; no setup or GPU-model attempt was consumed during implementation.
