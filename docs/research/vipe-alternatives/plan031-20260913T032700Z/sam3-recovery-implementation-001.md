# SAM3 access recovery implementation

The user confirmed that SAM3 gated access was granted and explicitly instructed continuation. The pinned checkpoint, sidecars, BPE vocabulary and source are now downloaded and verified; [transfer evidence](sam3-download-verification-001.json) retains all original failures and charges every new byte. The checkpoint is 3,450,062,241 bytes and matches SHA-256 `9999e2341ceef5e136daa386eecb55cb414446a00ac2b55eb2dfd2f7c3cf8c9e` at the original prescribed revision.

[The narrow authorization](sam3-recovery-authorization-001.json) creates one separately identified `E3-setup-recovery-001` process. It is an explicit user-directed exception for SAM3, not an automatic retry pool. The checked ledger transition binds the user instruction and cleaned-up original failure, rejects duplicate recovery allocations, and retains the original failed state and time. Recovery acquisition also reduces cumulative setup time; its already-recorded CPU charge is conservatively retained. No other failed setup is reopened. The existing 57,600-second setup, 60-GiB transfer, 150-GiB artifact, eight-worker and exclusive 22-GiB GPU limits remain enforced.

Recovery creates fresh request/output/log identities. It reuses the verified checkpoint and source archive and extracts a separate source tree for editable installation, preserving the preparation tree. S3 can use only a successfully completed, hash-bound E3 recovery with the prescribed runtime and completed import-only qualification. The original E3 result remains unavailable. Final accounting/reporting separately discloses the recovery and original failure.

The [actual pinned source review](actual-e3-source-review.md) found no definite active-path API or dependency blocker. The adapter now observes the native image loader's detector-only selection and `strict=False` result, rejects missing required weights, records unused keys and the selected-key fingerprint, and restores observers on failure. It does not strict-load the full video checkpoint or alter key remapping. Video loading remains strict. Explicit device movement supports the already accepted `cuda:0` spelling without changing dtype. Native perflib overrides and nonserial video rank/world size are rejected; selected CUDA connected-component/NMS implementations are recorded. Import-only setup now includes the lazy bundled Triton entry points; no kernel or model probe is added.

[Validation007](implementation-validation-007.json) passes **304 CPU fixture tests** and preserves all 64 source/test files in an immutable local snapshot. New fixtures exercise one-shot recovery accounting, cumulative acquisition charges, rejected scope/tampering/concurrency/replay, source isolation without repeat asset downloads, successful qualification binding, missing image weights, restored native methods, unused-key disclosure, CUDA device selection and native serial/perflib guards. `git diff --check` passed. No model forward has run under this change yet; numerical behavior, memory qualification and benchmark quality remain unverified.

The earlier E1/E2 consumed attempts and E4/E5–E7 package-network stop remain in effect. This milestone does not establish completion of Plan 031 or its experimental comparisons.
