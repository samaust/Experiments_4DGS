# Plan 031 assessment after SAM3 access recovery

The explicit user resume resolved SAM3 gated access. Its pinned checkpoint, sidecars, BPE and source were downloaded and verified. The separately authorized E3 recovery completed its exact runtime and native import qualification. S3 calibration completed all 510 images; reconstruction failed before its first exported pair because the benchmark's Python audit hook blocked Triton's `/sbin/ldconfig -p` helper. This is an application guard denial, not a Codex denial or a recurrence of the SAM3 HTTP 403.

| Criterion | Status | Evidence and remaining gap |
| --- | --- | --- |
| P31-1: Reproducibility | Unverified | [Verified downloads](sam3-download-verification-001.json), [E3 qualification](sam3-setup-qualification-001.json), immutable implementation snapshots and [calibration results](sam3-calibration-results-001.json) preserve exact assets/runtime/outputs. Remaining matrix provenance is absent. |
| P31-2: Engineering | Unverified | [305 tests pass](implementation-validation-008.json), E3 setup and S3 image qualification succeed. [Video reconstruction fails](S3-reconstruction-failure.json) on the isolation guard; its native kernel/child-toolchain qualification is incomplete. The [review](triton-subprocess-isolation-review-001.md) identifies a scoped repair without claiming that an argv matcher alone confines compiler descendants. |
| P31-3: Reviewed quality | Not met | The user-authorized 232-image reviewed proxy bundle remains frozen. S3 image outputs now exist, but staged scoring is unstarted. Human truth, missing semantic strata and independent motion/static truth remain unavailable under the documented annotation amendment. |
| P31-4: Matrix coverage | Not met | [All 54 identities are accounted](matrix-accounting-004.json), including the one user-authorized E3 recovery: six complete, five failed, 40 blocked, three skipped. Explicit stop accounting is not completion of the remaining experimental comparisons. |
| P31-5: Integrity/accounting | Not met | Original failed attempts and time remain intact. All real jobs respected their recorded elapsed/device/storage ceilings and cleanup is confirmed. The previous E4 review overrun remains; the [SAM3 asset review](license-review-E3-assets-20260913T084127Z.md) additionally exceeded its internal 300-second cap by 2.543001 seconds, stopped, and charged 303 seconds conservatively. |
| P31-6: Supported conclusions | Not met | Formal staged aggregation and final report remain unstarted. Comparative quality and physical accuracy are unverified. Asset-only license findings do not establish the full E3 runtime's license preferences. |

Current consumption is 646.942 GPU seconds across four attempts; 3,565.713 setup seconds across five attempts, including the separately prepared SAM3 assets once in setup; and 7,996.919 CPU preparation seconds. The original CPU charge for those assets is conservatively retained. No motion, neighbor, aggregation or report attempt has been consumed. [Stop validation004](../../../../.local/vipe-alternatives/plan031-20260913T032700Z/stop-validation-004.json) verifies all 64 current source/test hashes, completed result-manifest hashes, unchanged original SAM3 failure, no active attempts and confirmed cleanup of every started process group.

The run is stopped, not complete. A new S3 reconstruction attempt is outside the original per-job attempt allocation. The saved failure proposes one additional attempt of at most 5,400 seconds, within the unchanged cumulative 93,600-second GPU cap and all other existing ceilings, only after authorization and a validated native-helper isolation repair. No retry, model substitution, cached-kernel workaround or guard relaxation occurred. The separate prior E4/E5–E7 network stop remains recorded; this SAM3-only recovery did not reopen it or other consumed setups.
