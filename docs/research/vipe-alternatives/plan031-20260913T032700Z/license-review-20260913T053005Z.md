# Exact local license review — S0, D0 and E8

Independent reviewer: Codex subagent `/root/geometry_scoring`, 2026-09-13.
Review started at 05:30:05 UTC, within the parent's 1,800-second preparation
allocation. The final local receipt records elapsed wall time. No ledger events,
model imports, inference, GPU work, installs, downloads, tracked implementation
changes or git operations were performed.

The review uses the complete frozen inventories in `qualification-full-inventory`
and the exact historical asset records. Commercial permission and **open-source
licensing other than AGPL** retain their separate frozen meanings. The literal
installed dependency scope is preserved, including proprietary platform packages;
no exception is introduced to make C2 eligible.

| Reviewed scope | Subjects | Commercial: verified / restricted / unverified | Non-AGPL open-source: verified / restricted / unverified |
| --- | ---: | ---: | ---: |
| S0 | 144 | 76 / 2 / 66 | 76 / 16 / 52 |
| D0 | 141 | 76 / 2 / 63 | 76 / 16 / 49 |
| Each of M0–M2, N0–N2 | 85 | 37 / 0 / 48 | 37 / 14 / 34 |

`licenses.assess` accepts all eight exact manifests/reviews when the proposed
events are supplied in memory. Its resulting whole-scope decisions are
**restricted / restricted for S0 and D0**, and **unverified / restricted for the
six E8 M/N scopes**. A changed-source-revision fixture is rejected. The actual
ledger was untouched.

The full ViPE source asset and editable distribution contain separate
Segment-and-Track AGPL-3.0 and UniDepth CC BY-NC 4.0 notices. The noncommercial
source terms restrict the complete recorded asset; AGPL separately defeats the
requested non-AGPL preference. This does not claim every S0 invocation executes
the depth code. AGPL itself permits commercial activity under its obligations.
The old SAM, DeAOT, Grounding DINO, BERT and UniDepth files lack exact local
checkpoint/snapshot grant records in their frozen asset manifests. Their missing
weight evidence is retained separately from source-code conclusions.

Fourteen installed NVIDIA subjects have custom platform grants. Their limitations
on modification and/or open-source relicensing prevent the literal whole-stack
open-source preference. They are not labeled AGPL or noncommercial. The notices
grant conditioned platform use, but unresolved embedded-component mappings leave
whole-subject commercial coverage unverified. Host Python, driver and operating
system library terms outside the package inventory remain an explicit separate
platform limitation.

The audit preserves material exceptions: `plyfile` GPL-3.0 supports both
preferences; OpenCV's FFmpeg LGPL-2.1 and non-headless Qt LGPL-3.0 notices are not
treated as AGPL or commercial prohibitions. NumPy/SciPy explicitly identify BSD
OpenBLAS/LAPACK and GPL runtime libraries with GCC exceptions. NCCL's actual
BSD-style notice conflicts with its proprietary metadata and refers separately
to NVTX; exact embedded-NVTX coverage remains unresolved. Native/vendored packages
without a completed component-to-notice mapping remain unverified rather than
inheriting a project-wide grant.

The separate geometry supplement records the RoMa code declaration (MIT except
Apache DINOv2), source-bundle and weight limitations, and E8 runtime findings.
Installed `romatch` originated from `.local/RoMa`; the admitted geometry checkout
is `.local/RoMa-edgs`. Their evidence is not substituted across those paths.

All records are under
`.local/vipe-alternatives/plan031-20260913T032700Z/license-review-20260913T053005Z/`:

- `S0-review.json`, `D0-review.json`, `M0-review.json` through `N2-review.json`,
  and matching `*-subjects.json`: dated reviewer, exact scope hash, per-subject
  evidence hashes, independent statuses and reasons.
- `installed-package-reviews.json` and `notice-catalog-v3.json`: actual local
  notices/metadata and the transparent text-recognition/manual-exception method.
  Earlier catalogs are retained as preparation artifacts. SPDX lookup source
  modules and AUTHORS lists are explicitly excluded from legal-grant inference.
- `validation.json`, `assessed-results.json`, and
  `geometry-license-supplement.json`: API checks and distinct geometry evidence.
- `review-receipt.json` and `proposed-registration.json`: final scope/artifact
  hashes, preparation wall time and proposed entries for parent review. These
  proposals do not append or charge the run ledger.

These are evidence assessments for Plan 031's preferences, not legal advice,
term acceptance, deployment clearance, altered research eligibility, or quality
evidence. Missing native/transitive grants and physical/model quality remain
separate unresolved matters.
