# Narrow SAM3 recovery review

Recommendation: permit exactly one separately identified E3 recovery after the user's reported confirmation that Hugging Face gated access was granted and instruction “Continue (download should now work)”. Record that instruction in a new immutable amendment before dispatch. This is an explicit exception to the consumed E3 setup-attempt limit; it does not reset the original attempt, reopen E4, or authorize E5–E7 network work. Preserve Plan 031, protocol/config hashes, runtime versions, model settings and all scientific gates.

## Minimal checked implementation

1. Add a checked ledger authorization transition for the single identity `E3-setup-recovery-001`, parent `E3-setup`, environment E3, components `[S3]`, maximum one recovery. Bind the amendment's file hash, exact user instruction, original failed request, failure evidence and finish-event hash `0984a3465bcb4c5b90cff666d43af9bcb9c3cf20649afe939324186d9eac62cc`. Under the ledger lock, require the parent to remain failed with confirmed cleanup, no active reservation, no prior recovery authorization/attempt, and available aggregate setup time. Make this a dedicated checked event, disallowed through generic `note`. Do not weaken `resume_unstarted`, which correctly rejects consumed attempts.

2. Resolve this registered identity as a setup job through one common checked job-spec lookup used by ledger reservation, supervisor and dispatch. Keep ordinary unknown identities rejected and retain protocol-derived jobs unchanged. Ordinary reserve/finish events then count the recovery as another setup attempt. Original E3 remains failed with **13.575414187973365 seconds** charged; all four failed setups retain **3099.9479431927903 seconds**. The recovery deadline is bounded by the remaining common **57600 − 3099.9479431927903 = 54500.05205680721 seconds**, recomputed at reservation. This authorizes one E3 exception only; unused time does not allow another attempt. Retain 60-GiB download, 150-GiB artifact, eight-worker, serial setup, exclusive-device and 22-GiB limits, including cleanup inside the deadline.

3. Use fresh `requests/E3-setup-recovery-001.json`, `jobs/E3-setup-recovery-001/`, sibling log and temporary directory. Bind the new request/result to the authorization and failed parent. Permit its E3/environment identity only through the registered recovery mapping; do not generally relax the CLI's setup identity check. Preserve old requests, failures, trees, partials, transfer receipts, environment outputs and validation archives. No delete, overwrite, rename-to-hide, or failed-state clearing is needed.

4. Reuse the separately prepared complete S3 assets once verified. During this review, the parent reported that a separately charged asset-preparation operation was already running in a fresh path: config access succeeded and checkpoint transfer exceeded 2.6 GB. It fetched new tree/README/LICENSE copies; the original successful tree, README (26038 bytes), LICENSE (7352 bytes), failed config attempt and transfer records remain immutable. Record this small redundant transfer openly and count every repeat byte. No repeated environment build occurred. The final recovery can use the existing complete `reuse_assets` interface with hash-bound prepared asset provenance, avoiding a new per-file continuation helper. An incomplete preparation must not be represented as a complete asset. Continue only the still-unstarted E3 environment/import/inventory operations using the prescribed recipe, with no retries or version fallbacks. Since model-asset acquisition is setup scope, ensure the separately charged preparation wall time also reduces the common 16-hour setup allowance exactly once (a recorded conservative deduction from the recovery deadline is sufficient if that time is currently only charged as CPU preparation). Preserve its existing historical charge; do not erase or relabel it silently.

5. Keep `result_record(local, 'E3-setup')` unavailable because the original failed. Add an explicit E3 setup-result resolver in `components()` that may select only the registered recovery's successful, hash-verified result. Validate its environment E3, components `[S3]`, pinned assets, runtime target and import/inventory records. Never select the latest arbitrary directory or treat recovered access/download as completed environment qualification. After successful setup and ordinary common admission, reopen only the unstarted S3 calibration/reconstruction slots through the existing checked resume transition, preserving their original GPU limits. Use targeted stage dispatch, not a broad matrix resume. Other dependency blocks, including G-S3's missing depth/motion prerequisites, remain in effect.

## Stop scope and tests

E4's distinct connection-reset stop and E5–E7 network setup remain stopped. The user's SAM3 access statement provides no evidence that the E4 network failure is resolved. E1/E2 remain consumed. A recovery failure consumes this exception; no automatic retry follows. New permission/access failures retain repository stop handling.

Before dispatch, CPU fixtures should reject absent/duplicate authorization, wrong parent/environment/candidate, unauthorized job IDs, consumed-recovery replay, old output reuse, concurrent reservations, exhausted aggregate cap, tampered retained files and incomplete recovery results. Confirm original failure/time remain unchanged, recovery adds exactly one attempt, successful recovery alone admits S3 through its exact result, and unrelated blocked states remain untouched. No such implementation/tests were executed by this read-only reviewer.

## Read-only evidence and accounting

Read AGENTS.md, Plan 031, saved status, ledger.py, execution.py, targeted controller/supervisor/setup code, E3 failure/transfer metadata and accounting003. No network, GPU, native imports, builds, source changes, ledger changes or commits. No prompts content read. Only this review document was written. Resource values above are from the saved accounting; dispatch must recompute live remaining budgets.

- `scripts/vipe_benchmark/ledger.py` SHA-256: `07baedcb59247f5a19f559517a12226abe80c873a241e48982d9aac5eb9e9030`
- `scripts/vipe_benchmark/execution.py` SHA-256: `3ab3942cb12d914ba11c7085d87875221150a82b9cb4b16f1ef47efbb381198a`
- `docs/research/vipe-alternatives/plan031-20260913T032700Z/E3-setup-failure.json` SHA-256: `959d92c3bcb2ea7e47b035af633568cc6363782159a65f24232120f7a2a1fc61`

Started 2026-09-13T08:24:27+00:00; completed 2026-09-13T08:27:55.189909+00:00; conservative full wall elapsed **208.190 seconds**, idle 0, within the new 240-second allocation. This is separate from the exhausted E4 review.
