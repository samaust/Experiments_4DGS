# Plan 049 — Complete the S1 progress blocker without an operational time ceiling

Prepared 2026-09-23 UTC. Calling task: [Plan031](plan_031.md). Run directory **R**: `docs/resolve-blocker/plan031-progress-20260922/`. This is a new prospective plan for B1–B4; [Plan048](plan_048.md), [correction001](../docs/resolve-blocker/plan031-progress-20260922/plan048-correction-001.md), [correction002](../docs/resolve-blocker/plan031-progress-20260922/plan048-correction-002.md), all reports and old source sets remain historical evidence. Current B1/B2/B3 are **not met**, B4 **unverified**. No test result, launch readiness, or Plan031 completion is claimed by this plan.

## 1. Authorization, duration and retained limits

The user's new CPU-only allocation explicitly removes all operational time limits. For this blocker there is **no wall-clock ceiling, CPU-second ceiling, stage allocation deadline, invocation timeout, reserved final-duration deadline, maximum focused/aggregate attempt count, or correction-count limit**. An elapsed duration greater than 300 seconds cannot fail B3. Do not replace these removed limits with larger finite numbers, timeout environment overrides, watchdog kills, admission-expiration timers, or an implicit exhaustion rule. This supersedes the 7,200-second allocation, 6,000-second source cutoff, 1,200-second reserve, 120/300-second focused/aggregate caps, and four/two launch caps in Plan048 and its addenda. Old attempts remain recorded; they do not constrain the new correction loop.

This removal concerns how long the work and validation invocation may take. **Original W (work), C (cleanup/total), equality-is-late rules, reservation values, protocol request/ack/census/setup deadlines, synthetic clock advances, and internal timing assertions that define the behavior under test remain unchanged.** In particular retain the existing L01–L36/P01–P06 real-scenario 2-second action/1-second safety-retirement assertions, original P deadlines, and cancellation/freeze cutoffs. These are observable correctness criteria, not a duration ceiling on this task. Do not extend them because a test fails. Distinguish each timer by its purpose in a static timer inventory before editing. Remove execution-only waits around the six direct declaration scripts (10-second invocation/2-second forced completion caps) and comparable generic subprocess waits that merely limit test execution; retain every scripted protocol timeout that is the subject of an assertion. No assertion about a production/internal deadline may be removed under this authorization. Report measured durations for scripts, scenarios, focused invocations, aggregates, stage work and the complete correction run.

Record start/end UTC and monotonic stamps, running session/PID identity, log offsets and actual terminal results. Poll/yield at intervals no longer than 60 seconds to preserve responsiveness; polling is not a termination deadline. CPU process-tree time is unknown unless measured; never label wall time as CPU seconds or unknown use as zero. Keep a duration journal, not a remaining-time countdown. A diagnosed deadlock can be interrupted for correction with evidence and owned cleanup; slow execution alone is not a failure or permission to kill a run.

Historical accounting is retained: known Plan031 baseline plus stages009–017 subtotal **30,269.38357106703 seconds**, with incomplete earlier overhead and Review duration unknown; Plan048 charged 300 seconds for Plan, 95.043 seconds post-plan handoff and 180.986 seconds predispatch preparation. Its correction002 implementer reported **872.169 seconds** of the former 910-second allocation. Independent recheck recorded **269.801104 conservative wall seconds** against 257.063, an overrun of **12.738 seconds** (rounded), and **812.738575 seconds** total independent upper charge. Main's last detailed checkpoint before that correction was 3,894.535 seconds; this is a checkpoint, not a complete final total. Preserve all measured intervening main/agent intervals and unknown overhead; do not invent a total or claim the old 7,200 seconds was unused. No duration ceiling remains for this CPU blocker. This does not release scientific or production authorization/headroom under Plan031.

Retain **B+max(1,H)≤8**, full process/thread/wrapper/descendant accounting, one active subagent, serial real scenarios/scripts and no extra observer/helper/thread/process/scenario. Keep the existing **150GiB artifact cap**, **one call-local memo entry ≤64MiB total retained key/content/derived buffers**, all protocol/schema bounds, numeric/counter limits and fixture dimensions. No GPU/device/model, production, real-ledger read/write/API, setup/download or scientific action; never read `prompts`. Synthetic CPU fixtures already in the suite remain permitted. Persistent storage/ownership uncertainty is a real stop, not a removed time limit.

## 2. Exact scope and preservation

Existing implementation files allowed by Plan048 remain:

- `scripts/vipe_benchmark/{s1_progress,s1_evidence,stages,s1_recovery,s1_cpu_helper,s1_helper_session,supervisor}.py`.
- `tests/test_vipe_benchmark_{supervisor,s1_helper_fixtures,s1_recovery}.py`.
- `scripts/basketball_vipe_worker.py` only if needed to propagate the original clock/context/reference.

The explicit removal of invocation limits requires a narrow additional exception: `scripts/vipe_benchmark/s1_validation_capture.py` and `scripts/vipe_benchmark/s1_validation_contract.py` may change **only** to represent, launch, bind and validate an explicit no-timeout execution mode while retaining legacy timed-mode validation. Current capture rejects >120/300 and waits with a timeout; current `validate_execution` requires elapsed≤timeout≤300. Leaving them unchanged would contradict the new authorization. Keep the runner byte-identical, including stdin, child argv, discovery, selectors, suite order and receipt collection. Preserve contract parsing/cache, source enumeration, typed IDs, counts, successful wait requirements and all other integrity checks. No environment-selected alternate authority or broad validation bypass is authorized. New mode must have an unambiguous version/schema and `timeout_seconds: null` (not zero, infinity, NaN or a huge number), and prove that its actual wait is unbounded. Reject malformed mode/value combinations. Historical timed-mode checks stay intact; no historical artifact becomes current evidence merely because its duration is now allowed.

New Plan049 driver/dispatch/status snapshots, baselines, patches, command metadata, evidence and reports use exclusive fresh paths under R; main alone updates compact `status.md` and parent status. Preserve old Plan048 driver and prepared commands as unlaunched history. Update the allowed `s1_helper_session` launch authority to pin fixed Plan049 plan/driver/main-dispatch/status and command/mode, with independently anchored provenance and fresh byte verification. Do not permit the environment to choose a dispatch, plan or relaxed schema. Add no source module or test method: current ordered **78 source members, 249 methods, 1,028 typed callbacks** remain exact. Retain original 248/1,012, all sixteen memo cases, all original assertion expressions in order/multiplicity except correction001's previously authorized late `elapsed==91` removal. Retain the six Plan047 declaration migrations exactly: hash/write/readback 5→3, reverify/pre_input 5→4, reuse 7→5. The only additional exception is the exact R2-B correction approved below; it is not blanket assertion-edit authority.

Preserve Plan047 §§3–8 and Appendices A–C and Plan048 §§3–5 as the detailed finite-case, numeric, schema and scientific contract, subject only to this plan's explicit operational-time amendment, no-timeout capture/contract exception and D49-1 assertion correction. Their exhausted launch budgets, former duration caps, obsolete workflow and ledger-reading instructions do not carry forward.

Keep `s1_clock.py`, generic `files.py`, runtime/backend modules, validation runner/cache algorithms, discovery, selectors, suite order, config and scientific settings byte-identical. Use S1-local gated adapters/equivalents inside allowed files for compound I/O, preserving original computations/bytes and non-S1 behavior. If that cannot expose an interior boundary without changing a frozen module, document the exact call graph and request one narrow scope decision; do not silently widen the file list. Preserve boxes/phrases/scores/thresholds/weights/preprocessing/SAM/tracking/S0 token-score assignment, full510/340/170 guards, strict full-result parser, small P04 and full completion/final-sample success/failure pair. No smaller fixtures, missing scripts, stubs standing in for guard/authority, split aggregates, or copied pointers.

## 3. Evidence baseline and narrow R2-B correction

Read and carry forward [implementer correction report](../docs/resolve-blocker/plan031-progress-20260922/implement-048-correction-report-002.md), [independent recheck](../docs/resolve-blocker/plan031-progress-20260922/validate-048-recheck-report-002.md), its static/collection/assessment JSON and correction002 source snapshot. The independent recheck verified all78 current records, allowed nine-file delta, 249/1028 collection, exact ordered assertion preservation, syntax, artifact hashes and scoped diff check at that revision. Reuse this as historical static evidence and as the before-map; rehash before assuming applicability to current files. V2 no-deadline historical clock sampling and V5 trusted optional-successor/path construction were repaired by inspection; V6 real numerical invalid/cap/eviction constructions and V4 real acknowledged-fallback mutations were improved. Their execution remains **unverified**. Zero focused/aggregate invocations ran under Plan048. Historical Plan047 P01/P06 failures and both 300-second aggregate timeouts remain failures; Plans041/043/044/045/046/047 strict acceptance remains false.

**R2-B is logically incompatible as presently written.** In `ReservationClockTests.test_identical_reuse_and_conflicts`, `later_failure` first creates a passed first-result, advances to **C+2**, creates a new `RuntimeError`, calls `preserve_failure`, then asserts (a) `primary.s1_first_result == original` and (b) `read_json(raw['path'])['clock_status'] == 'unverified'`. At that point no failure diagnostic for that new error exists. Preserving acknowledged first-result metadata already held in memory is possible and required. Producing the asserted newly readable failure diagnostic requires post-C publication, which is forbidden. Returning the passed first-result is not valid: its status is verified. Precreating a future error record, backdating it, moving the failure before C, mocking the read, using another error's file, or replacing a real file read with a virtual in-memory file would not preserve this callback's semantics. No feasible semantics-preserving route was established by source inspection.

**Decision D49-1 — approved narrow correction in this revised plan.** Main explicitly directed this plan to document the correction under the user's authorization. In only this `later_failure` callback, keep its ID, **C+2**, original exception class/text/object, first-result equality assertion and unchanged original file-byte assertion. Remove only the impossible assertion `self.assertEqual(read_json(raw['path'])['clock_status'],'unverified')`; replace it with `raw is None`, no new failure record, an explicit unavailable reason, and no-post-C open/read/hash/write/serialization/publication tripwires. Retain genuine previously acknowledged first metadata in memory without reopening it, using a trusted existing call-local/lifecycle handoff (not path lookup or a global cache), and prove no fresh qualification/authority arises from it. The exception/first-result references must remain available even when publication is unavailable; do not unconditionally clear already known acknowledged metadata at C. Perform the unchanged retrospective original-byte comparison only after leaving the production clock/tripwire scope, as existing tests do.

This is an explicit correction of the incompatible legacy expectation, not a semantics-preserving implementation of that impossible assertion. Preserve its old form in the baseline/history and record this **one** newly authorized AST difference alongside correction001's already authorized difference. Keep every other assertion in order/multiplicity, all other callbacks and clock values. No timing shift, fictional prewritten failure record, post-C publication or weakened no-I/O rule is authorized. Add runtime evidence of the real after-C branch and independent review of this exact exception before B1/B3 can pass. The decision is resolved prospectively; implementation and validation remain outstanding.

## 4. Ordered corrections and observable gates

Use one high-effort Astra implementer for this consequential blocker under resolve-blocker, no nested delegation. Reuse it for related corrections. Main owns all runtime launches and acceptance. Before edits, save current exact source records, git diff, ordered assertion/typed declaration map and timer-purpose inventory. Hash-bind them to this plan, authorization note and dispatch snapshot. Work through the following coherent correction sequence; runtime proofs occur in the preserved collected tests.

### 4.1 W/C and R2-A production observations

1. In `s1_progress.operation`, recheck original W immediately **after** `trace_event('start')` returns and immediately before invoking the actual operation. Test trace returning before/equal/after W and trace failure; no successor call at/after W, primary preserved. Apply equivalent reasoning to every trace, audit, callback or publication between a deadline check and its subject operation.
2. Finish the compound call graph: runtime capture, backend initialization, loaded-runtime collection, request/context/source verification, inventory/recheck, row publication, first-result read/reconcile/preservation, result acceptance and terminal preparation. At each fallible inner open/read/stat/hash/parse/decode/serialize/write/flush/fsync/normal-close/readback/publication enforce the original applicable W/C immediately before and after. An outer wrapper alone is insufficient. CPU fixtures exercise these code paths with existing native/device seams; do not execute real model/device setup. Audit NPY/hash, each NPZ member/finalization, second cleanup array, raw guard and directory/link operations distinctly. No late NPZ exception-finalization writes; descriptor retirement is distinct from normal close/publication.
3. Fix `open_parent` close-failure ownership: after acquiring `new`, both the old and new descriptors must have an explicit owner until their individual successful retirement, even if retiring old raises. Track uncertain close state conservatively and avoid blind retries that could close a reused FD. Inject close failure and independently prove no new-descriptor leak, no double close and original-primary preservation.
4. Resolve **R2-A** by attaching the existing load observation/injection to the actual S1 production RGB acquisition/decode seam shared by segment and qualification. The current `S1RGBLoader.load`/`load_rgb` bypass of patched `access.RGBLoader.load` must cease to bypass observation. Changing the patch target is allowed if it observes actual work; synthetic counter increments, redundant dummy loads, old unsafe RGB I/O or removed memo checks are not. Keep every exact count (6/8/2/… and existing migrated declarations), frame50/frame62 sets, single prediction assertion, and second-input deliberate stop. Prove one observed event corresponds to one actual logical RGB load and retain gated internals/real byte equivalence.
5. Build the **exact segment next-input** gate: acknowledge/reverify the first frame50 record, reach the segment loop immediately preceding frame62's actual loader operation, advance W at the preceding real return, tripwire frame62 RGB/valid/native calls and any subsequent raw work. Before-W control reaches frame62 once and observes the existing deliberate stop; equal/after do not. An unrelated guard or an earlier qualification failure does not count.
6. Complete all **114 preserved deadline callbacks** with an evidence row per typed ID: target operation and preceding real return, exact clock value/W/C, actual target entry/exit, distinct forbidden-successor tripwire, partial byte hex/hash/length, descriptor acquisition/normal close/retirement observations and primary/secondary result. Save zero-entry observations explicitly, not absence inferred from a generic TimeoutError. Preserve all before/equal/after values and target distinctions. Cover the trace-crossing and open-parent edges within existing methods/callbacks without altering their declaration contract.

### 4.2 Ownership, retained state, recovery and cancellation

7. Exercise the actual Owner.run helper, supervisor worker, direct-script, L18 sentinel, existing helper-test/race workers and existing detached descendant creation entrypoints through pure syscall seams. The four registry callbacks must invoke these enclosing paths, not call the common gate with event labels. At each existing creation, perform fresh pinned authority/capacity audit, recheck original W after it, retain unresolved PID/handle before fallible identity registration, and guarantee finally cleanup. Inject creation, registration, census/identity and cleanup errors; save returned handle and observed retirement. Race workers without a new process group require handle/PID-specific retirement, never an assumption that `stop_group` owns a group. Detached child commands need local registration-failure cleanup before proceeding; no orphan on exceptional exit.
8. Retain full boot/PID/start-tick/PGID/thread identities, terminated ancestry, all detached roots, live charges and unresolved ownership. Unknown identities remain charged and block creation until resolved. Verify ≤8 at every existing creation, including capture/runner/owner/native wrappers. No new process just to observe it. Ensure completion retires every known child/wrapper without erasing uncertainty or killing unrelated processes.
9. Complete detached **Consumer** state: use the existing detached scenario/seam to carry attached→reparented full identity through retained producer binding, actual Consumer acknowledgment/reconciliation and fallback; reject PID start-tick reuse. A standalone Owner.lineage lookup followed by unrelated publication is insufficient. Exercise accepted/acknowledged/frozen state separately.
10. Execute and finish the actual acknowledged reconcile-fallback mutation route for after-publication aliases: real second original row, acknowledged fallback, closed inventory mutation, real integrity failure and production `record_progress_failure`, frozen pointer/count/history, poisoned/stop state and rejected cold recovery. Preserve generic controls as additional checks. Reach the enclosing counter/parser at every capacity endpoint with exact observed counter and proof no smaller unrelated bound masked it.
11. Complete strict current/previous/intended/retained/optional-body matrix: exact next-generation path and trusted successor intent, missing head/body versus integrity fault, validly shaped future/self-issued references, malformed nested schema rejected by its intended validator rather than KeyError/TypeError. Keep retained lower bounds, conflict/overflow/dropped/first/last/primary diagnostics, stop_required/summary_adapter, semantically invalid but produced=1/qualified=0 sealed first row, duplicate-result inventory and actual late-recovery W advance. Cancellation freeze must have an empty read/retain/send suffix; late visible ack remains uncertain, producer receipt differs from owner acknowledgment. V2 historical authority must run without any live monotonic/boot sample when no live deadline exists.

### 4.3 Numerical/memo relevance and original failure evidence

12. Execute all sixteen ordered `test_progress_plan048_pure_memo` cases from Plan048 §4, preserving their full meanings: snapshot_member_once, uncached_valid_equivalence, uncached_invalid_equivalence, same_size_restored_mtime, inode_replacement, path_replacement, request_changed, context_changed, identity_changed, source_changed, lookup_before_w, lookup_equal_w, lookup_after_w, retention_cap, eviction, failure_cleanup. Preserve original numerical preprocessing and all envelope checks; the invalid case changes a real native logit. A hit still performs fresh anchored bytes/hash/identity/source/context/request checks and all authority/qualification decisions. Integrate context/source rejection with the **same warm-memo production pipeline**, not a separate Publisher demonstration. Prove ≤64MiB combined retention, unchanged uncached path above cap, actual padded numerical fixture, two real pair eviction, exact result/rejection equivalence, one materialization/member and clearing on failure/exit. Record operation counts/durations; speedup is measured evidence, not a required maximum runtime or reason to alter scientific checks.
13. Complete each P01–P06 real supervisor outcome, retaining its named fault and original primary class/text/kind/phase/first-observation time. P01 keeps deadline classification and empty late raw/reconcile/first/runtime/accept suffix; P06 keeps timeout/acceptance classification apart from secondary cleanup/publication failures. P05 stays **pre-head-replace** and proves that boundary/partial bytes, never claims post-replacement readback. Keep P04's small fixture distinct from full510 completion/final-sample pair.
14. For child/owner trace correlation, enumerate each required boundary subsequence (raw guard, write/flush/fsync/normal-close/readback, checkpoint/head/ack as applicable) and bind original reservation, W/C, session/request/producer full identity, sequence, sealed version/hash and pointer. Require terminal witness or explicit missing-tail failure; zero overflow is not proof of completeness. If an event claims an installed body, absence fails correlation. If the event precedes install and absence is expected, require exact preinstall fault/byte evidence and no later installation/ack claim. Correlate producer-produced, owner-retained/acknowledged and accepted versions separately; never skip verification merely because a path is absent. Trace writing itself respects C; parent retrospective reporting is labeled separately from production I/O.
15. Protect the **entire** exceptional cleanup path, including lifecycle close/reap, record assembly/serialization, evidence file open/write, secondary JSON write, timing assertions and final trace checks. Capture primary before cleanup; every cleanup error becomes ordered bounded secondary evidence without replacing it. If secondary persistence fails, retain that failure in memory and report it through already-authorized enclosing evidence; do not recurse into unbounded late writes. No pending primary means genuine cleanup failures still fail the test. Verify both branches, including scenario-secondary write failure. Timing violations remain separately failing evidence, not swallowed to preserve the primary.

## 5. No-timeout launch preparation and reproducible commands

Implement the narrow capture/contract exception in §2 and a fresh **R/launch-049-exec.py**. Driver accepts `diagnostic|aggregate`, a strictly increasing positive attempt index with no maximum (format at least three digits, no three-digit upper bound), and a concrete reason. Use `execve` into capture, no extra live wrapper, no timeout/admission-expiration/count-ceiling checks. Freshness instead comes from immediate independently pinned bytes/identity rechecks. Each main-created exclusive admission binds current complete sources, immutable dispatch/status/plan/driver, command/environment/stdin, ownership root/terminated ancestry, output paths, current counts for history only, cleanup resolution, resource capacity, no-timeout mode and the concrete correction/evidence reason. Retire unused names; never reuse an output directory. Main must inspect partial effects before retry.

Exact proposed capture CLI after implementation:

```text
.local/envs/stg-colmap/bin/python -B -m vipe_benchmark.s1_validation_capture <absolute R/diagnostic-049-NNN> --no-timeout --diagnostic
.local/envs/stg-colmap/bin/python -B -m vipe_benchmark.s1_validation_capture <absolute R/aggregate-049-NNN> --no-timeout
```

`--no-timeout` is to be implemented, not currently available. It is mutually exclusive with an explicitly supplied `--timeout`; retain legacy default/timed capture behavior outside this prospective mode. Capture waits to actual child completion, records monotonic elapsed and UTC, binds mode to Plan049 admission and emits the versioned execution evidence. Validator retains exit0, wait_completed, timed_out=false, actual PID, receipt, stdin, runner/capture/interpreter and exact source before/after bindings; no elapsed≤300 test for this mode. Use the unchanged child `.local/envs/stg-colmap/bin/python -B -`, with exact contract stdin and final newline. Pin thread settings OMP/OPENBLAS/MKL/NUMEXPR/OPENCV to1, VIPE_CPU_VALIDATION=1, absolute scripts PYTHONPATH; unset S1_HELPER_DIAGNOSTIC/S1_RECEIPT_DIAGNOSTIC before capture, with only the existing diagnostic selection set by capture.

Main-owned command for the first justified focus after readiness:

```sh
exec /home/auss/git_repos/samaust/Experiments_4DGS/.local/envs/stg-colmap/bin/python -B /home/auss/git_repos/samaust/Experiments_4DGS/docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py diagnostic 1 'Plan049 integrated corrections and memo coverage'
```

Main-owned complete aggregate, when the focused evidence and static recheck support readiness:

```sh
exec /home/auss/git_repos/samaust/Experiments_4DGS/.local/envs/stg-colmap/bin/python -B /home/auss/git_repos/samaust/Experiments_4DGS/docs/resolve-blocker/plan031-progress-20260922/launch-049-exec.py aggregate 1 'Plan049 complete current-source independent validation'
```

Use the next vacant index if one is already retired. Later commands differ only in fresh index/reason and bound revision; no selector changes. Diagnostic runs retain the complete existing HelperSessionTests selection; aggregate runs exactly **s1_semantics, s1_recovery, backends, contracts, component_recovery, execution, budgets, supervisor, review_annotations** in that order. All42 L/P scenarios, six direct-script declaration checks, 249 methods/1028 typed callbacks, full guards and no-timeout contract controls must complete. Recovery/R2-A/R2-B and historical-authority tests outside the focus are established by the whole aggregate; a passing focus does not claim them. Test no-timeout mode parsing/receipt semantics through existing collected contract-oriented methods with unchanged literal callback declarations, including legacy timed rejection, null/mode mismatch, fabricated early completion, missing wait/receipt and a synthetic elapsed>300 accepted only with otherwise valid no-timeout provenance. Do not add a >300-second sleep just to demonstrate the contract.

Main saves exact tool start, synchronous/session identity, polls, outer terminal exit/signal, capture execution.json, inner receipt, stdout/stderr and owned cleanup independently. Missing receipt, source drift, failed outer exit, skipped/error/failure/discovery counts or unresolved child state fail acceptance regardless of inner text. No standalone profile/probe or speculative aggregate to rediscover known static contradictions. No arbitrary number of attempts; each correction run needs a changed source or concrete invalidating concern. No automatic rerun of unchanged passing evidence.

## 6. Independent validation and acceptance matrix

After source handoff, activate a **distinct** high-effort Astra validator, no nested delegation, one active subagent at a time. It does not edit implementation/tests or validate its own work. It independently inspects actual code, before/after hashes, assertions, timer classification, R2-A relevance, D49-1 implementation and exact assertion exception, all named target evidence and source-bound receipts. It reports static launch readiness before main launches, then independently assesses execution artifacts and outer completion. Main supplies requested launches; validator separation is preserved. Reuse historical static evidence only where exact unchanged bytes/inputs apply. Every source/command/environment change makes affected runtime evidence stale; the final corrected source set requires a fresh whole aggregate. Independent receipt/trace inspection is mandatory even after a green aggregate, but a new validator alone is not a reason to rerun an unchanged passing aggregate.

| ID | Initial state | Required observable acceptance |
| --- | --- | --- |
| B1 | not met | R2-A actual RGB observations and unchanged counts; D49-1 exact correction independently validated; original W/C at every inner operation including post-trace gate; all114 target/successor/partial-byte/retirement observations; exact next-input boundary; close-failure ownership; actual creation/registration-failure paths; fresh authority and ≤8 live charge; full detached Consumer/fallback/cancellation/strict recovery matrix; historical no-clock contract; all16 real numerical/memo cases, integrated context/source checks; preserved declarations/assertions with only documented narrow exceptions. |
| B2 | not met | All six P scenarios actually reach their real supervisor fault; original primary plus ordered secondary/timing/cleanup outcomes; complete child/owner boundary and version correlation; exact pointer and acknowledgment/acceptance distinction; explicit trace truncation/absence handling; no post-C production evidence I/O; cleanup/secondary-write failures do not replace primary. Historical failures remain unchanged. |
| B3 | not met | One complete current-source unchanged-suite aggregate, exactly249/1028 including original248/1012, all42 scenarios/six scripts/full guards, zero failures/errors/skips/discovery errors; exact source/command/environment/stdin/receipt/execution agreement and actual outer exit0, completed wait and resolved cleanup. **Record elapsed duration with no upper duration criterion and no invocation timeout.** No sum of partial/split runs substitutes for this aggregate. |
| B4 | unverified | Only allowed code/evidence changes and explicit no-timeout contract exception; independently checked preservation maps/source history; no forbidden operations; CPU-only/concurrency/storage/memory bounds satisfied; complete fresh ownership/retirement and duration journal including old overrun/unknown intervals; historical acceptance unchanged, ledger conclusions explicitly inherited, no scientific/production authority inferred. |

## 7. Correction loop, commits and return to Plan031

For each failure/unverified gate, append a correction record under R with criterion, observed evidence, root cause or supported hypothesis, exact files, intended change/checks, selected effort and **CPU-only allocation with no time or attempt ceiling**. Continue implementer correction → distinct-validator recheck → main-owned justified runtime check. Do not automatically restart Review/Plan, repeat unchanged probes, lower criteria or shrink fixtures. A repeated failure calls for a revised evidence-supported hypothesis and appropriate reasoning effort, not blind reruns. Escalate only a concrete unresolved problem under resolve-blocker; no nested agents. Main records current stage, agents/jobs/logs, criterion states, consumption and exact next action at meaningful transitions.

Do not stop because an old reservation, number of attempts, or elapsed duration is exhausted. Stop affected work only for user interruption, required repository permission/access stop, unresolved ownership/capacity/integrity risk, missing required validator/capability, a newly discovered logically inconsistent acceptance requirement, or concrete missing information/authorization for which no independent progress remains. Preserve partial effects and live jobs. Follow AGENTS.md's one safe require_escalated retry for suspected sandbox/host-access failure; inspect partial state first, retry only the failed operation, wait pending approvals, stop after denied/unavailable/failed outside-sandbox retry. Report exact command/error, definite versus suspected restriction and existing/missing scoped rule. Never bypass denial or substitute data/configuration.

Main alone accepts B1–B4 after the independent report substantiates **all met** and final hashes are current. Save implementation report, exact source snapshot/map/patch, target evidence/relevance matrix, timer inventory, no-timeout authority/command records, all runtime/outer artifacts, independent assessment, duration/cleanup record and limitations. No production-ledger access is needed to produce these artifacts.

Create task-only local commits for completed independently validated implementation milestones, with title and description; no commit during this Plan assignment or for an unvalidated partial source set. Main stages explicit paths using separate escalated `git add` (prefix `["git","add"]`), inspects staged diff, then separate escalated `git commit` (prefix `["git","commit"]`); preserve unrelated staged changes and do not push/amend/rewrite history. A commit is a checkpoint. Continue remaining authorized blocker steps until complete, then return evidence to Plan031 and assess its next gate. Resolving B1–B4 is not by itself completion of Plan031, S1-2/S1-3 or any scientific/reporting gate; this CPU-only authorization cannot launch production/model/GPU work.

## 8. Correction016 — conditional parallel-work planning authority

The user authorized six parallel development-and-test workstreams, followed by a return to the existing ordered, serial implementation and testing sequence. Correction016 is adopted as **conditional planning authority only**: [Correction016 proposal and gates](../docs/resolve-blocker/plan031-progress-20260922/plan049-correction-016.md). Its six bounded lanes and phased charge analysis may guide preparation and independent review. This adoption does not authorize temporary source edits, focused runtime tests, a diagnostic, or an aggregate.

For this bounded six-lane phase, the user's explicit parallel-work authorization supersedes Plan049's one-active-subagent limit for coordination and isolated lane preparation. Main may use up to six subagents at once alongside Main, assigning one bounded lane to each; no nested delegation is allowed. Each lane must use its own artifact/patch area, and no agents may edit the shared worktree concurrently. This exception does not authorize unreviewed implementation or tests and does not waive complete ownership/resource accounting if a subagent launches any process.

The existing `B+max(1,H)≤8`, CPU-only scope,150GiB artifact cap,64MiB memo cap, behavioral deadlines, assertions, selectors, declarations, and final aggregate remain unchanged. The capture/runner identity remains fixed. Any future six-lane runtime must use one independently reviewed, frozen canonical source set; show useful concurrent checks and receipts for all six lanes; retire workers before helper-heavy peaks; and prove complete process creation, registration, uncertainty, and retirement accounting. Bind intended lane output-path strings before admission but keep the attempt directory vacant; capture creates it exclusively, then the runner creates lane roots exclusively. Bind records in the current contract's acyclic order (source manifest → session proof with prospective admission path → admission binding both strict records → lane receipts → receipt index). The current six-way focused-test selector, source manifest, shared ownership gate, and temporary launch changes are unproved. The specific P16-1, manifest/anchor, and gate checks in Correction016 are stop conditions until a decision-complete addendum receives independent plan and source review.

C1–C3 session-proof blockers remain independent and unresolved. Correction016 does not authorize any evidence trust change. No parallel test, diagnostic, alternate wrapper, or aggregate may launch until the current proof contract is met or a separate, explicitly authorized and independently reviewed Correction008 trust/evidence amendment is adopted. After any separately authorized temporary phase, the exact reviewed inverse patch, serial diagnostic, and unchanged final aggregate remain required.
