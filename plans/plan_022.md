# Plan 022 — Repair complete evaluator coverage and adjudicate the fixed cohort

## Objective, scope and authority

Implement [iteration 006 Review R1–R8](../docs/continuous-improvement/20260908-plan016/iteration-006-recommendations.md)
for the unchanged [objective](../docs/continuous-improvement/20260908-plan016/objective.md)
and [assessment](../docs/continuous-improvement/20260908-plan016/iteration-006-assessment-01-review.md).
Produce v15's real full-component, public callback, cold-transform, operand audit
and retained-decision coverage. If and only if admission and frozen readiness
pass, adjudicate exactly the previous 16 cases/32 cold-returned slots once.
This advances SC-05 and prerequisites of SC-01/SC-03; accepted Basketball timing,
reconstruction and its measured comparison remain outside this allocation.
Preserve SC-02/SC-04 evidence and historical SC-05 limitations.

Read AGENTS.md, objective, status, latest assessment, Review and this plan before
Implementation and after context loss. No delegation; never read `prompts`
content. Parent owns authorization, criterion assessments, status and checkpoint
commits. Implementation saves changes and validation evidence for task-only
milestone commits coordinated with parent. Follow all interruption, git and
single safe same-command escalated retry/permission stop rules.

AGENTS.md standing approval `b62f2ea` and the active explicit same-objective resume
cover these Review-recommended, Plan-finalized limits. Parent compares them with
remaining user ceilings, then records this plan's hash, scope, consumption,
approval basis and a new T0/deadlines in run artifact
`iteration-006-plan022-authorization.json` before dispatch. No routine user
confirmation or implementation clock starts in this Plan stage. The required
source coverage inspection below is an implementation check, not an approval flow.

## 1. Finite allocation and preserved history

| Scope | Hard limit |
| --- | --- |
| Entire Implementation, admission, source work, toys, supervision, evidence, commits | 7,200 elapsed seconds from parent T0 |
| Read-only admission complete | T0 + 900 seconds |
| Complete frozen coverage and final applicable passing readiness suite | T0 + 4,800 seconds |
| Science and every owned process cleaned up | T0 + 6,300 seconds |
| Scientific invocation, imports/setup/comparisons/I/O/cleanup included | One invocation, at most 1,500 wall seconds and bounded by T0 + 6,300 |
| Candidate / independent reference methods | One / one; unchanged Decimal80 and scalar-jet/rational methods |
| Fixed toy suite | At most two supervised invocations; each at most 120 wall seconds including imports, I/O and cleanup |
| Numerical concurrency | One numerical worker plus supervisor; toys and science never overlap |
| Scientific contexts, setup, SVD and component work | Exactly section 6's schedule for complete qualification; no extra entries |
| Optimizer/local fit/initialization/restoration attempts | Zero; mock callback plumbing only in specified toys |
| Scientific differences, displacements, rays, pilots and full screens | Zero |
| Training, rendering, GPU/device, network/download/install work | Zero |

Use `.local/envs/calibration-global/bin/python`, installed NumPy/SciPy and stdlib
Decimal/Fraction. Set OMP_NUM_THREADS, OPENBLAS_NUM_THREADS, MKL_NUM_THREADS and
NUMEXPR_NUM_THREADS to `1` before startup. Reserve the last five seconds of each
numerical invocation for TERM/KILL/reap; toy process-tree subcases have their own
three-second bounds and two-second cleanup reserve. Import and setup time counts.
The final 900 phase seconds after the science cleanup deadline cover retention,
validation and commits. Check deadlines inside long basis/setup, comparison and
rational transport loops, not only between contexts.

Suite 2 is allowed only after a documented implementation correction to suite 1's
failure, with unchanged frozen fixture definitions/test bytes. No unchanged green
rerun, third suite, ad hoc scientific import, development evaluation or numerical
probe. If the frozen test omits a required behavior, readiness is incomplete for
this plan; do not edit that test and treat a subsequent run as the same suite.
Missed admission/readiness forbids science. No post-science numerical repair or
retry. A launch failure after invocation consumption spends that invocation.
For a permitted source correction, retain suite 1's original source map and add a
source-only correction record binding the unchanged coverage/assertion definitions
to suite 2's new source hashes/call sites. Do not replace historical mappings or
use this record to add omitted tests.

All previous allocations are terminal; unused attempts/time are not reusable.
Preserve Plan 016's 405 outcomes and six preflights, and Plan 018–020 consumption
as recorded in [Plan 021 section 1](plan_021.md). Plan 021 consumed both toy slots,
2.873430340 toy seconds and 1653.358525463 final phase seconds, with zero science;
commits `147e0cb` and `0c5100b` remain historical. Training stays exactly
**22523.417254 charged seconds** under the 24-hour total and unchanged 7200-second
method/scene ceilings. No stopped method restarts or allocation redistribution.
No overall loop ceiling was supplied. This new plan's exhausted allocation does
not prevent an otherwise authorized subsequent Review/Plan.

## 2. Versioned surfaces and command contract

Create `_v15.py` versions of the six `_v14.py` modules: acceleration Decimal,
components, accounting, solver, independent reference and evaluator harness.
Create `tests/test_basketball_shared_evaluator_v15.py`,
`docs/experiments/basketball-shared-timing-v15/`, its sibling `.md` report and run
artifacts `iteration-006-implementation.md` and `iteration-006-validation.md`.
Preserve every v14 and earlier source/evidence byte. Carry forward acceleration
math unchanged except immutable ownership, instrumentation and deadline wiring.
Do not turn the injected `solve` callback shim into an optimizer implementation.

The stdlib harness exposes `admit`, `toy`, `ready`, `run`, `package` and guarded
internal `worker`/`toy-worker`. `toy` exclusively consumes the next suite marker
before imports and supervises the complete frozen test. Direct test/worker entry
without the matching owned token/PID/marker fails before numerical imports. Both
toys and science use production supervisor/entry/chain/comparator functions; tiny
manifests use explicit test roots rather than changing global scientific roots.
Record exact commands, source bytes and environment. Consumed markers are never
deleted. Source/JSON/AST/hash/link inspection outside toy slots is read-only and
imports no numerical module. Packaging imports no evaluator mathematics.

## 3. Admission and immutable authorization chain (R5/R6)

Keep [Plan 021 section 3](plan_021.md)'s exact case order, original cold/returned
pins, observations, corrected-time operands, actual multipliers, saved transforms,
state/dependency identity and historical source substitution requirements binding.
There are six baseline conditional cases `00,07,13,03,10,16`, six metric conditional
cases in that order, then metric `joint-00/descending`, `joint-01/cold`,
`joint-02/ascending`, `joint-03/ascending`. Conditional paths end in `/cold`.
Seven cases have weight one and nine zero; 54-dimensional conditional and
55-dimensional joint states, one group, 18 coefficient rows, 300 observations,
150 quadrature samples, window `[50,149]`, spacing 10, gauge camera 1.

Read-only admission may byte-copy each v14 input into a separately owned v15
candidate/reference input directory after verification. Both original copies
must hash to `1b4779b8c0328f9f893af6c2cd1b2fa9133bdad337467a2f805525d8913c1eed`;
v14 admission must hash to
`7f294998d9ee7f12448740710bd7951ec503f111297a96582024f843e240611c`.
This authorized local source reuse does not waive original provenance validation.
Recheck original v10 artifacts/receipts/hash-linked journals, unique cold-transform
and exact returned ledger/last callback, actual q/x/y/vg/vby, saved P/origin/D,
problem keys, dependencies, manifest/policy/source maps and v11 substitution
attestations. Recheck Plan 021's v11/v13 input pins, quadrature bytes and v12/v13
chain. Preserve synthetic benchmark origin and held-out exclusions. No coordinate
arithmetic, quadrature regeneration, new observations, states or constructor fits.

Implement one stdlib `verify_chain(stage, root, authorization)` used by admission
finalization, `ready`, `run` before consumption/spawn and worker before numerical
imports/input consumption. Its required graph is:

1. Parent auth pins Plan 022 and objective, run identity, T0 and all absolute
   monotonic/UTC deadlines, entry limits, thread policy and historical consumption.
2. Admission pins auth, its original admission source copy, both complete input
   files, archive, provenance, historical/environment manifests and previous v14
   admission/input hashes. Manifests pin their actual leaf bytes, not just labels.
3. Readiness pins admission/auth, frozen schedule/schema/tolerances, complete
   coverage definitions/map, source-only inspection, final applicable test result
   and its consumed marker, frozen test and current six source hashes, resolved
   imports and environment. Each passing coverage row has a matching executed
   assertion/call-site/boundary record from this exact suite/source version.
4. Started marker pins that readiness graph, auth/deadlines and sources, supervisor
   token/PID/namespace; worker checks those links and actual process identities
   before its exclusive consumed marker and numerical imports. Environment leaf
   hashes and four actual thread variables must match. Check admission completed
   by +900 and readiness by +4800; launch in time to meet +6300 and +1500 limits.

Historical auth is retained as historical; it never grants a current invocation.
Freeze new source/test hashes after source-only review. Expected substitutions
must be explicitly attested; a changed current leaf is not explained by merely
rewriting a manifest. Reject source/input/auth/deadline/coverage mismatch before
dependent math; do not regenerate a missing operand. Bind source paths as well as
hashes and reject excluded paths. Candidate/reference inputs contain original
operands only; archive/classifications live solely with the comparison driver.

## 4. Required implementation repairs (R1–R5)

Retain all [Plan 021 section 4](plan_021.md) mathematical definitions, context,
operation order and independent method. In particular use explicit Decimal80
ROUND_HALF_EVEN with its existing exponent/trap policy, exact binary64 decode,
no snapping/adaptive precision, full coefficient gradient/Hessian, separate image
and acceleration terms and an exact inactive acceleration contract. Reference
uses its own scalar basis derivatives and second-order spatial jets plus rational
acceleration; no candidate mathematical imports, B/T/curvature or result inputs.

- R1: Factor first-derivative projection and observation preparation. Transform
  calls build B/B', image values/J and robust residual/J only. They must not build
  a second spline derivative, B'', HP/HT/Hcam/image H, scalar objective G/H, or
  normalized constraint z/Jz/Hz/bounded chain. Perspective camera-z needed to
  project remains allowed. Put separately instrumentable boundaries around the
  forbidden work so throwing sentinels can verify the production path.
- R2: Full preparation retains B/B'/B'', trajectory/projection/chain intermediates
  once per complete context. Assemble every coefficient, offset and mixed term,
  physical/q/y arrays, raw and bounded weighted curvature. Never let a component
  rebuild observation geometry. Full values are authoritative image plus Decimal
  acceleration, not reconstructed from exported r/J. Normalize center/diameter
  and apply free-offset D=25 once.
- R3: Extract `cold_transform(model, cold_state, audit)` and a production metric
  builder callable by worker and toys. The former calls real
  `Candidate.evaluate(..., transform_only=True)` once; the latter checks
  rows >= coefficient columns before one SVD, with no second-SVD fallback anywhere.
  Preserve deterministic rank/group/projector policy, active inverse singular
  scales clipped `[1e-3,1e3]`, inactive scales one, full symmetric coefficient P,
  nuisance identity/cross zeros, inverse and cold origin. `TransformCache` caches
  this complete entry; repeated requests cannot prepare residuals or perform SVD.
- R4: Explicitly document canonical `request` returns as physical components
  (with `R.frames` naming x/q/y). Worker admitted traversals call actual `fun`,
  `jac`, `hess`, `constraint`, `constraint_hessian` and `public_report`; their
  derivatives are y-coordinate arrays. Add an explicit public report method.
  Preserve [Plan 021 section 5](plan_021.md)'s five request orders with six surfaces, one repeat,
  one replay probe per adapter and canonical fallback upon rejection. Actual
  wrappers, not `request(public=True)` labels, establish public API coverage.
- R5: Own deeply frozen copies of state, multiplier, scale/P/origin/inverses,
  saved bound inverse, case and setup operands. Include scope and transform in
  immutable identity/cache keys. Caller mutation cannot change calculation or
  report; frozen results cannot be made writable. At actual decoder/calculator
  boundaries record the decoded x, coefficient slice, vg/vby and geometry/setup
  operands actually consumed. Compare these to admitted bytes before math;
  copying the expected state dictionary to `Audit.observe` is insufficient.
  For derived q-only boundaries retain derivation links; do not independently
  round-trip x to invent q. Journal entered/observed/completed/interrupted records
  with unique parent IDs and subentry ordinals. Reject wrong state/multipliers,
  source, scope or setup bytes before their dependent math. Keep both 200 caps.

Mock `solve` must check callback and returned state/multiplier identity before
claiming successful completion, and retain coherent cached F/G/KKT from ordinary,
completed and interrupted paths. Wrong return state/multipliers yield explicit
error and unsuccessful status; any retained last valid report is labeled as such.
Do not erase errors because `owned_report` can still return an older cached result.

## 5. Frozen 20-group readiness and complete toy inventory

Before suite 1, save `toy-definitions.json`, `readiness-coverage.json`,
`coverage-source-review.md`, `comparison-schema.json` and `expected-schedule.json`.
For every R1–R8 requirement, map a stable fixture/subcase ID to exact production
call sites, independent low-level counters/sentinels, concrete expected fields,
assertion IDs and planned entry counts below. Source-only inspect the entire
worker call graph through public wrappers, transforms, real calculators, audit,
comparison driver and decision reducer. Check that no required row is satisfied
only by a dummy provider, manually assigned counter or a `passed=True` stub.
Send parent these paths for source inspection during the active stage; no new
agent, user question or routine approval is needed. Save the inspection before
freezing suite 1's definitions/test hash. Readiness later cross-checks actual
observations/assertions with this map rather than trusting its aggregate flag.

Use Plan 021 section 7's fixed fixtures 1–11, with the exact inventory below.
No random data or arithmetic ladder. For the integrated scene fix:

- Cubic Bezier U=`[0,0,0,0,1,1,1,1]`, Q=`[0,0.5,1]`, one group, C rows
  `[[0,0,4],[1,0,4],[1,1,5],[2,1,6]]`, free camera `[3]`, both offsets zero,
  center `[1,-2,3]`, diameter 2, n=12, nacc=3, dimension 13.
- Cameras 1 then 3 each have frames `[0,5,10,15,20,25]`, xy all zeros. Camera 1
  has R/K identity, t=0, k=0. Camera 3 R maps `(X,Y,Z)` to `(-Y,X,Z)`,
  t=`[1,-1,2]`, K=`[[2,0,0.25],[0,3,-0.5],[0,0,1]]`, k=`1/8`.
- x=`[0]+row_major(C)`, q has identical bytes because its offset is zero;
  D=`diag(25,1,...,1)`. Saved origin zero, P offset block 1, first two
  coefficient-coordinate block `[[1.25,0.75],[0.75,1.25]]`, remaining coefficient
  diagonal 2, other entries zero. Saved inverse has that 2x2 block
  `[[1.25,-0.75],[-0.75,1.25]]`, remaining coefficient diagonal 0.5, offset 1.
  Saved y is zero for offset and first two coefficients, remaining coefficients
  divided by 2. Serialize all operands as little-endian binary64 with stdlib
  struct. This dyadic saved replay is exact without searching.
- Fixed actual vg[i]=`(i+1)/8` for i=0..11 in camera/frame order;
  vby[j]=`(-1)^j*(j+1)/16` for j=0..12. These dyadic formulae freeze all bytes.
  Use both weight 0 and 1. Toy cold-transform state uses this same q/x with a
  distinct cold scope; Pnew origin equals this q, so ynew=0 exactly replays it.
  Original vby remains tied to saved P; new-P remapping is only an algebra check.

The integrated oracle constructs B directly from the four closed cubic Bernstein
polynomials, B' and B'' from their analytic derivatives, and T/second derivatives
from `-B'C/25`, `B''C/625`, `-B'/25`. It does not call `basis`, Candidate.prepare
or use their outputs. Assemble the image/residual/depth spatial chain using the
already analytically checked independent `spatial` jets, once per 12 observations,
then retain those data/geometry oracle arrays across both weights. Independently
assemble acceleration F/G/H/r/J from the closed second-derivative row
`[6*(1-t), -12+18*t, 6-18*t, 6*t]` in the three fixed Q points. Build full x/q/y
and weighted curvature/force oracles once per weight/needed transform; compare
every entry of both full calculators. Self-consistency alone is insufficient.

| Group/subcases | Real calls and required assertions |
| --- | --- |
| 01–05 | Preserve constant, linear, quadratic, cubic and 2^40 translated kernel fixtures and complete F/G/H/r/J/null checks from Plan 021. One setup and bundle each. |
| 06 | Preserve 18-row Greville fixture, all 54 G and 54x54 H entries; one setup/bundle and five independent order-2 basis calls at its fixed Q. |
| 07a–d | One nonuniform setup/bundle; two independent endpoint basis calls; one repeated-knot setup/bundle; domain and zero-denominator setup rejections before mapping. |
| 08 | One weight-zero setup/bundle, exact zero/empty outputs; throwing acceleration-map sentinel never entered. |
| 09a–c | Two case-4 setups/bundles under default and changed ambient Decimal contexts, identical outputs and restored context; one explicit decode of 0.1/-0; four output rejections NaN/Infinity/1e400/1e-400 and two invalid decode rejections. |
| 10 | Existing fixed Jet polynomial, reciprocal and sqrt analytic first/second derivative checks; no spatial scene or evaluator call. |
| 11a–c | Two existing camera projection-second/spatial/robust-residual comparisons; one extra identity projection-second analytic check. Retain their fixed zero-center/unit-diameter oracle checks as prerequisites. |
| 12a–b | One real complete Candidate and one real complete Reference per integrated weight, separately owned setups, plus each real report. Check all full/split F/G/H/r/J x/q/y and every offset/mixed entry against the retained closed-form oracle. Retain objects/results for later assertions. |
| 13a–c | Reuse group 12 outputs; compare z/Jz/g/gp/gpp/vd/Jg/weighted Hz/Hg, actual forces, KKT vector/norm and complementarity, all entries/frames. Check saved transform algebra and original vg/vby. One fixed `bounded([1e200])` underflow rejection. Reuse group 14 Pnew/report outputs when available for remap assertions; no extra evaluator calls. |
| 14a–j | Five fresh real Candidate adapters for each weight, exact Plan 021 order and repeats using all six actual public callbacks. The first request invokes real provider exactly once. Sequence 5 invokes real cold_transform/TransformCache, once per weight, then cache repeat with throwing calculator. Instrument real preparation/projection/B''/depth/objective/acceleration/SVD boundaries independently; transform-only forbidden boundaries throw. Assert actual metric rank threshold/scales/full subspace coverage, inverse/symmetry and exact nuisance/inactive contracts. One additional fixed zero matrix of shape (1,12) calls the same metric entry and rejects before SVD. |
| 15a–i | For each weight reuse the exact-replaying group 14 sequence-1 real adapter; a mock optimizer calls fun/jac/hess/constraint/constraint_hessian and callback, then returns the same state/vectors. Repeat as interrupted-after-callback, wrong-state-return and wrong-multiplier-return cases (eight total mock calls). All use caches with no complete entries; reports/identities/actual multiplier bytes agree, wrong returns stay errors. Separately call real report plumbing once on the fixed six-variable deliberate cached fixture with F=7, G=[1..6], r=[100,100], J=ones, vg=[3,2], vd=[0.3,0.4], inverse/P identity and vby=0 from v14 case 15; F/G must remain authoritative, not r@r/2J.T r. |
| 16a–e | Preserve adjacent binary64 1/next bit, signed zero, scope and cross-ledger identity cases. Mutate original dictionaries/arrays backing group 12 case/setup and group 14 input/P/origin/inverses/saved bound inverse; owned bytes and retained repeats stay unchanged, direct writes/setflags to owned arrays fail. Two fresh real Candidate adapters, one per weight, compute the same integrated state once each to prove no cross-adapter result reuse; both then repeat without low-level entries. Reference case/setup ownership is inspected on group 12 objects without reevaluation. |
| 17 | Exactly 200 integer-labeled dummy states and iteration labels; attempted 201st state and iteration rejected before sentinel math, no evaluator. |
| 18a–f | For each real method separately: one allocated complete attempt with decoder-hook wrong actual coefficient/x bytes, rejected by production audit before preparation math; one allocated complete attempt whose actual prepare boundary throws, retaining consumed/interrupted entry. Expected labels remain correct so the actual-byte observer is necessary. Also reject wrong vg/vby on existing real adapters, wrong actual setup operands through the production setup-entry verifier before setup math, duplicate/unallocated entries and wrong scope/source using fixed byte fixtures. No extra full calculation or setup. |
| 19a–e | Real shared chain verifier/launch path on tiny temporary manifests: intact chain, mutated input, auth, source and missing coverage row; invalid cases reject before invocation marker/spawn. Large-origin `2^54` rejected replay uses a tiny cached six-variable provider and public wrappers, zero provider entries on rejection, then one canonical dummy provider entry. Real comparator and retained validator/reducer execute the exact positive/negative subcases below. |
| 20a–d | Real supervisor with fixed tiny sleeping process tree in deadline and SIGTERM-interruption modes, each <=3 seconds with 2-second reserve; partial logs retained and no descendants. Direct worker rejection before numerical import and exclusive marker/collision checks on temporary roots. No evaluator arithmetic. |

Group 13's new-P assertions can be finalized after group 14 produces its retained
results; group numbering is a coverage identity, not permission to recompute.
Any failed prerequisite prevents dependent toy arithmetic, retaining missing rows.

### Exact per-suite numerical inventory

These are planned entries, not a pool to spend on other tests. Every attempted
entry is counted even if it throws. Maximum actual work fits Review's 20 Candidate,
6 Reference, 2 transform/metric and 2 SVD ceilings:

| Work | Frozen count and owner |
| --- | --- |
| Successful complete Candidate bundles | 14: group 12 two, group 14 ten, group 16 two; seven each weight |
| Failed Candidate complete entries | Two group 18; wrong-decoder before preparation and interrupted preparation before geometry math; total complete attempts 16 |
| Successful complete Reference bundles | Two group 12; one each weight |
| Failed Reference complete entries | Two group 18 as above; total complete attempts four |
| Integrated Candidate geometry/acceleration setups | Two shared immutable keys weight 0 then 1; one active four-column map/H construction |
| Integrated Reference setups | Two keys weight 0 then 1; active setup three scalar order-2 basis rows and one 4x4 rational H construction |
| Real cold residual/J-only, metric and SVD | Two each, group 14 only; two TransformCache repeats, zero repeated numerical work |
| Rejected undersized metric | One group 14, no residual provider/setup/SVD entered |
| Complete Candidate preparation/residual/data-H/full-sum/raw-depth/bounded-values/weighted-Hz/bounded-Hg | 14 each; plus the two transform first-order preparation/residual paths; one additional failed pre-math preparation in group 18 |
| Complete Reference preparation/data/residual/data-H/full-sum/raw-depth/bounded-values/weighted-Hz/bounded-Hg | Two each; one additional failed pre-math preparation in group 18 |
| Integrated active acceleration traversals | Candidate eight (seven complete, one transform); Reference one; weight-zero paths never enter active acceleration |
| Kernel-only dc.setup attempts, groups 01–09 | 13: 11 successful (10 active, one inactive), two group 07 rejections before mapping |
| Kernel-only dc.bundle calls, groups 01–09 | 11: 10 active, one inactive; these are additional to integrated acceleration traversals |
| Kernel setup mapping calls | 57 unit-column mappings: 20 in groups 01–05, 18 in 06, 11 in 07, eight in 09; integrated active setup adds four, total 61 |
| Kernel-only direct Reference.basis | Seven: five group 06 order-2 and two group 07 endpoints; no hidden setup/bundle |
| Integrated Reference basis | Three active setup rows plus 72 observation calls (two complete bundles x12 observations x3 derivative orders); oracle uses closed polynomials instead |
| Standalone spatial/projection/residual helpers | Group 11: two spatial, three projection-second, two robust-residual/J; integrated oracle: 12 spatial once. Real Reference adds 24 spatial; Candidate complete adds 28 two-camera projection/residual batches and transforms add four first-order batches |
| Bounded helper outside full bundles | One group 13 underflow rejection; no other standalone bound experiment |
| Report/transport assembly | 14 real Candidate primary reports (12 saved-P and two Pnew), two additional sequence-5 saved-P reports, and two real Reference saved reports. Two new-P independent algebra contexts using retained q oracle/reference; one dummy authoritative report in group 15. Other callbacks/reports are cache-only |

Kernel setup totals therefore are 15 Candidate setup attempts including integrated
keys (13 successful, two rejected), separately from two Reference setups and
zero-math setup-identity rejections. Direct group 10 Jet operations and analytic
oracle array arithmetic are exactly the fixed expressions above, not complete
bundles. Low-level count assertions bind real call entry; assigning expected
integers to counters does not qualify. Keep full numerical inventory separate
from fixed byte-only guard/mutation/ledger cases.

Toy analytic comparisons keep atol `1e-12`, rtol `1e-11`, Decimal internal
absolute `1e-65`, and exact byte/zero/identity contracts. The production comparison
driver is also applied once to each of the 14 retained Candidate reports against
the corresponding already retained independent full Reference report (sequence
5's saved-P report), and once per weight for new-P; no recalculation. Preserve
all actual passing/failing entries and hashes, not only aggregate maxima.

### Group 19 comparator and decision subcases

Use a fixed tiny shape manifest m=2, n=2, data residual rows=4, acceleration rows=0,
cold/returned labels. A synthetic complete report has F/Fdata=1, Facc=0; all G,
J, Hz/Hg, forces and complementarity zero; H/Hdata identity, Hacc zero;
z=[1,2], g=[0.5,0.75], gp=[0.25,0.125], gpp=[-0.125,-0.0625], vg=[1,2],
vd=[0.25,0.25], vby=[0,0], all r zero, all KKT norms zero, P/inverse identity.
This is comparator protocol data, not a physically claimed evaluator result.
Freeze ten actual production comparison-driver calls:

1. Complete identical returned pair passes every required field.
2. Missing `frames.y.KKT_inf` is incomplete, never vacuous pass.
3. Jg shape `(2,1)` against required `(2,2)` is an integrity/shape failure.
4. F=2 against reference 1 is a recorded numerical failure.
5. Strict new-P Hg candidate diagonal `1+2^-15` against reference diagonal 1
   passes its prescribed bound; use full new-P driver with complete tiny fields.
6. Same strict new-P Hg with `1+3*2^-16` fails `(2e-5,2e-5)`; it must not use
   objective-H tolerance `(2e-5,3e-5)`.
7. New-P H with off-diagonal `[0,1]=2^-20`, `[1,0]=0` fails symmetry separately
   even if elementwise H tolerance passes. Check both candidate/reference H sides.
8. Weight-zero Gacc[0]=`2^-40` fails exact zero despite approximate tolerance.
9. Asymmetric comparison-policy fixture inside the production driver: candidate
   2, reference 1, atol=0, rtol=0.5 must fail (reverse orientation would pass).
   This explicit test-only policy never changes the scientific schema.
10. Rational-policy fixture: candidate binary64 `1/3`, exact reference Fraction
    `(1,3)`, atol=0, rtol=0 fails exact forward error despite equal rounded bytes;
    scientific rational tolerances remain unchanged.

Freeze eight actual retained-validator/decision-reducer invocations on a tiny
schedule containing one candidate cold/returned report, one reference pair,
one public replay, one transform/new-P comparison and complete entered/actual/
completed/hash receipts: complete passing; one missing report; duplicate identity;
interrupted context; wrong output hash; missing required comparison check; known
numerical failure plus missing result; public rejection with otherwise complete
canonical evidence. The last two must retain failure separately from completeness.
These use hand-authored bytes/JSON, never a fake `passed=True` readiness substitute.
Use the same parameterized validator as the fixed full schedule, with explicit
tiny expected identities/schema. Include the missing-summary path in the missing
result variant and verify absent summary cannot produce zero-failure success.

## 6. Unchanged scientific schedule and exact receipts (R5/R8)

[Plan 021 sections 3–6](plan_021.md) remain normative for cohort, independent math,
request order, scopes, 200-state/200-iteration caps and every scientific low-level
ceiling. The fixes above add observations/receipts, not operating calculations.
Freeze this exact manifest before toys, with per-case metadata and source hashes:

1. Candidate setups weight 0 then 1. For case 0..15, cold complete/report, then
   returned sequences 1..5. Sequence 5 first owns cold T/residual/J/SVD/Pnew;
   sequences 1..4 use saved P. Each returned sequence owns saved inverse and one
   public replay probe, then its prescribed six actual callbacks or explicit
   canonical fallback, then one identical traversal repeat. Repeat T is a cache
   hit. Returned reports retain original saved bound multipliers, and sequence 5
   adds its already allocated saved-transform report transport. Close Candidate.
2. Reference setups weight 0 then 1, then case 0..15 cold and returned complete
   reports in order, independent saved inverse only for returned. Close Reference
   before any comparison reads Pnew. Independent mathematics never reads a
   candidate output; no candidate helper imports or cross-method result caches.
3. Consume 32 component comparison contexts in case/cold-returned order before
   exact rational transport or other comparison arithmetic. Cold context yields
   one canonical comparison, returned context yields five, total **96**. Include
   one separately labeled historical comparison per returned context using the
   already completed independent returned result. No new evaluator entry.
4. Consume 16 new-P comparison contexts in case order before transport/math.
   Transport the completed independent q components through retained Pnew and
   compare every required field, norm, symmetry and remap/transform contract.

| Boundary | Candidate exact full-completion count | Reference count |
| --- | ---: | ---: |
| Setup keys | 2 | 2 |
| Complete bundles | 96 (16 cold +80 returned) | 32 |
| Transform-only contexts | 16 | 0 |
| Observation preparations | 112 (96 full,16 first-order only) | 32 |
| Objective/data-G and residual/J | 96 | 32 |
| Additional residual/J-only | 16 | 0 |
| Data H / full sum / raw depth-J / bounded values / weighted Hz / bounded Hg | 96 each | 32 each |
| Returned reports / cold reports | 80 /16 | 16 /16 |
| SVD and Pnew/inverse | 16 | 0 |
| Saved inverse | 80 | 16 |
| Public replay probes | 80 (64 saved-y,16 new-y) | 0 |
| Extra saved-P report transports | 16 | 0 |
| Active acceleration traversals | 49 (42 full +7 transform) | 14 |
| Component / new-P comparison contexts, driver only | 32 /16 | 0 |

For each context/low-level boundary/request/comparison record use deterministic
ID `(method,case,label,sequence,scope,kind,ordinal)` plus allocation/source/input/
state/multiplier/setup/transform hashes. Durably consume entry before math,
observe actual operands, exclusively publish result then completion hash. Record
skipped/inapplicable fields explicitly by frozen schema. Public request identity
includes actual wrapper name, requested coordinate frame, y bytes and probe
receipt; repeated values have equal bytes and zero new low-level entries.
Retain actual wrapper return bytes as well as hashes. Inside the already
allocated component/new-P comparison contexts compare admitted public O/G/H/C/CH/R
returns to the independent y-coordinate fields, including both members of C;
canonical fallbacks use their explicitly physical component semantics. Comparing
only a report while discarding actual wrapper outputs cannot qualify the public
surface. These checks add no evaluator or comparison contexts.
Transform/preparation repeats have independent counters and throwing sentinels
in toys. Complete qualification needs exact identities and order, not counts
below caps or a worker-complete file. A mismatched entry or output cannot be
rescued by an aggregate summary. Match each completed receipt to its artifact,
all 96 canonical comparisons, 80 replay decisions, 16 new-P comparisons and 16
historical corroborations. Reconcile unknown/interrupted records explicitly.

## 7. Central comparison schema and decisions (R7/R8)

One frozen production `comparison_spec` drives canonical, new-P, archive and
toy protocol validation. No iteration over only fields found in an output.
For scientific n=300, m=54/55, nrdata=600, nracc=450 at weight 1 or zero at
weight 0, require these keys/shapes (and toy substitutions from section 5):

- Scalars F/Fdata/Facc; z/g/gp/gpp/vg/vd `(n,)`; rdata `(2n,)`, racc `(nracc,)`,
  r `(2n+nracc,)`; frames exactly x/q/y. Returned vby `(m,)` and complementarity
  `(n,)`; cold vby/complementarity explicitly null and no KKT/force claims.
- Every frame: G/Gdata/Gacc `(m,)`; H/Hdata/Hacc/Hz/Hg `(m,m)`;
  Jdata `(2n,m)`, Jacc `(nracc,m)`, J `(2n+nracc,m)`, Jz/Jg `(n,m)`.
  Returned Cd/Cb/KKT `(m,)`, KKT_inf scalar in each frame. No skipped new-P norm.
- Acceleration exact oracle at active states: F rational pair, all 54 G rational
  pairs, scalar-axis 18x18 H rational pairs with explicit expansion to all axes
  and zero offset/cross terms. Retain every rational transported Gacc/Hacc entry.
- Transform records: P/inverse `(m,m)`, origin `(m,)`, singular/scales/active
  `(54,)`, rank threshold scalar, group indices/bases/projectors covering every
  coefficient direction, coverage `(54,54)`. For toys substitute 12 coefficients.
  Extra new-P report retains actual vby, remapped multiplier and back-map `(m,)`
  with an explicit algebra-only role and saved bound-transform identity.

For candidate v and independent r, `abs(v-r) <= atol + rtol*abs(r)` with separate
binary64 multiply then add, no FMA/symmetric max. For rational r_exact use
`abs(Fraction.from_float(v)-r_exact)` against
`Fraction.from_float(atol + rtol*abs(float(r_exact)))`. Fail nonfinite/overflow;
retain shapes, both value bytes, every error/allowed byte or rational pair and
failed indices. Dense shape-tagged little-endian arrays are allowed storage;
they must retain every element and exact rational oracle, with hashes.

| Applicable field, in x/q/y including new-P | atol | rtol |
| --- | ---: | ---: |
| F/Fdata/Facc | 1e-10 | 1e-9 |
| G/Gdata/Gacc, Cd/Cb/KKT/KKT_inf, complementarity | 1e-10 | 1e-8 |
| z/g | 1e-12 | 1e-12 |
| vg/vd/vby, remapped multipliers/back-map | 1e-12 | 1e-10 |
| H/Hdata/Hacc | 2e-5 | 3e-5 |
| Jz/Jg/gp/gpp | 2e-6 | 2e-5 |
| Hz/Hg, including new-P | 2e-5 | 2e-5 |
| r/rdata/racc, J/Jdata/Jacc | 2e-5 | 2e-4 |
| P inverse product vs identity; full coefficient coverage | 1e-10 | 1e-10 |
| P vs P.T, saved and new | 1e-12 | 1e-12 |
| All five H-family matrices vs transpose, each calculator/frame | 1e-10 | 0 |

Check both inverse-product orientations using already owned inverse and P in
the existing comparison context; no extra inverse. Nuisance identity/cross zeros,
inactive scales one, inactive Facc/Gacc/Hacc, empty acceleration residual/J blocks,
zero offset acceleration rows/columns, owned byte identities and repeat outputs
are exact contracts, not approximate comparisons. Exact numerical zero need not
change a retained signed zero; state/cache equality always compares exact bytes.
New-P norm is max(abs(independently transported KKT)), not transported scalar.
Original actual vg/vby also match admitted bytes exactly before approximate
cross-calculator tests. Remapped `Pnew.T@Cbq` and inverse-transpose back-map are
explicit algebra checks only; do not replace actual historical vby.

Historical corroboration uses archive as left operand and independent returned
value as right operand, once in each returned comparison context. Map
`objective -> F`, `G -> frames.q.G`, `C_depth -> frames.q.Cd`,
`C_bounds -> frames.q.Cb`, `KKT -> frames.q.KKT`,
`optimality -> frames.q.KKT_inf`, `complementarity_original -> complementarity`,
`multipliers_depth -> vd`, `bound_multipliers -> frames.q.Cb` (the historical
solver stored inverse-transformed bound force here, not conditioned vby).
Use the relevant category tolerance above and retain historical `qualified`
unchanged. Missing archive fields are explicit incomplete corroboration. Archive
disagreement does not fail a v15 canonical component that agrees with independent
math, or revise old qualification; it is its own named stratum.

Retained-only `validate_package(expected, retained)` checks hashes/schema/exact
schedule and returns evidence to a pure `reduce_decision` shared by worker/package
and group 19. Required separate dimensions are source/coverage readiness,
package integrity, schedule completion, canonical numerical qualification,
public API qualification, transform algebra and historical corroboration. Each
dimension records completeness and known failures separately. Missing evidence
is incomplete; an observed mismatch stays failed even if another result is absent.
Overall `qualified_on_fixed_cohort` requires complete admissible schedule and all
canonical/public/transform checks passing. Complete but failed required science
is `rejected`; partial execution is `incomplete` with retained failures explicit.
Historical numerical disagreement alone cannot veto otherwise qualified v15;
missing required historical evidence still makes package/schedule incomplete.

## 8. Execution, packaging and acceptance

After passing final readiness, `run` revalidates the full chain before exclusive
`started.json`, consuming the single invocation. Supervisor starts its owned
process group; worker revalidates tokens/PID/PGID/SID/source/input/auth graph
before numerical import and exclusive worker marker. Numerical deadline is
`min(start+1500,T0+6300)-5`; cleanup ends by the corresponding full deadline.
Check and retain sources/operands before every entry. Numerical mismatches may
allow remaining already scheduled independent work within this invocation;
integrity/ownership/source/permission/interruption/deadline failures stop dependent
work immediately. No replacing a rejected public state or post-hoc repair.

`package` must retain failed admission/readiness/toys and partial science even
when no last passing toy exists. Use available JSON/bytes/hashes only; create an
explicit missing-artifact list rather than raising before recording a decision.
Reconcile all consumed slots, entered/observed/completed/interrupted/unknown IDs,
output hashes, toy results, worker identity/exit/cleanup and phase budgets. A
retained incomplete run can have valid package integrity; missing required
qualification outputs remain incomplete. Package must not recompute components,
transform/inverses, rational math, forces or numerical comparisons.

| Acceptance | Observable evidence / criterion |
| --- | --- |
| A1 admission | Complete original provenance and current auth/input/source chain, or explicit blocking missing evidence; SC-05, prerequisite SC-01/03 |
| A2 repaired readiness | R1–R8 source map and actual twenty-group coverage with finite exact inventory; missing rows prohibit science; SC-05 |
| A3 scientific adjudication | Exact scheduled independent arrays/reports/replay/transport checks with frozen tolerances, or explicit partial/failure receipts; prerequisite SC-01/03 |
| A4 retention | Honest separate decisions, complete available hashes/counts/budget/process evidence, immutable history, no owned jobs; SC-05 |
| A5 milestone | Source/retained JSON/hash/link and whitespace checks plus validated task-only local commit with title and description; SC-05 |

Source checks/readiness passing do not imply numerical qualification. A fully
retained numerical rejection or incomplete bounded execution is a reviewable
implementation milestone, not successful main-objective attainment. Parent
reassesses every criterion; SC-01/SC-03 remain not met until their complete saved
outcomes exist. Record exact toy/science/setup/component/reference/comparison
times, phase elapsed including commits, zero forbidden/training work, and any
work that could not be confirmed stopped. Stage explicit task paths via separate
escalated git add, inspect staged diff, then separate escalated git commit under
AGENTS.md; parent and implementer coordinate one owner. Git failure stops the loop.

Under every outcome publish `accepted_timing=null`, `production_candidate=null`,
`final_validation_protocol=null`, `ready_for_full_screens=false`,
`main_objective_attained=false`. Preserve v11 A2/A3 failures, v12 failures, the
other 57 unadjudicated ray failures and v13's limited six costs/36 partials.

If the cohort qualifies, next Review selects one finite full-coordinate focused
solver intervention and fresh baseline using retained starts/KKT, including
weight-zero initialization/stopping/observable-growth gaps. If canonical math
fails, target the recorded component/coordinate defect; if only public replay
fails, select a finite solver-coordinate/ownership design; if incomplete, name
the precise missing readiness/schedule prerequisite. Do not enlarge the passing
cohort, tune tolerances or default to another precision ladder.

All [Plan 015 sections 6 and 8](plan_015.md) gates remain binding: focused targets
and dependencies within both 200 caps, all-three-path agreement and preserved
controls/costs; missing seeds/transform/support/growth/initialization/stopping
resolved before screens. Full conditioning remains 144 attempts/48 problems,
all 94 prior v6 qualifications, at least 25/50 recoveries and median KKT ratio
<=0.1. Independent scalar screening retains 7,344 initial paths, inclusive
0.05/0.01 refinements and all nine basin/cost comparisons. Both screens precede
144-path combined pilot and later evaluator/selection/final-validation gates.
KKT <=1e-6, qualification depth >1e-7, competitive objective tolerance
`1e-6 + 1e-4*max(abs(F1),abs(F2))`, splits and held-outs remain unchanged.
After this finite allocation parent continues authorized Review/Plan automatically
unless an actual loop stop applies; a commit or individual plan completion is
a checkpoint.

## Plan-stage record

Read local instructions/objective/status/assessment/Review, Plan 021, v14
source/tests and relevant v10 reporting source. Only source/Markdown/retained
JSON inspection and documentation were performed: no scientific import/math,
test, evaluation, fit, training, device/network/install, Git mutation or job.
Only this new plan and its iteration plan-link artifact are written by Planner.
No implementation T0 has started, no scope/budget blocker is identified, and no
jobs remain. Parent performs the final applicability comparison and fresh
Implementation dispatch under standing approval.
