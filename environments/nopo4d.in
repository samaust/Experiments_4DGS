# Compatibility probe, not the complete model/backbone installation.
# Upstream metadata (including NumPy restrictions) must also resolve unchanged
# or be deliberately ported; this file does not override that metadata.
-r base-cu130.in
numpy
einops
huggingface_hub
jaxtyping
gsplat
xformers
