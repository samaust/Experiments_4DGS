# Python dependencies only; apply/build the tracked STG CUDA patch separately.
# Bundled MMCV KNN is deferred for checkpoint rendering, not ported for training.
-r base-cu130.in
numpy
scipy
scikit-image
opencv-python
Pillow
plyfile
kornia
natsort
tqdm
yapf
